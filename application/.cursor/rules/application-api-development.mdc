---
alwaysApply: false
description: application 接口开发规则 - API、Controller、Entity 创建标准化流程
---

# Application 接口开发规则

`application/` 目录是项目的 Web 应用层，采用 GoFrame v2 框架，遵循 RESTful API 设计原则。

## 目录结构

```text
application/
├── api/[module]/v1/           # API 接口定义
├── internal/controller/[module]/  # 控制器实现
├── internal/model/entity/     # 实体模型
└── internal/cmd/web.go        # Web 服务注册
```

## 开发流程

### 1. API 接口定义

**文件位置**: `api/[module]/v1/[module]_[action].go`

**参考示例**: [setting_get.go](mdc:application/api/setting/v1/setting_get.go)

**关键规范**:

- 请求结构体: `[Action][Module]Req`
- 响应结构体: `[Action][Module]Res`
- 操作类接口使用空响应结构体: `type UpdateSettingRes struct {}`
- 时间字段统一使用 `*time.Time` 类型

### 2. 模块接口定义

**文件位置**: `api/[module]/[module].go`

**参考示例**: [setting.go](mdc:application/api/setting/setting.go)

### 3. 实体模型

**文件位置**: `internal/model/entity/[module].go`

**参考示例**: [setting.go](mdc:application/internal/model/entity/setting.go)

**关键规范**:

- 实现 `UnmarshalValue` 方法用于数据转换
- 时间字段使用 `*time.Time` 类型

### 4. 控制器实现

**文件位置**: `internal/controller/[module]/[module]_v1_[action].go`

**参考示例**:

- [setting_v1_get_setting.go](mdc:application/internal/controller/setting/setting_v1_get_setting.go)
- [setting_v1_update_setting.go](mdc:application/internal/controller/setting/setting_v1_update_setting.go)

**关键规范**:

- 构造函数: [setting_new.go](mdc:application/internal/controller/setting/setting_new.go)
- 操作类接口成功时返回空结构体: `return &v1.UpdateSettingRes{}, nil`
- 失败时通过 `err` 返回，让 GoFrame 自动处理错误响应

### 5. Web 服务注册

**文件位置**: `internal/cmd/web.go`

**注册步骤**:

1. 导入控制器包: `"application/internal/controller/[module]"`
2. 绑定控制器: `group.Bind([module].NewV1())`

## 核心设计原则

### 响应结构体设计

1. **操作类接口**（创建、更新、删除）:
   - 使用空响应结构体
   - 成功时返回 `&ResponseStruct{}`
   - 失败时通过 `err` 返回

2. **查询类接口**:
   - 返回具体的数据结构
   - 时间字段使用 `*time.Time` 类型

3. **时间字段处理**:
   - 统一使用 `*time.Time` 类型
   - 支持 `nil` 值表示未设置状态
   - 从 `gtime.Time` 转换为 `*time.Time`: `&model.CreatedAt.Time`

### 参数验证规范

**重要**: 判断传入参数是否为必填时，必须先查看对应的 Model 定义。如果 Model 中定义了必填字段，则该参数为必填。

#### 1. 基于 Model 定义的参数验证

```go
// ✅ 正确 - 基于 Model 定义进行参数验证
func (c *Controller) CreateDevice(ctx context.Context, req *v1.CreateDeviceReq) (*v1.CreateDeviceRes, error) {
    // 查看 s_db_model.SDeviceModel 的定义
    // 如果 Model 中 Name 字段有 required 标签或非空约束，则验证必填
    if req.Name == "" {
        return nil, errors.New("设备名称不能为空")
    }
    
    // 如果 Model 中 Type 字段有 required 标签，则验证必填
    if req.Type == "" {
        return nil, errors.New("设备类型不能为空")
    }
    
    // 如果 Model 中 Enabled 字段有默认值，则可以为空
    // 如果 Model 中 Enabled 字段没有默认值且非空约束，则验证必填
    
    deviceId, err := s_db.GetDeviceService().CreateDevice(ctx, req.Name, req.Type, req.Enabled)
    if err != nil {
        return nil, gerror.WrapCode(gcode.CodeInternalError, err, "创建设备失败")
    }
    
    return &v1.CreateDeviceRes{Id: deviceId}, nil
}
```

#### 2. Model 字段验证规则

**查看 Model 定义的方法**:

1. **检查结构体标签**:
```go
type SDeviceModel struct {
    Name    string `json:"name" orm:"name" required:"true"`     // 必填
    Type    string `json:"type" orm:"type" required:"true"`     // 必填
    Enabled bool   `json:"enabled" orm:"enabled" default:"true"` // 有默认值，非必填
}
```

2. **检查数据库约束**:
```sql
CREATE TABLE device (
    name VARCHAR(255) NOT NULL,     -- 必填
    type VARCHAR(255) NOT NULL,     -- 必填
    enabled BOOLEAN DEFAULT 1       -- 有默认值，非必填
)
```

3. **检查字段类型**:
```go
// 指针类型通常表示可选字段
StartTime *time.Time `json:"startTime"`  // 可选
// 非指针类型通常表示必填字段
Name      string     `json:"name"`       // 必填
```

#### 3. 参数验证最佳实践

```go
// ✅ 正确 - 基于 Model 定义进行验证
func (c *Controller) UpdateDevice(ctx context.Context, req *v1.UpdateDeviceReq) (*v1.UpdateDeviceRes, error) {
    // 1. 先查看 SDeviceModel 的字段定义
    // 2. 根据 Model 中的约束进行验证
    
    updateData := map[string]interface{}{}
    
    // 如果 Model 中 Name 字段有 required 标签，则验证
    if req.Name != "" {
        updateData[s_db_model.FieldName] = req.Name
    }
    
    // 如果 Model 中 Enabled 字段有约束，则验证
    if req.Enabled != nil {
        updateData[s_db_model.FieldEnabled] = *req.Enabled
    }
    
    err := s_db.GetDeviceService().UpdateDevice(ctx, req.Id, updateData)
    if err != nil {
        return nil, gerror.WrapCode(gcode.CodeInternalError, err, "更新设备失败")
    }
    
    return &v1.UpdateDeviceRes{}, nil
}
```

#### 4. 参数验证检查清单

- [ ] **查看对应 Model 定义**: 检查 `s_db_model` 中的相关 Model 结构体
- [ ] **检查结构体标签**: 查看 `required`、`default` 等标签
- [ ] **检查数据库约束**: 查看 SQL 创建语句中的 `NOT NULL`、`DEFAULT` 约束
- [ ] **检查字段类型**: 指针类型通常表示可选，非指针类型通常表示必填
- [ ] **验证必填参数**: 根据 Model 定义验证必填参数
- [ ] **处理可选参数**: 根据 Model 定义处理可选参数
- [ ] **使用字段常量**: 在验证和更新时使用 `s_db_model` 中的字段常量

### 错误处理

- 参数验证错误: `errors.New("参数不能为空")`
- 业务逻辑错误: `gerror.NewCode(gcode.CodeNotFound, err, "资源不存在")`
- 内部错误: `gerror.WrapCode(gcode.CodeInternalError, err, "操作失败")`

#### 错误处理最佳实践

**重要**: 当返回 error 时，不需要在控制器层重复添加日志输出。GoFrame 会自动处理错误响应。

```go
// ✅ 正确 - 参数验证错误，直接返回，不记录日志
if req.Id <= 0 {
    return nil, errors.New("自动化任务ID必须大于0")
}

// ✅ 正确 - 业务逻辑错误，记录日志并包装错误
err = s_db.GetAutomationService().UpdateAutomation(ctx, req.Id, updateData)
if err != nil {
    g.Log().Errorf(ctx, "更新自动化任务失败: %+v", err)
    return nil, gerror.WrapCode(gcode.CodeInternalError, err, "更新自动化任务失败")
}

// ❌ 错误 - 不要在返回错误前重复记录日志
if req.Id <= 0 {
    g.Log().Error(ctx, "自动化任务ID必须大于0")  // 不需要这行
    return nil, errors.New("自动化任务ID必须大于0")
}
```

#### 错误处理规则

1. **参数验证错误**: 直接返回 `errors.New()`，让 GoFrame 自动处理错误响应
2. **业务逻辑错误**: 使用 `gerror.WrapCode()` 包装并记录日志用于系统监控
3. **成功操作**: 记录成功日志用于业务监控
4. **避免重复日志**: 不要在返回错误前重复记录日志，GoFrame 会自动处理

### 数据库字段常量使用规范

**重要**: 在查询、更新、创建操作中，必须使用 `s_db_model` 中定义的字段常量，而不是直接使用字符串。

#### 1. 查询操作中使用字段常量

```go
// ✅ 正确 - 使用字段常量
func (c *Controller) GetDeviceById(ctx context.Context, req *v1.GetDeviceByIdReq) (*v1.GetDeviceByIdRes, error) {
    device, err := s_db.GetDeviceService().GetDeviceById(ctx, req.Id)
    if err != nil {
        return nil, gerror.WrapCode(gcode.CodeNotFound, err, "设备不存在")
    }
    
    // 使用字段常量进行查询
    filters := map[string]interface{}{
        s_db_model.FieldDeviceId: device.Id,
        s_db_model.FieldEnabled:  true,
    }
    
    return &v1.GetDeviceByIdRes{
        Device: device,
    }, nil
}

// ❌ 错误 - 直接使用字符串
filters := map[string]interface{}{
    "device_id": device.Id,  // 应该使用 s_db_model.FieldDeviceId
    "enabled":   true,       // 应该使用 s_db_model.FieldEnabled
}
```

#### 2. 更新操作中使用字段常量

```go
// ✅ 正确 - 使用字段常量
func (c *Controller) UpdateDevice(ctx context.Context, req *v1.UpdateDeviceReq) (*v1.UpdateDeviceRes, error) {
    updateData := map[string]interface{}{
        s_db_model.FieldName:    req.Name,
        s_db_model.FieldEnabled: req.Enabled,
        s_db_model.FieldSort:    req.Sort,
    }
    
    err := s_db.GetDeviceService().UpdateDevice(ctx, req.Id, updateData)
    if err != nil {
        return nil, gerror.WrapCode(gcode.CodeInternalError, err, "更新设备失败")
    }
    
    return &v1.UpdateDeviceRes{}, nil
}

// ❌ 错误 - 直接使用字符串
updateData := map[string]interface{}{
    "name":    req.Name,    // 应该使用 s_db_model.FieldName
    "enabled": req.Enabled, // 应该使用 s_db_model.FieldEnabled
    "sort":    req.Sort,    // 应该使用 s_db_model.FieldSort
}
```

#### 3. 创建操作中使用字段常量

```go
// ✅ 正确 - 使用字段常量
func (c *Controller) CreateDevice(ctx context.Context, req *v1.CreateDeviceReq) (*v1.CreateDeviceRes, error) {
    deviceId, err := s_db.GetDeviceService().CreateDevice(ctx, 
        req.Name,
        req.Type,
        req.Enabled,
        req.Sort,
    )
    if err != nil {
        return nil, gerror.WrapCode(gcode.CodeInternalError, err, "创建设备失败")
    }
    
    return &v1.CreateDeviceRes{
        Id: deviceId,
    }, nil
}
```

#### 4. 分页查询中使用字段常量

```go
// ✅ 正确 - 使用字段常量
func (c *Controller) GetDevicePage(ctx context.Context, req *v1.GetDevicePageReq) (*v1.GetDevicePageRes, error) {
    filters := map[string]interface{}{}
    
    if req.Name != "" {
        filters[s_db_model.FieldName] = req.Name
    }
    
    if req.Type != "" {
        filters[s_db_model.FieldType] = req.Type
    }
    
    if req.Enabled != nil {
        filters[s_db_model.FieldEnabled] = *req.Enabled
    }
    
    devices, total, err := s_db.GetDeviceService().GetDevicePage(ctx, req.Page, req.PageSize, filters)
    if err != nil {
        return nil, gerror.WrapCode(gcode.CodeInternalError, err, "获取设备列表失败")
    }
    
    return &v1.GetDevicePageRes{
        Devices: devices,
        Total:   total,
    }, nil
}
```

#### 5. 字段常量导入规范

```go
import (
    "s_db/s_db_model"  // 导入字段常量
)

// 使用字段常量
filters := map[string]interface{}{
    s_db_model.FieldId:      id,
    s_db_model.FieldName:    name,
    s_db_model.FieldEnabled: enabled,
}
```

#### 6. 字段常量使用的好处

1. **类型安全**: 避免字符串拼写错误
2. **重构友好**: 字段名变更时只需修改常量定义
3. **代码可读性**: 使用有意义的常量名提高代码可读性
4. **IDE 支持**: IDE 可以提供自动补全和重构支持
5. **统一管理**: 所有字段名在 `s_db_model` 中统一管理

### 命名约定

- **文件命名**: `[module]_v1_[action].go`
- **结构体命名**: `[Action][Module]Req/Res`
- **方法命名**: `[Action][Module]`
- **时间字段**: `CreatedAt`, `UpdatedAt`, `DeletedAt`

## 开发检查清单

### 创建新模块

- [ ] 创建 API 接口定义文件
- [ ] 更新模块接口定义
- [ ] 创建实体模型
- [ ] 实现控制器
- [ ] 注册到 Web 服务
- [ ] 测试功能正确性

### 响应结构体设计检查

- [ ] 操作类接口使用空响应结构体
- [ ] 时间字段使用 `*time.Time` 类型
- [ ] 成功时返回空结构体，失败时通过 `err` 返回
- [ ] 时间字段支持 `nil` 值

### 错误处理检查

- [ ] 统一使用 `gerror` 进行错误处理
- [ ] 参数验证错误直接返回 `errors.New()`，不记录日志
- [ ] 业务逻辑错误使用 `gerror.WrapCode()` 包装并记录日志
- [ ] 避免在返回错误前重复记录日志
- [ ] 错误信息支持多语言（如需要）

### 参数验证检查

- [ ] **查看对应 Model 定义**：检查 `s_db_model` 中的相关 Model 结构体
- [ ] **检查结构体标签**：查看 `required`、`default` 等标签
- [ ] **检查数据库约束**：查看 SQL 创建语句中的 `NOT NULL`、`DEFAULT` 约束
- [ ] **检查字段类型**：指针类型通常表示可选，非指针类型通常表示必填
- [ ] **验证必填参数**：根据 Model 定义验证必填参数
- [ ] **处理可选参数**：根据 Model 定义处理可选参数

### 数据库字段常量使用检查

- [ ] **查询操作中使用字段常量**：所有查询条件都使用 `s_db_model` 中的字段常量
- [ ] **更新操作中使用字段常量**：所有更新数据的 Map 都使用字段常量作为 key
- [ ] **创建操作中使用字段常量**：创建时传递的参数使用字段常量
- [ ] **分页查询中使用字段常量**：分页查询的过滤条件使用字段常量
- [ ] **导入字段常量包**：正确导入 `s_db/s_db_model` 包
- [ ] **避免硬编码字符串**：不使用硬编码的数据库字段名字符串
- [ ] **使用正确的字段常量**：根据字段类型使用通用字段常量或表特有字段常量

## 最佳实践示例

**Setting 模块** - 完整的模块抽取和响应结构体设计示例:

- [setting.go](mdc:application/api/setting/setting.go) - 模块接口定义
- [setting_get.go](mdc:application/api/setting/v1/setting_get.go) - 接口定义示例
- [setting_v1_get_setting.go](mdc:application/internal/controller/setting/setting_v1_get_setting.go) - 查询类接口实现
- [setting_v1_update_setting.go](mdc:application/internal/controller/setting/setting_v1_update_setting.go) - 操作类接口实现
- [setting.go](mdc:application/internal/model/entity/setting.go) - 实体模型示例

## GoFrame 开发规范

### RESTful API 设计

**标准接口映射**:
- 创建资源: `PUT /resource`
- 资源列表: `GET /resource`  
- 资源详情: `GET /resource/{id}`
- 修改资源: `POST /resource/{id}`
- 删除资源: `DELETE /resource/{id}`

**设计原则**:
- 一个 API 只执行一个任务
- 使用标准的 HTTP 状态码

### 代码生成

**主要命令**:
```bash
gf gen ctrl    # 生成控制器代码
gf gen service # 生成服务接口代码
```

**开发流程**:
1. 在 `api/` 目录定义接口结构
2. 使用 `gf gen ctrl` 生成控制器代码
3. 实现具体业务逻辑
4. 使用 `gf gen service` 生成服务接口

**注意事项**:
- 生成的代码文件不要手动修改
- 使用 `--clear` 参数清理无用文件
- 定期运行生成命令保持代码同步

## 技术栈

- **GoFrame v2**: Web 框架和路由管理
- **Context**: 上下文传递和生命周期管理
- **gerror**: 错误处理
- **g.Log()**: 日志记录
