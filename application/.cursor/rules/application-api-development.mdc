---
alwaysApply: false
description: application 接口开发规则 - API、Controller、Entity 创建标准化流程
---

# Application 接口开发规则

`application/` 目录是项目的 Web 应用层，采用 GoFrame v2 框架，遵循 RESTful API 设计原则。遵循以下标准化开发流程：

## 目录结构

```text
application/
├── api/                        # API 接口定义层
│   ├── [module]/              # 模块目录
│   │   ├── [module].go        # 模块接口定义 (自动生成)
│   │   └── v1/                # 版本目录
│   │       └── [module]_*.go  # 具体接口定义
├── internal/                   # 内部实现层
│   ├── controller/            # 控制器实现
│   │   └── [module]/          # 模块控制器
│   │       ├── [module]_new.go        # 控制器构造函数 (自动生成)
│   │       └── [module]_v1_*.go       # 具体接口实现
│   ├── model/                 # 数据模型层
│   │   ├── entity/            # 实体模型
│   │   └── convert/           # 数据转换
│   ├── consts/                # 常量定义
│   └── logic/                 # 业务逻辑层
└── manifest/                  # 配置文件
```

## 开发流程

### 1. 创建 API 接口定义

在 `api/[module]/v1/` 目录下创建接口定义文件：

```go
package v1

import (
    "application/internal/model/entity"
    "github.com/gogf/gf/v2/frame/g"
)

// [Action][Module]Req 请求结构体
type [Action][Module]Req struct {
    g.Meta `path:"/[module]/[action]" method:"[method]" tags:"[模块名]相关" summary:"[接口描述]"`
    
    // 路径参数
    [ParamName] string `json:"[paramName]" v:"required" dc:"[参数描述]"`
    
    // 查询参数
    [QueryParam] string `json:"[queryParam]" dc:"[查询参数描述]"`
    
    // 请求体参数
    [BodyParam] string `json:"[bodyParam]" v:"required" dc:"[请求体参数描述]"`
}

// [Action][Module]Res 响应结构体
type [Action][Module]Res struct {
    // 基础响应字段
    [ResultField] []*entity.S[Module]Entity `json:"[resultField]" dc:"[结果字段描述]"`
    
    // 分页字段 (如需要)
    Total int `json:"total" dc:"总数"`
    Page  int `json:"page" dc:"当前页"`
    Size  int `json:"size" dc:"每页大小"`
}
```

**API 定义规范**：

- 文件命名：`[module]_[action].go`
- 结构体命名：`[Action][Module]Req/Res`
- 必须包含 `g.Meta` 标签定义路由信息
- 路径参数使用 `v:"required"` 验证
- 所有字段都要有 `dc` 标签描述
- 响应结构体使用 `entity` 包中的实体模型

### 2. 更新模块接口定义

在 `api/[module]/[module].go` 中添加新接口方法：

```go
type I[Module]V1 interface {
    // 现有方法...
    
    // 新增方法
    [Action][Module](ctx context.Context, req *v1.[Action][Module]Req) (res *v1.[Action][Module]Res, err error)
}
```

**注意**：此文件由 GoFrame CLI 自动生成，通常不需要手动编辑。

### 3. 创建实体模型

在 `internal/model/entity/` 目录下创建实体文件：

```go
package entity

import (
    "common/c_base"
    "github.com/pkg/errors"
    "reflect"
    "github.com/gogf/gf/v2/util/gconv"
)

// S[Module]Entity 实体结构体
type S[Module]Entity struct {
    // 基础字段
    Id        string `json:"id" dc:"ID"`
    Name      string `json:"name" dc:"名称"`
    CreatedAt string `json:"createdAt" dc:"创建时间"`
    UpdatedAt string `json:"updatedAt" dc:"更新时间"`
    
    // 业务字段
    [BusinessField] string `json:"[businessField]" dc:"[业务字段描述]"`
    
    // 关联字段
    [RelatedField] []*S[RelatedModule]Entity `json:"[relatedField]" dc:"[关联字段描述]"`
}

// UnmarshalValue 实现值转换接口
func (e *S[Module]Entity) UnmarshalValue(value interface{}) error {
    if record, ok := value.(*c_base.S[BaseType]); ok {
        *e = S[Module]Entity{
            Id:        record.Id,
            Name:      record.Name,
            CreatedAt: record.CreatedAt,
            UpdatedAt: record.UpdatedAt,
            // 其他字段映射...
        }
        return nil
    }
    return errors.Errorf(`unsupported value type for UnmarshalValue: %v`, reflect.TypeOf(value))
}
```

**实体模型规范**：

- 文件命名：`[module].go`
- 结构体命名：`S[Module]Entity`
- 必须实现 `UnmarshalValue` 方法用于数据转换
- 使用 `gconv` 进行类型转换
- 字段要有完整的 JSON 和描述标签

### 4. 实现控制器

在 `internal/controller/[module]/` 目录下创建控制器实现：

```go
package [module]

import (
    "context"
    "application/api/[module]"
    "application/internal/model/entity"
    "common"
    "s_db"
    
    v1 "application/api/[module]/v1"
    "github.com/gogf/gf/v2/errors/gerror"
    "github.com/gogf/gf/v2/errors/gcode"
    "github.com/gogf/gf/v2/frame/g"
    "github.com/gogf/gf/v2/util/gconv"
)

// [Action][Module] 实现接口方法
func (c *ControllerV1) [Action][Module](ctx context.Context, req *v1.[Action][Module]Req) (res *v1.[Action][Module]Res, err error) {
    // 1. 参数验证和预处理
    if req.[ParamName] == "" {
        return nil, gerror.NewCode(gcode.CodeInvalidParameter, "参数不能为空")
    }
    
    // 2. 调用业务逻辑或服务层
    var result []*entity.S[Module]Entity
    
    // 方式1: 调用 common 层服务
    if manager := common.GetDeviceManager(); manager != nil {
        // 使用设备管理器
        configs := manager.GetDeviceConfigList()
        err = gconv.Scan(configs, &result)
        if err != nil {
            g.Log().Errorf(ctx, "数据转换失败: %+v", err)
            return nil, gerror.WrapCode(gcode.CodeInternalError, err, "数据转换失败")
        }
    }
    
    // 方式2: 调用 s_db 服务
    // service := s_db.Get[Module]Service()
    // data, err := service.Get[Module]sByCondition(ctx, condition)
    // if err != nil {
    //     g.Log().Errorf(ctx, "查询失败: %+v", err)
    //     return nil, gerror.WrapCode(gcode.CodeInternalError, err, "查询失败")
    // }
    
    // 3. 构建响应
    return &v1.[Action][Module]Res{
        [ResultField]: result,
        // 分页信息 (如需要)
        Total: len(result),
        Page:  req.Page,
        Size:  req.Size,
    }, nil
}
```

**控制器实现规范**：

- 文件命名：`[module]_v1_[action].go`
- 方法命名：`[Action][Module]`
- 必须进行参数验证
- 使用 `gerror` 进行错误处理
- 使用 `g.Log()` 记录日志
- 返回统一的响应格式

### 5. 更新控制器构造函数

在 `internal/controller/[module]/[module]_new.go` 中确保控制器已注册：

```go
package [module]

import (
    "application/api/[module]"
)

type ControllerV1 struct{}

func NewV1() [module].I[Module]V1 {
    return &ControllerV1{}
}
```

**注意**：此文件由 GoFrame CLI 自动生成，通常不需要手动编辑。

### 6. 注册接口包到 Web 服务

创建新的接口包后，必须在 `internal/cmd/cmd_web.go` 中注册控制器：

#### 6.1 导入控制器包

在文件顶部的导入部分添加新的控制器包：

```go
import (
    "application/internal/consts"
    "application/internal/controller/alarm"
    "application/internal/controller/control"
    "application/internal/controller/device"
    "application/internal/controller/driver"
    "application/internal/controller/log"
    "application/internal/controller/network"
    "application/internal/controller/policy"      // 新增模块
    "application/internal/controller/protocol"
    "application/internal/controller/system"
    "application/internal/controller/[newmodule]" // 新增模块
    // ... 其他导入
)
```

#### 6.2 绑定控制器到路由组

在 `startWeb` 函数的 API 路由组中添加新的控制器绑定：

```go
s.Group("/api", func(group *ghttp.RouterGroup) {
    group.Middleware(
        ghttp.MiddlewareCORS,
        func(r *ghttp.Request) {
            httpLanguage := r.GetHeader("Accept-Language")
            if httpLanguage == "" {
                httpLanguage = "zh-CN"
            }
            r.SetCtx(gi18n.WithLanguage(r.Context(), httpLanguage))
            r.Middleware.Next()
        },
    )

    // 现有控制器绑定
    group.Bind(device.NewV1())
    group.Bind(driver.NewV1())
    group.Bind(network.NewV1())
    group.Bind(system.NewV1())
    group.Bind(protocol.NewV1())
    group.Bind(control.NewV1())
    group.Bind(log.NewV1())
    group.Bind(alarm.NewV1())
    group.Bind(policy.NewV1())
    
    // 新增控制器绑定
    group.Bind([newmodule].NewV1())
})
```

#### 6.3 注册步骤检查清单

创建新接口包时，确保完成以下注册步骤：

- [ ] 在 `internal/cmd/cmd_web.go` 中导入新的控制器包
- [ ] 在 API 路由组中添加 `group.Bind([module].NewV1())`
- [ ] 确保控制器构造函数 `NewV1()` 存在
- [ ] 验证路由注册成功（启动服务后检查日志）
- [ ] 测试 API 接口可访问性

#### 6.4 路由注册示例

**完整的注册示例**：

```go
// 1. 导入部分
import (
    "application/internal/controller/[newmodule]"
)

// 2. 路由绑定部分
s.Group("/api", func(group *ghttp.RouterGroup) {
    // ... 中间件配置 ...
    
    // 绑定新模块控制器
    group.Bind([newmodule].NewV1())
})
```

**注意事项**：

1. **导入顺序**：按字母顺序排列导入包
2. **绑定顺序**：按模块功能重要性或字母顺序排列绑定
3. **命名一致性**：确保包名、控制器名、接口名保持一致
4. **错误处理**：如果控制器注册失败，服务启动时会报错

## 命名约定

### 文件命名

- API 定义: `[module]_[action].go`
- 控制器实现: `[module]_v1_[action].go`
- 实体模型: `[module].go`
- 构造函数: `[module]_new.go`

### 类型命名

- 接口: `I[Module]V1`
- 控制器: `ControllerV1`
- 请求结构: `[Action][Module]Req`
- 响应结构: `[Action][Module]Res`
- 实体: `S[Module]Entity`

### 方法命名

- 查询: `Get[Module]`, `Get[Module]List`, `Get[Module]ById`
- 创建: `Create[Module]`
- 更新: `Update[Module]`
- 删除: `Delete[Module]`
- 操作: `[Action][Module]`

## 路由规范

### HTTP 方法映射

- `GET`: 查询操作
- `POST`: 创建操作
- `PUT`: 更新操作
- `DELETE`: 删除操作

### 路径规范

- 资源集合: `/[module]` (如 `/device`)
- 单个资源: `/[module]/{id}` (如 `/device/{deviceId}`)
- 子资源: `/[module]/{id}/[submodule]` (如 `/device/{deviceId}/telemetry`)
- 操作: `/[module]/[action]` (如 `/device/enable`)

### 标签规范

- 模块标签: `[模块名]相关` (如 `设备相关`)
- 接口描述: 简洁明确的功能描述

## 异常处理

### 错误处理原则

1. **统一错误返回**：所有异常都通过 `err` 返回，不使用自定义响应体
2. **错误码标准化**：使用 GoFrame 标准错误码
3. **错误信息本地化**：错误信息支持多语言
4. **详细日志记录**：记录错误上下文和堆栈信息

### 错误码使用

```go
import (
    "errors"
    "github.com/gogf/gf/v2/errors/gerror"
    "github.com/gogf/gf/v2/errors/gcode"
)

// 参数验证错误
if req.ParamName == "" {
    return nil, errors.New("参数不能为空")
}

// 业务逻辑错误
if !isValidFormat(req.Data) {
    return nil, gerror.NewCode(gcode.CodeInvalidParameter, "数据格式不正确")
}

// 资源不存在
if resource == nil {
    return nil, gerror.NewCode(gcode.CodeNotFound, "资源不存在")
}

// 权限错误
if !hasPermission(ctx, resource) {
    return nil, gerror.NewCode(gcode.CodeNotAuthorized, "权限不足")
}

// 内部错误（包装原始错误）
if err != nil {
    return nil, gerror.WrapCode(gcode.CodeInternalError, err, "操作失败")
}
```

### 异常处理最佳实践

```go
func (c *ControllerV1) ExampleMethod(ctx context.Context, req *v1.ExampleReq) (res *v1.ExampleRes, err error) {
    // 1. 参数验证
    if err := c.validateRequest(req); err != nil {
        g.Log().Errorf(ctx, "参数验证失败: %+v", err)
        return nil, err
    }
    
    // 2. 业务逻辑处理
    result, err := c.processBusinessLogic(ctx, req)
    if err != nil {
        g.Log().Errorf(ctx, "业务逻辑处理失败: %+v", err)
        return nil, err
    }
    
    // 3. 数据转换
    var response []*entity.ExampleEntity
    if err := gconv.Scan(result, &response); err != nil {
        g.Log().Errorf(ctx, "数据转换失败: %+v", err)
        return nil, gerror.WrapCode(gcode.CodeInternalError, err, "数据转换失败")
    }
    
    // 4. 成功返回
    g.Log().Infof(ctx, "操作成功，处理了 %d 条记录", len(response))
    return &v1.ExampleRes{
        Data: response,
    }, nil
}

// 参数验证方法
func (c *ControllerV1) validateRequest(req *v1.ExampleReq) error {
    if req.ID == "" {
        return errors.New("ID不能为空")
    }
    if !strings.HasPrefix(req.ID, "prefix_") {
        return errors.New("ID格式不正确，必须以prefix_开头")
    }
    return nil
}
```

### 错误日志记录

```go
// 错误日志 - 包含上下文信息
g.Log().Errorf(ctx, "操作失败 - 用户ID: %s, 操作: %s, 错误: %+v", userID, operation, err)

// 警告日志 - 业务异常
g.Log().Warningf(ctx, "业务异常 - 资源ID: %s, 原因: %s", resourceID, reason)

// 信息日志 - 成功操作
g.Log().Infof(ctx, "操作成功 - 处理了 %d 条记录", count)

// 调试日志 - 详细调试信息
g.Log().Debugf(ctx, "调试信息 - 请求参数: %+v, 响应数据: %+v", req, response)
```

### 错误响应格式

**成功响应**：

```json
HTTP 200 OK
{
    "code": 0,
    "message": "success",
    "data": { ... }
}
```

**错误响应**：

```json
HTTP 400 Bad Request
{
    "code": 400,
    "message": "参数不能为空"
}
```

**内部错误响应**：

```json
HTTP 500 Internal Server Error
{
    "code": 500,
    "message": "操作失败: 数据库连接异常"
}
```

## 数据转换

### 使用 gconv 进行类型转换

```go
// 结构体转换
var result []*entity.SModuleEntity
err = gconv.Scan(sourceData, &result)

// 基础类型转换
intValue := gconv.Int(stringValue)
boolValue := gconv.Bool(stringValue)
```

### 实体转换

```go
// 实现 UnmarshalValue 方法
func (e *SEntity) UnmarshalValue(value interface{}) error {
    if record, ok := value.(*c_base.SBaseType); ok {
        // 字段映射
        *e = SEntity{
            Id:   record.Id,
            Name: record.Name,
            // ...
        }
        return nil
    }
    return errors.Errorf(`unsupported value type: %v`, reflect.TypeOf(value))
}
```

## 技术栈

- **GoFrame v2**: Web 框架和路由管理
- **Context**: 上下文传递和生命周期管理
- **gconv**: 类型转换工具
- **gerror**: 错误处理
- **g.Log()**: 日志记录

## 开发检查清单

### 创建新模块接口包

创建新的 API 模块时，确保完成以下步骤：

- [ ] 在 `api/[module]/v1/` 中创建接口定义
- [ ] 在 `api/[module]/[module].go` 中添加接口方法
- [ ] 在 `internal/model/entity/` 中创建实体模型
- [ ] 在 `internal/controller/[module]/` 中实现控制器
- [ ] 在 `internal/cmd/cmd_web.go` 中导入新的控制器包
- [ ] 在 `internal/cmd/cmd_web.go` 中绑定控制器到路由组
- [ ] 确保所有方法都有适当的错误处理
- [ ] 添加必要的日志记录
- [ ] 测试接口的正确性
- [ ] 验证路由和参数绑定
- [ ] 验证服务启动和路由注册成功

### 添加新接口到现有模块

在现有模块中添加新接口时，确保完成以下步骤：

- [ ] 在 `api/[module]/v1/` 中创建新接口定义
- [ ] 在 `api/[module]/[module].go` 中添加新接口方法
- [ ] 在 `internal/controller/[module]/` 中实现新接口控制器
- [ ] 确保新接口有适当的错误处理
- [ ] 添加必要的日志记录
- [ ] 测试新接口的正确性
- [ ] 验证路由和参数绑定

### 错误处理检查清单

- [ ] 所有异常都通过 `err` 返回，不使用自定义响应体
- [ ] 参数验证错误使用 `errors.New()` 返回
- [ ] 业务逻辑错误使用 `gerror.NewCode()` 返回
- [ ] 内部错误使用 `gerror.WrapCode()` 包装原始错误
- [ ] 所有错误都有详细的日志记录
- [ ] 错误信息支持多语言（如需要）
- [ ] 错误响应格式符合标准

### 代码质量检查清单

- [ ] 遵循命名约定和文件结构
- [ ] 使用统一的错误处理方式
- [ ] 添加适当的日志记录
- [ ] 实现数据转换接口（如需要）
- [ ] 添加必要的参数验证
- [ ] 确保代码可读性和可维护性
- [ ] 添加必要的注释和文档

## 示例参考

参考现有实现：

- [device.go](mdc:application/api/device/device.go) - 模块接口定义
- [device_get.go](mdc:application/api/device/v1/device_get.go) - 接口定义示例
- [device_v1_get_device_tree.go](mdc:application/internal/controller/device/device_v1_get_device_tree.go) - 控制器实现示例
- [device.go](mdc:application/internal/model/entity/device.go) - 实体模型示例
- [protocol_v1_create_protocol.go](mdc:application/internal/controller/protocol/protocol_v1_create_protocol.go) - 创建操作示例
