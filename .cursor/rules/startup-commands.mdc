---
alwaysApply: false
description: EMS 启动命令、测试方法和调试指南
---

# EMS 启动命令和调试指南

## ⚠️ 重要提示

**所有启动命令都默认包含 `--force=true` 参数**，确保能够强制启动，避免因已有实例而启动失败。强制启动会：

- 检查并终止已有的运行实例
- 优雅关闭旧进程（SIGTERM → SIGKILL）
- 清理旧的 PID 文件
- 启动新的实例

## 启动命令详解

### 基本启动命令

```bash
# 进入应用目录
cd application

# 基本启动（无 Web 服务，强制启动）
go run main.go --force=true

# 启动 Web 服务（强制启动）
go run main.go --web=true --force=true

# 启动 GUI 界面（强制启动）
go run main.go --gui=true --force=true

# 启动 GUI 全屏模式（强制启动）
go run main.go --gui-full=true --force=true
```

#### 注意

如果是linux或者mac电脑，使用nohup &的方式，使得实例后台运行。防止阻塞进程。并且任务结束后手动关闭进程

### 完整参数列表

| 参数 | 简写 | 默认值 | 说明 |
|------|------|--------|------|
| `--web` | `-w` | `false` | 启动 Web 服务 |
| `--gui` | `-g` | `false` | 启动 GUI 界面 |
| `--gui-full` | `-f` | `false` | 启动 GUI 界面并全屏 |
| `--device-name` | `-d` | `device` | 设备配置文件名称 |
| `--driver-path` | `-dp` | `./driver` | 驱动文件存放路径 |
| `--runtime-path` | `-rp` | `./out/runtime` | 实时数据库路径 |
| `--db-path` | `-cp` | `./out/data` | 配置数据库路径 |
| `--language` | `-l` | `zh-CN` | 全局语言设置 |
| `--profile` | `-p` | `prod` | 配置环境 (dev/prod) |
| `--test` | `-t` | `false` | 测试模式：启动3秒后自动关闭 |
| `--force` | - | `false` | 强制启动：忽略PID检查 |
| `--active-device-root-id` | - | - | 强制激活根设备ID |

### 常用启动组合

```bash
# 开发环境启动（强制启动）
go run main.go --web=true --profile=dev --driver-path=resources/drivers --force=true

# 生产环境启动（强制启动）
go run main.go --web=true --profile=prod --force=true

# 测试模式启动（强制启动）
go run main.go --test=true --web=true --force=true

# 强制启动（忽略已有实例）
go run main.go --force=true --web=true

# Windows 环境默认启动 GUI（强制启动）
go run main.go --force=true  # Windows 下自动启动 GUI
```

### 构建和运行

```bash
# 开发环境构建
go build -tags=dev -o ems main.go

# 生产环境构建
go build -o ems main.go

# 带 GUI 的开发环境构建
go build -tags="dev,cgo" -o ems main.go

# 运行构建后的程序（强制启动）
./ems --web=true --force=true
```

## 单实例和强制启动

### PID 文件机制

系统使用 PID 文件确保只有一个实例运行：

- **PID 文件路径**: `out/ems.pid`（可在配置中修改）
- **检查机制**: 启动时检查 PID 文件是否存在且对应进程是否运行
- **跨平台支持**: 支持 Linux、macOS、Windows 的进程检查

### 强制启动逻辑

```bash
# 正常启动（如果已有实例会失败）
go run main.go --web=true

# 强制启动（终止已有实例）
go run main.go --force=true --web=true
```

**强制启动行为**:

1. 检查是否有运行中的实例
2. 如果存在，先发送 `SIGTERM` 信号优雅关闭
3. 等待 10 秒，如果进程仍存在则发送 `SIGKILL` 强制终止
4. 删除旧的 PID 文件
5. 写入新的 PID 文件
6. 启动新实例

### PID 文件管理

```bash
# 查看当前 PID
cat out/ems.pid

# 手动删除 PID 文件（如果进程已死）
rm out/ems.pid

# 检查进程是否运行
ps -p $(cat out/ems.pid)
```

## 测试方法

### 单元测试

```bash
# 运行所有测试
go test ./...

# 运行特定包的测试
go test ./common/c_func/...

# 运行测试并显示覆盖率
go test -cover ./...

# 运行测试并生成覆盖率报告
go test -coverprofile=coverage.out ./...
go tool cover -html=coverage.out
```

### 集成测试

```bash
# 测试模式启动（3秒后自动关闭，强制启动）
go run main.go --test=true --force=true

# 测试模式结合其他参数（强制启动）
go run main.go --test=true --web=true --profile=dev --force=true

# 使用 GoFrame 开发模式（强制启动）
gf run main.go -p ./bin -a "--web=true --force=true" -w "api/*.go" -w "internal/*.go"
```

### 测试文件示例

创建测试文件 `*_test.go`:

```go
package main

import (
    "testing"
    "context"
    "application/internal/cmd"
)

func TestMainStartup(t *testing.T) {
    // 测试启动逻辑
    ctx := context.Background()
    
    // 模拟启动参数
    parser := &gcmd.Parser{}
    parser.SetOpt("web", true)
    parser.SetOpt("test", true)
    
    // 测试启动函数
    err := cmd.Main.Func(ctx, parser)
    if err != nil {
        t.Errorf("启动失败: %v", err)
    }
}

func TestPidFileManagement(t *testing.T) {
    // 测试 PID 文件管理
    // ...
}
```

## 调试方法


### VS Code 调试配置

创建 `.vscode/launch.json`:

```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug EMS",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/application/main.go",
            "args": ["--web=true", "--profile=dev", "--force=true"],
            "cwd": "${workspaceFolder}/application",
            "env": {},
            "showLog": true
        },
        {
            "name": "Debug EMS Test",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/application/main.go",
            "args": ["--test=true", "--web=true", "--force=true"],
            "cwd": "${workspaceFolder}/application"
        }
    ]
}
```

### 日志调试

```bash
# 启用调试日志（强制启动）
go run main.go --web=true --profile=dev --force=true

# 查看日志文件
tail -f out/logs/app/*.log

# 查看特定设备日志
tail -f out/logs/device_*.log
```

## 开发环境配置

### GoFrame 开发模式

```bash
# 使用 GoFrame 开发模式（热重载，强制启动）
gf run main.go -p ./bin -a "--web=true --force=true" -w "api/*.go" -w "internal/*.go"

# 构建配置
gf build -n ems -a "386,amd64" -s "linux,darwin,windows"
```

### 环境变量

```bash
# 设置环境变量
export EMS_PROFILE=dev
export EMS_DRIVER_PATH=resources/drivers
export EMS_WEB_ENABLED=true

# 运行程序（强制启动）
go run main.go --force=true
```

## 故障排查

### 常见问题

1. **端口占用**

```bash
# 检查端口占用
netstat -tlnp | grep :8000
lsof -i :8000

# 杀死占用进程
kill -9 $(lsof -ti:8000)
```

1. **PID 文件问题**

```bash
# 检查 PID 文件
cat out/ems.pid

# 检查进程是否运行
ps -p $(cat out/ems.pid)

# 强制删除 PID 文件
rm out/ems.pid
```

1. **数据库锁定**

```bash
# 检查数据库文件
ls -la out/data/
sqlite3 out/data/db.sqlite3 ".tables"
```

### 调试工具

```bash
# 使用 pprof 进行性能分析（强制启动）
go run main.go --web=true --force=true
# 访问 http://localhost:8000/debug/pprof/

# 内存分析
go tool pprof http://localhost:8000/debug/pprof/heap

# CPU 分析
go tool pprof http://localhost:8000/debug/pprof/profile
```

## 相关文件

- [main.go](mdc:application/main.go) - 程序入口
- [cmd/main.go](mdc:application/internal/cmd/main.go) - 启动命令定义
- [utils/pid.go](mdc:application/internal/utils/pid.go) - PID 文件管理
- [config.yaml](mdc:application/manifest/config/config.yaml) - 配置文件
- [README.md](mdc:README.md) - 项目说明文档
