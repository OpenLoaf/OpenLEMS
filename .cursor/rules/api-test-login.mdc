---
alwaysApply: false
description: API接口测试登录规则 - 用于测试带权限的接口时获取认证信息。包含登录请求格式、认证头设置、测试流程、环境配置等完整测试指南。适用于API接口测试、认证流程验证、权限测试等场景。
globs: application/api/**/*.go,application/internal/controller/**/*.go,*.md,*.yaml,*.json,*.sh
---

# API 接口测试登录规则

## 概述

本规则定义了在测试带权限的 API 接口时如何快速获取认证信息，包括登录请求格式、认证头设置和测试流程。

## 登录请求格式

### 基础登录请求

```bash
curl --location --request POST 'http://127.0.0.1:15880/api/auth/login' \
--header 'User-Agent: Apifox/1.0.0 (https://apifox.com)' \
--header 'Content-Type: application/json' \
--header 'Accept: */*' \
--header 'Host: 127.0.0.1:15880' \
--header 'Connection: keep-alive' \
--data-raw '{
    "password": "888888",
    "role": "admin"
}'
```

### 响应处理

登录成功后，响应中会包含 `ems_session_id` 信息，需要保存此值用于后续请求的认证。

## 认证头设置

### 带认证的请求格式

```bash
curl --location --request GET 'http://127.0.0.1:15880/api/[endpoint]' \
--header 'User-Agent: Apifox/1.0.0 (https://apifox.com)' \
--header 'Content-Type: application/json' \
--header 'Accept: */*' \
--header 'Host: 127.0.0.1:15880' \
--header 'Connection: keep-alive' \
--header 'Cookie: ems_session_id=[从登录响应中获取的session_id]' \
--data-raw '{
    "request": "data"
}'
```

## 测试流程

### 1. 获取认证信息

```bash
# 步骤1: 执行登录请求
LOGIN_RESPONSE=$(curl -s --location --request POST 'http://127.0.0.1:15880/api/auth/login' \
--header 'Content-Type: application/json' \
--data-raw '{
    "password": "888888",
    "role": "admin"
}')

# 步骤2: 提取session_id（根据实际响应格式调整）
SESSION_ID=$(echo $LOGIN_RESPONSE | jq -r '.session_id' 2>/dev/null || echo $LOGIN_RESPONSE | grep -o 'ems_session_id=[^;]*' | cut -d'=' -f2)
```

### 2. 使用认证信息测试接口

```bash
# 使用提取的session_id进行认证请求
curl --location --request GET 'http://127.0.0.1:15880/api/[endpoint]' \
--header 'Content-Type: application/json' \
--header 'Cookie: ems_session_id='$SESSION_ID
```

## 环境配置

### 开发环境

- **端口**: `15880`
- **基础路径**: `/api/`
- **默认密码**: `888888`
- **默认角色**: `admin`

### 生产环境

根据实际部署情况调整：

- **端口**: 根据实际部署端口修改
- **基础路径**: 根据实际API路径修改
- **认证信息**: 使用生产环境的认证凭据

## 常用测试命令

### 快速登录并获取Session

```bash
# 一键登录并保存session_id到环境变量
export EMS_SESSION_ID=$(curl -s -X POST 'http://127.0.0.1:15880/api/auth/login' \
  -H 'Content-Type: application/json' \
  -d '{"password": "888888", "role": "admin"}' | \
  grep -o 'ems_session_id=[^;]*' | cut -d'=' -f2)

echo "Session ID: $EMS_SESSION_ID"
```

### 使用环境变量进行认证请求

```bash
# 使用保存的session_id进行认证请求
curl -X GET 'http://127.0.0.1:15880/api/[endpoint]' \
  -H 'Content-Type: application/json' \
  -H "Cookie: ems_session_id=$EMS_SESSION_ID"
```

## 测试脚本示例

### 完整测试脚本

```bash
#!/bin/bash

# 配置
BASE_URL="http://127.0.0.1:15880"
API_BASE="$BASE_URL/api"
LOGIN_DATA='{"password": "888888", "role": "admin"}'

# 登录获取认证信息
echo "正在登录..."
LOGIN_RESPONSE=$(curl -s -X POST "$API_BASE/auth/login" \
  -H 'Content-Type: application/json' \
  -d "$LOGIN_DATA")

# 检查登录是否成功
if [ $? -eq 0 ]; then
    echo "登录成功"
    
    # 提取session_id（根据实际响应格式调整）
    SESSION_ID=$(echo $LOGIN_RESPONSE | jq -r '.session_id' 2>/dev/null || \
                 echo $LOGIN_RESPONSE | grep -o 'ems_session_id=[^;]*' | cut -d'=' -f2)
    
    if [ -n "$SESSION_ID" ]; then
        echo "Session ID: $SESSION_ID"
        
        # 测试带权限的接口
        echo "测试带权限的接口..."
        curl -X GET "$API_BASE/[endpoint]" \
          -H 'Content-Type: application/json' \
          -H "Cookie: ems_session_id=$SESSION_ID"
    else
        echo "无法提取Session ID"
        exit 1
    fi
else
    echo "登录失败"
    exit 1
fi
```

## 注意事项

### 端口和路径配置

1. **端口号**: 根据实际部署情况修改 `15880`
2. **基础路径**: 根据实际API路径修改 `/api/`
3. **协议**: 根据实际部署协议修改 `http://` 或 `https://`

### 认证信息管理

1. **Session有效期**: 注意session的有效期，过期后需要重新登录
2. **安全考虑**: 不要在代码中硬编码认证信息
3. **环境隔离**: 不同环境使用不同的认证凭据

### 错误处理

1. **登录失败**: 检查用户名密码是否正确
2. **Session无效**: 检查session是否过期
3. **权限不足**: 检查用户角色是否有相应权限

## 相关文件

- [application/api/auth/](mdc:application/api/auth/) - 认证相关API
- [application/manifest/config/](mdc:application/manifest/config/) - 配置文件
- [application/out/](mdc:application/out/) - 部署输出目录

## 更新日志

- **2024-01-XX** - 初始版本，定义API测试登录规则
- **未来更新** - 根据API变更和测试需求持续改进

---

**注意**: 本规则主要用于开发和测试环境，生产环境请使用适当的认证机制和安全措施。