---
alwaysApply: true
description: Cursor规则文件创建、管理和使用规范 - 定义规则文件结构、命名规范、内容规范、创建流程、维护流程等。适用于所有规则文件的创建、更新、维护等场景。
globs: .cursor/rules/**/*.mdc
---

# Cursor 规则管理规范

## 概述

本规范定义了 EMS 项目中 Cursor 规则文件的创建、管理和使用标准，确保规则系统的一致性和可维护性。

## 规则文件结构

### 标准格式

所有规则文件必须遵循以下标准格式：

```markdown
---
alwaysApply: true/false
description: 规则描述
globs: 文件匹配模式（可选）
---

# 规则标题

## 概述
规则的整体说明和用途

## 具体规范
详细的规范内容

## 示例
代码示例和使用方法

## 注意事项
重要的注意点和限制
```markdown

### 元数据字段说明

- **alwaysApply**:
  - `true` - 规则始终应用，适用于全局规范
  - `false` - 规则需要手动应用，适用于特定场景
- **description**: 规则的简要描述，用于规则检索
- **globs**: 文件匹配模式，使用 gitignore 风格的模式
  - 示例：`*.go,*.ts,*.tsx` 或 `*config*.go,*Config*.go`

## 规则分类

### 1. 全局规则 (alwaysApply: true)

适用于整个项目的规则，自动应用到所有请求：

- **code-common-library.mdc** - 通用库结构和命名规范
- **mgt-cursor-rules.mdc** - 规则管理规范（本文件）

### 2. 特定文件规则 (globs 模式)

适用于特定文件类型的规则：

- **code-config-struct-tags.mdc** - 配置结构体标签规范
  - 适用文件：`*config*.go`, `*Config*.go`

### 3. 手动应用规则 (alwaysApply: false)

需要手动应用的规则，适用于特定开发场景：

- **arch-project-structure.mdc** - 项目整体结构指南
- **arch-services-architecture.mdc** - 服务架构指南
- **biz-setting-system.mdc** - 系统设置管理规范

## 规则文件命名规范

### 命名格式

规则文件使用以下命名格式：

```markdown
<功能描述>.mdc
```markdown

### 命名示例

- `code-common-library.mdc` - 通用库规范
- `arch-project-structure.mdc` - 项目结构指南
- `arch-services-architecture.mdc` - 服务架构规范
- `code-config-struct-tags.mdc` - 配置标签规范
- `biz-setting-system.mdc` - 系统设置规范
- `mgt-cursor-rules.mdc` - 规则管理规范

### 命名原则

1. **功能导向** - 文件名应清楚表达规则的功能
2. **简洁明了** - 使用简洁的英文描述
3. **避免重复** - 确保文件名在规则目录中唯一
4. **语义化** - 使用有意义的词汇组合

## 规则内容规范

### 1. 结构要求

每个规则文件应包含以下标准结构：

```markdown
# 规则标题

## 概述
- 规则的用途和适用范围
- 解决的问题和达到的目标

## 核心规范
- 具体的规范要求
- 详细的实施标准

## 示例
- 代码示例
- 使用场景演示

## 最佳实践
- 推荐的做法
- 常见错误和避免方法

## 注意事项
- 重要的限制和约束
- 特殊情况处理

## 相关文件
- 相关的代码文件引用
- 其他相关规则文件
```markdown

### 2. 内容质量要求

- **准确性** - 规则内容必须准确反映项目实际情况
- **完整性** - 规则应覆盖相关主题的所有重要方面
- **可操作性** - 规则应提供具体的操作指导
- **一致性** - 规则之间不应存在冲突
- **时效性** - 规则应及时更新以反映项目变化

### 3. 代码示例规范

规则中的代码示例应：

- 使用真实的项目代码风格
- 包含完整的上下文信息
- 提供正确和错误的对比示例
- 使用项目中的实际文件路径引用

## 规则创建流程

### 1. 需求分析

在创建新规则前，需要：

- 确定规则要解决的问题
- 分析规则的适用范围
- 评估规则的必要性和优先级

### 2. 规则设计

- 选择合适的规则类型（全局/特定文件/手动应用）
- 设计规则的结构和内容
- 确定规则的文件名和位置

### 3. 规则实现

- 创建规则文件
- 编写规则内容
- 添加必要的代码示例
- 设置正确的元数据

### 4. 规则验证

- 检查规则内容的准确性
- 验证代码示例的正确性
- 确保规则格式符合标准

### 5. 规则发布

- 将规则文件提交到版本控制
- 通知团队成员新规则的存在
- 更新相关文档

## 规则维护流程

### 1. 定期审查

- 定期检查规则的时效性
- 评估规则的有效性
- 收集使用反馈

### 2. 规则更新

当项目发生变化时：

- 识别需要更新的规则
- 修改规则内容
- 更新相关示例
- 保持版本控制记录

### 3. 规则废弃

对于不再适用的规则：

- 标记为废弃状态
- 提供迁移指导
- 在适当时机删除

## 规则使用指南

### 1. 规则检索

使用 `fetch_rules` 工具检索相关规则：

```go
// 检索特定规则
fetch_rules(["common-library", "project-structure"])

// 检索所有规则
fetch_rules(["*"])
```markdown

### 2. 规则应用

- **自动应用规则** - 系统自动应用，无需手动操作
- **文件匹配规则** - 在编辑匹配文件时自动应用
- **手动应用规则** - 需要明确请求时应用

### 3. 规则优先级

当多个规则冲突时，按以下优先级处理：

1. 特定文件规则（globs 匹配）
2. 手动应用规则
3. 全局规则

## 规则文件引用规范

### 文件引用格式

在规则文件中引用其他文件时，使用以下格式：

```markdown
[文件名](mdc:相对路径)
```markdown

### 引用示例

```markdown
- [common-library.mdc](mdc:.cursor/rules/common-library.mdc)
- [main.go](mdc:application/main.go)
- [s_db_basic_interface.go](mdc:services/s_db/s_db_basic/s_db_basic_interface.go)
```markdown

### 引用原则

- 使用相对路径从工作区根目录开始
- 确保引用的文件存在且路径正确
- 优先引用项目内的文件
- 避免引用外部依赖文件

## 规则系统最佳实践

### 1. 规则设计原则

- **单一职责** - 每个规则专注于一个特定主题
- **适度粒度** - 规则不应过于细碎或过于宽泛
- **可扩展性** - 规则应支持未来的扩展和修改
- **向后兼容** - 规则更新时应保持向后兼容性

### 2. 规则组织原则

- **逻辑分组** - 相关规则应组织在一起
- **层次清晰** - 规则之间应有清晰的层次关系
- **易于查找** - 规则应易于发现和检索
- **避免重复** - 避免规则内容重复

### 3. 规则维护原则

- **及时更新** - 项目变化时及时更新规则
- **版本控制** - 规则变更应有完整的版本记录
- **团队协作** - 规则变更应经过团队讨论
- **文档同步** - 规则更新时同步更新相关文档

## 规则系统限制

### 1. 技术限制

- 规则文件大小限制
- 规则数量限制
- 文件引用深度限制

### 2. 使用限制

- 规则不能替代代码审查
- 规则不能强制执行所有规范
- 规则需要人工验证和调整

### 3. 维护限制

- 规则需要定期维护
- 规则更新需要团队协调
- 规则废弃需要迁移计划

## 相关文件

- [.cursor/rules/](mdc:.cursor/rules/) - 规则文件目录
- [mgt-cursor-rules-naming.mdc](mdc:.cursor/rules/mgt-cursor-rules-naming.mdc) - 规则文件命名规范
- [code-common-library.mdc](mdc:.cursor/rules/code-common-library.mdc) - 通用库规范
- [arch-project-structure.mdc](mdc:.cursor/rules/arch-project-structure.mdc) - 项目结构指南
- [arch-services-architecture.mdc](mdc:.cursor/rules/arch-services-architecture.mdc) - 服务架构规范
- [code-config-struct-tags.mdc](mdc:.cursor/rules/code-config-struct-tags.mdc) - 配置标签规范
- [biz-setting-system.mdc](mdc:.cursor/rules/biz-setting-system.mdc) - 系统设置规范

## 更新日志

- **2024-01-XX** - 初始版本，定义规则管理规范
- **未来更新** - 根据项目需要和团队反馈持续改进

---

**注意**: 本规范是动态文档，会根据项目发展和团队反馈持续更新。所有规则创建和维护都应遵循本规范的要求。
