---
alwaysApply: true
description: 统一点位管理系统设计规范 - 核心架构、构造函数模式、预定义配置使用。定义SPoint核心结构、协议点位定义、构造函数模式、预定义配置复用、启动验证等完整规范。适用于所有点位定义、协议开发、数据访问配置等场景。
globs: common/c_base/*point*.go,common/c_proto/**/*.go,common/c_default/**/*.go,plugins/plug_drivers/**/*.go
---

# 统一点位管理系统设计规范

## 核心原则

1. 以 `SPoint` 为核心，统一管理协议点位、反射遥测点位、配置点位
2. 所有协议点位通过 `SProtocolPoint` 作为基础，派生具体协议点位
3. 点位定义采用**构造函数 + 选项模式**（A+D 混合模式）
4. **优先复用** `c_default` 预定义点位与数据访问配置
5. 创建后数据不可变；启动时进行完整性验证
6. 保持业务层取值方式与接口不变

## 分层结构

```text
SPoint（基础点位信息）
└── SProtocolPoint（协议点位基础，持有 DataAccess）
    ├── SModbusPoint（Modbus 协议点位）
    ├── SCanbusPoint（CAN 总线协议点位）
    └── SDidioPoint（DIDO 协议点位）

SReflectPoint（反射遥测点位，持有 *SPoint + MethodName）
SConfigPoint（配置点位，持有 *SPoint + 配置约束字段）
```

## 核心结构

### SPoint - 点位元数据

```go
type SPoint struct {
    Key          string                  // 点位唯一标识
    Name         string                  // 显示名称
    ValueType    c_enum.EValueType       // 值类型（必填）
    Group        *SPointGroup            // 分组
    Unit         string                  // 单位
    Desc         string                  // 描述
    Sort         int                     // 排序
    Min          int64                   // 最小值
    Max          int64                   // 最大值
    Precise      uint8                   // 精度
    Hidden       bool                    // 是否隐藏
    ValueExplain []*SFieldExplain        // 值解释
    Trigger      func(...) (...)         // 告警触发（可选）
}
```

### SProtocolPoint - 协议点位基础

```go
type SProtocolPoint struct {
    *SPoint
    DataAccess *SDataAccess // 必填：数据访问配置
}
```

### SModbusPoint - Modbus 协议点位

```go
type SModbusPoint struct {
    *SProtocolPoint
    Addr uint16              // Modbus 地址
    
    // 可覆盖字段
    ValueExplain []*c_base.SFieldExplain
    Trigger      func(value interface{}) (bool, c_enum.EAlarmLevel, error)
}
```

## 点位定义规范（Modbus 为例）

### 字段顺序要求

1. **Addr**（Modbus 地址，必须放第一位）
2. **SProtocolPoint**（内含 SPoint 与 DataAccess）
3. **ValueExplain**（可选）
4. **Trigger**（可选）

### 构造函数选择指南

#### 基础构造函数（80% 场景）

**简单点位 - NewModbusPointWithDesc（推荐）：**

```go
var Year = c_proto.NewModbusPointWithDesc(
    30297,                          // 地址
    "Year",                         // Key
    "年",                           // Name
    c_enum.EUint16,                 // ValueType
    "",                             // Unit（可选）
    "年",                           // Desc
    c_default.VDataAccessUInt16,    // DataAccess
)
```

**使用预定义点位 - NewModbusPointFromPreset（强烈推荐）：**

```go
var DCVoltage = c_proto.NewModbusPointFromPreset(
    0x1103,                              // 地址
    c_default.VPointDcVoltage,           // 预定义点位
    c_default.VDataAccessUInt16Scale01,  // 预定义数据访问
)
```

#### 选项模式（20% 复杂场景）

**复杂点位 - NewModbusPointExt：**

```go
var OnOffCommand = c_proto.NewModbusPointExt(30314,
    c_proto.WithKey("OnOffCommand"),
    c_proto.WithName("开关机指令"),
    c_proto.WithValueType(c_enum.EUint16),
    c_proto.WithDesc("On/off command: 0- Shutdown, 1- Startup, 2- Standby"),
    c_proto.WithDataAccess(c_default.VDataAccessUInt16),
    c_proto.WithValueExplain([]*c_base.SFieldExplain{
        {Key: "0", Value: "关机",   Color: "#d9d9d9"},
        {Key: "1", Value: "已启动", Color: "#52c41a"},
        {Key: "2", Value: "待机",   Color: "#faad14"},
    }),
)
```

**可用选项函数：**

- `WithKey(string)` - 设置 Key
- `WithName(string)` - 设置名称
- `WithValueType(c_enum.EValueType)` - 设置值类型
- `WithUnit(string)` - 设置单位
- `WithDesc(string)` - 设置描述
- `WithDataAccess(*c_base.SDataAccess)` - 设置数据访问配置
- `WithValueExplain([]*c_base.SFieldExplain)` - 设置值解释
- `WithTrigger(func)` - 设置告警触发函数
- `WithGroup(*c_base.SPointGroup)` - 设置分组
- `WithMinMax(int64, int64)` - 设置最小最大值
- `WithPrecise(uint8)` - 设置精度
- `WithHidden(bool)` - 设置是否隐藏
- `WithPresetPoint(*c_base.SPoint)` - 使用预定义点位

### 预定义配置优先使用

#### 预定义点位配置（c_default.VPoint*）

参考：[c_default_point_v.go](mdc:common/c_default/c_default_point_v.go)

**匹配规则：**

- `Name: "A相电流"` + `Unit: "A"` → `c_default.VPointIa`
- `Name: "A相电压"` + `Unit: "V"` → `c_default.VPointUa`
- `Name: "电池电量"` + `Unit: "%"` → `c_default.VPointSOC`
- `Name: "电池健康度"` + `Unit: "%"` → `c_default.VPointSOH`
- `Name: "直流电压"` + `Unit: "V"` → `c_default.VPointDcVoltage`
- `Name: "直流电流"` + `Unit: "A"` → `c_default.VPointDcCurrent`
- `Name: "温度"` → `c_default.VPointTemp`
- `Name: "最高温度"` → `c_default.VPointTempMax`
- `Name: "最低温度"` → `c_default.VPointTempMin`
- `Name: "频率"` + `Unit: "Hz"` → `c_default.VPointFreq`

#### 预定义数据访问配置（c_default.VDataAccess*）

参考：[c_default_data_access_v.go](mdc:common/c_default/c_default_data_access_v.go)

**匹配规则：**

- `DataFormat: UInt16` + `ValueType: EUint16` → `c_default.VDataAccessUInt16`
- `DataFormat: Int16` + `ValueType: EInt16` → `c_default.VDataAccessInt16`
- `DataFormat: Float32` + `ValueType: EFloat32` → `c_default.VDataAccessFloat32`
- `DataFormat: UInt16` + `Factor: 0.1` → `c_default.VDataAccessUInt16Scale01`
- `DataFormat: UInt16` + `Factor: 0.01` → `c_default.VDataAccessUInt16Scale001`
- `DataFormat: UInt16` + `Factor: 0.001` → `c_default.VDataAccessUInt16Scale0001`
- `DataFormat: UInt16` + `ValueType: EBool` → `c_default.VDataAccessUInt16ToBool`
- `DataFormat: BitRange` + `ValueType: EBool` → `c_default.VDataAccessBitRangeBool`

#### 预定义告警触发函数（c_default.FAlarmTrigger*）

参考：[c_default_alarm_trigger_f.go](mdc:common/c_default/c_default_alarm_trigger_f.go)

- `c_default.FAlarmTriggerWarnBool` - 布尔值警告触发
- `c_default.FAlarmTriggerAlertBool` - 布尔值警报触发
- `c_default.FAlarmTriggerErrorBool` - 布尔值故障触发
- `c_default.FAlarmTriggerWarnNotZero` - 非零值警告触发
- `c_default.FAlarmTriggerAlertNotZero` - 非零值警报触发
- `c_default.FAlarmTriggerErrorNotZero` - 非零值故障触发

### 避免冗余默认值

以下字段无需显式设置（已是默认值）：

- `Factor: 1.0` - 默认不缩放
- `Offset: 0` - 默认无偏移
- `ByteEndian: c_enum.ByteEndianBig` - 默认大端
- `WordOrder: c_enum.WordOrderHighLow` - 默认高字在前

### 位域访问配置规范

```go
DataAccess: &c_base.SDataAccess{
    BitIndex:   3,                        // 位索引放前
    BitLength:  1,                        // 位长度
    DataFormat: c_enum.DataFormatBitRange, // 位范围格式
    ByteEndian: c_enum.ByteEndianBig,     // 字节序（可省略）
    WordOrder:  c_enum.WordOrderHighLow,  // 字序（可省略）
}
```

## 启动时验证

系统启动时必须验证：

1. **SModbusPoint.SProtocolPoint** 必须非空
2. **SProtocolPoint.SPoint** 必须非空
3. **SProtocolPoint.DataAccess** 必须非空
4. 校验失败阻止启动并输出错误信息（设备、点位 Key、原因）

## 遥测获取统一逻辑

```go
// 统一遥测入口
func GetTelemetryValue(point c_base.IPoint, dev c_base.IDevice) (any, error) {
    switch p := point.(type) {
    case *c_base.SReflectPoint:
        // 通过反射获取
        return getTelemetryByReflect(p.MethodName, dev)
    case *c_base.SProtocolPoint:
        // 从缓存获取
        return getTelemetryFromCache(p, dev)
    default:
        return nil, errors.New("unsupported point type")
    }
}
```

## 规则与最佳实践

### 点位定义规范

1. **Key 唯一性**：同一驱动文件中点位 Key 必须唯一
2. **包级别定义**：点位定义在包级别，便于复用
3. **优先预定义**：优先使用 `c_default` 预定义配置
4. **构造函数优先**：80% 场景使用基础构造函数
5. **选项模式补充**：20% 复杂场景使用选项模式

### Modbus 任务分组

参考：[modbus-point-definition.mdc](mdc:plugins/plug_drivers/.cursor/rules/modbus-point-definition.mdc)

**任务分组建议：**

1. **心跳类任务**（GroupHeart）- 循环间隔：1000ms
2. **基本信息任务**（GroupInfo）- 循环间隔：1000ms
3. **时间类任务**（GroupTime）- 循环间隔：1000ms 或 0
4. **统计类任务**（GroupStatistics）- 循环间隔：1000ms 或更长，缓存：30s

**任务设计原则：**

- 地址连续性：同一任务中点位地址应连续
- 功能相关性：功能相关的点位组织在同一任务
- 读取频率：根据数据更新频率设置 CycleMill
- 缓存策略：根据数据重要性设置 Lifetime

### ValueExplain 规范

**Key 设置规范：**

- 布尔值类型：Key 使用 `"false"` 和 `"true"`
- 数值类型：Key 使用字符串格式数值，如 `"0"`, `"1"`, `"2"`
- 浮点类型：Key 使用字符串格式数值，如 `"0.0"`, `"1.5"`

**颜色设置建议：**

- 正常状态：`#52c41a`（绿色）
- 警告状态：`#faad14`（黄色）
- 错误状态：`#f5222d`（红色）
- 中性状态：`#d9d9d9`（灰色）
- 运行状态：`#1890ff`（蓝色）

## 反射遥测点位定义

```go
// 遥测点位定义 - 包级别，启动时验证 SPoint 字段
var (
    telemetrySocPoint = &c_base.SReflectPoint{
        SPoint: &c_base.SPoint{
            Key:       "soc",
            Name:      "SOC",
            Unit:      "%",
            ValueType: c_enum.EFloat32,
            Desc:      "电池电量",
            Min:       0,
            Max:       100,
        },
        MethodName: "GetSoc",
    }
)
```

## 配置点位定义

```go
// 配置点位定义 - 包级别
var (
    configPort = &c_base.SConfigPoint{
        SPoint: &c_base.SPoint{
            Key:       "port",
            Name:      "端口号",
            ValueType: c_enum.EInt32,
            Desc:      "Modbus TCP 端口",
        },
        Required: true,
        Default:  ptr("502"),
        Min:      1,
        Max:      65535,
    }
)
```

## 验证清单

开发时检查：

- [ ] 所有点位使用构造函数/选项模式定义
- [ ] 优先使用 `c_default` 预定义配置
- [ ] 无冗余默认值（Factor:1, Offset:0 等）
- [ ] 点位 Key 在驱动中唯一
- [ ] Modbus 任务按功能分组，地址连续
- [ ] ValueExplain 使用正确的 Key 格式
- [ ] 启动时验证通过，无空指针
- [ ] 业务取值方法继续正常工作

## 相关文件

- [统一点位管理系统设计方案.md](mdc:统一点位管理系统设计方案.md) - 完整设计方案
- [c_modbus_point_new_f.go](mdc:common/c_proto/c_modbus_point_new_f.go) - Modbus 点位构造函数
- [c_canbus_point_new_f.go](mdc:common/c_proto/c_canbus_point_new_f.go) - CANbus 点位构造函数
- [c_didio_point_new_f.go](mdc:common/c_proto/c_didio_point_new_f.go) - DIDO 点位构造函数
- [c_default_point_v.go](mdc:common/c_default/c_default_point_v.go) - 预定义点位配置
- [c_default_data_access_v.go](mdc:common/c_default/c_default_data_access_v.go) - 预定义数据访问配置
- [c_default_alarm_trigger_f.go](mdc:common/c_default/c_default_alarm_trigger_f.go) - 预定义告警触发函数
- [modbus-point-definition.mdc](mdc:plugins/plug_drivers/.cursor/rules/modbus-point-definition.mdc) - Modbus 点位定义详细规范
- [point-definition-migration-guide.mdc](mdc:plugins/plug_drivers/.cursor/rules/point-definition-migration-guide.mdc) - 点位定义迁移指南

## 遥测点位与协议点位选择规范

### 点位类型选择原则

#### 1. 协议点位优先原则

**优先使用协议点位（SModbusPoint）**，避免不必要的遥测点位（SReflectPoint）：

```go
// ✅ 推荐 - 直接使用协议点位
func (s *sDevice) GetTelemetryPoints() []c_base.IPoint {
    return []c_base.IPoint{
        Power,       // 直接使用协议点位
        SOC,         // 直接使用协议点位
        Status,      // 直接使用协议点位
    }
}

// ❌ 不推荐 - 使用重复的遥测点位
func (s *sDevice) GetTelemetryPoints() []c_base.IPoint {
    return []c_base.IPoint{
        telemetryPowerPoint,  // 反射调用 GetPower() -> GetFromPointFloat64(Power)
        telemetrySocPoint,    // 反射调用 GetSoc() -> GetFromPointFloat32(SOC)
        telemetryStatusPoint, // 反射调用 GetStatus() -> GetFromPointInt16(Status)
    }
}
```

#### 2. 遥测点位使用场景

**仅在以下场景使用遥测点位（SReflectPoint）：**

- **聚合计算**：需要从多个子设备聚合数据
- **数据转换**：需要单位转换、格式转换等业务逻辑
- **复杂计算**：需要复杂算法处理的数据
- **告警判断**：需要实时告警逻辑的数据

```go
// ✅ 正确使用 - 聚合计算场景
var telemetryTotalPowerPoint = &c_base.SReflectPoint{
    SPoint: &c_base.SPoint{
        Key:       "totalPower",
        Name:      "总功率",
        Unit:      "kW",
        ValueType: c_enum.EFloat32,
        Desc:      "所有设备功率总和",
    },
    MethodName: "GetTotalPower", // 聚合多个子设备功率
}

// ✅ 正确使用 - 数据转换场景
var telemetryTemperaturePoint = &c_base.SReflectPoint{
    SPoint: &c_base.SPoint{
        Key:       "temperature",
        Name:      "温度",
        Unit:      "°C",
        ValueType: c_enum.EFloat32,
        Desc:      "环境温度",
    },
    MethodName: "GetTemperature", // 温度单位转换逻辑
}
```

#### 3. 避免重复点位

**禁止创建功能重复的点位：**

```go
// ❌ 错误 - 功能重复
var (
    // 协议点位
    Power = c_proto.NewModbusPointWithDesc(0x1001, "Power", "功率", c_enum.EFloat32, "kW", "功率", c_default.VDataAccessFloat32)
    
    // 遥测点位 - 功能重复
    telemetryPowerPoint = &c_base.SReflectPoint{
        SPoint: &c_base.SPoint{
            Key:       "Power",
            Name:      "功率",
            Unit:      "kW",
            ValueType: c_enum.EFloat32,
            Desc:      "功率",
        },
        MethodName: "GetPower", // 内部只是调用 GetFromPointFloat32(Power)
    }
)
```

### 性能优化规范

#### 1. 数据获取路径优化

**直接访问缓存 vs 反射调用：**

```go
// ✅ 高效 - 直接访问缓存
func (s *sDevice) GetTelemetryPoints() []c_base.IPoint {
    return []c_base.IPoint{
        Power,  // 直接从 Modbus 缓存获取
        SOC,    // 直接从 Modbus 缓存获取
    }
}

// ❌ 低效 - 反射调用开销
func (s *sDevice) GetTelemetryPoints() []c_base.IPoint {
    return []c_base.IPoint{
        telemetryPowerPoint, // 反射调用 GetPower() -> GetFromPointFloat32(Power)
        telemetrySocPoint,   // 反射调用 GetSoc() -> GetFromPointFloat32(SOC)
    }
}
```

#### 2. 点位数量优化

**减少不必要的点位暴露：**

```go
// ✅ 优化后 - 精简点位
func (s *sDevice) GetTelemetryPoints() []c_base.IPoint {
    return []c_base.IPoint{
        Status,      // 设备状态
        Power,       // 功率
        SOC,         // 电量
        MaxPower,    // 最大功率
        Energy,      // 电量统计
    }
}

// ❌ 优化前 - 点位过多
func (s *sDevice) GetTelemetryPoints() []c_base.IPoint {
    return []c_base.IPoint{
        Status, Power, SOC, MaxPower, Energy,
        telemetryPowerPoint, telemetrySocPoint, telemetryEnergyPoint,
        // ... 更多重复点位
    }
}
```

### 设计检查清单

开发时检查：

- [ ] **避免重复点位**：同一功能不定义多个点位
- [ ] **协议点位优先**：优先使用协议点位，避免不必要的遥测点位
- [ ] **遥测点位合理**：遥测点位仅用于聚合、转换、复杂计算场景
- [ ] **性能优化**：直接访问缓存，避免反射调用开销
- [ ] **点位精简**：只暴露必要的关键点位
- [ ] **数据一致性**：协议点位数据更准确，避免数据不一致

### 迁移指南

#### 从重复点位迁移到优化点位

1. **识别重复点位**：
   ```bash
   # 查找可能的重复点位
   grep -r "telemetry.*Point" plugins/plug_drivers/your_driver/
   grep -r "func.*Get.*()" plugins/plug_drivers/your_driver/
   ```

2. **分析点位功能**：
   - 检查遥测点位方法是否只是简单调用协议点位
   - 识别真正需要业务逻辑的遥测点位

3. **优化点位选择**：
   ```go
   // 移除重复的遥测点位
   // telemetryPowerPoint -> 直接使用 Power
   // telemetrySocPoint -> 直接使用 SOC
   
   // 保留有业务逻辑的遥测点位
   // telemetryTotalPowerPoint -> 聚合计算，保留
   // telemetryTemperaturePoint -> 单位转换，保留
   ```

4. **更新 GetTelemetryPoints**：
   ```go
   // 优化前
   func (s *sDevice) GetTelemetryPoints() []c_base.IPoint {
       return []c_base.IPoint{
           telemetryPowerPoint,  // 移除
           telemetrySocPoint,    // 移除
           telemetryTotalPowerPoint, // 保留
       }
   }
   
   // 优化后
   func (s *sDevice) GetTelemetryPoints() []c_base.IPoint {
       return []c_base.IPoint{
           Power,                // 直接使用协议点位
           SOC,                  // 直接使用协议点位
           telemetryTotalPowerPoint, // 保留聚合计算点位
       }
   }
   ```

## 注意事项

1. **不向前兼容**：完全移除 v1 兼容性，直接使用新结构
2. **数据不可变**：点位创建后不允许修改
3. **启动验证**：系统启动时验证所有必填字段
4. **接口继承**：子结构体不重复实现接口方法
5. **预定义优先**：优先使用 `c_default` 包配置
6. **性能优先**：避免不必要的反射调用，直接使用协议点位
7. **功能唯一**：同一功能不定义多个点位，避免数据不一致
