---
alwaysApply: false
description: 枚举创建规范和最佳实践
globs: common/c_enum/*.go
---

# 枚举创建规范

## 概述

本规则定义了 EMS 项目中枚举类型的创建规范，包括整数类型枚举和字符串类型枚举的创建模式。

## 枚举类型分类

### 1. 整数类型枚举（需要 stringer）

**适用场景**：状态枚举、级别枚举、类型枚举等需要数值比较的场景

**文件命名规范**：
- 主文件：`c_enum_[name]_e.go`
- 字符串文件：`c_enum_[name]_e_string.go`（由 stringer 自动生成）

**创建模板**：

```go
//go:generate stringer -type=E[EnumName] -trimprefix=E[EnumName] -output=c_enum_[name]_e_string.go
package c_enum

import (
    "encoding/json"
    "fmt"
    "strings"
)

// E[EnumName] [枚举描述]
type E[EnumName] int

const (
    E[EnumName][Value1] E[EnumName] = iota // [值1描述]
    E[EnumName][Value2]                    // [值2描述]
    E[EnumName][Value3]                    // [值3描述]
)

// Parse[EnumName] 解析枚举字符串
func Parse[EnumName](value string) E[EnumName] {
    value = strings.ToLower(strings.TrimSpace(value))
    switch value {
    case "[value1]", "[alias1]":
        return E[EnumName][Value1]
    case "[value2]", "[alias2]":
        return E[EnumName][Value2]
    case "[value3]", "[alias3]":
        return E[EnumName][Value3]
    default:
        return E[EnumName][Value1] // 默认值
    }
}

// MarshalJSON 自定义JSON序列化
func (e E[EnumName]) MarshalJSON() ([]byte, error) {
    return json.Marshal(e.String())
}

// UnmarshalJSON 自定义JSON反序列化
func (e *E[EnumName]) UnmarshalJSON(data []byte) error {
    var value interface{}
    if err := json.Unmarshal(data, &value); err != nil {
        return err
    }

    switch v := value.(type) {
    case float64:
        *e = E[EnumName](int(v))
    case int:
        *e = E[EnumName](v)
    case string:
        *e = Parse[EnumName](v)
    default:
        return fmt.Errorf("invalid type for E[EnumName]: %T", v)
    }
    return nil
}
```

**示例参考**：
- [c_enum_status_e.go](mdc:common/c_enum/c_enum_status_e.go) - 状态枚举
- [c_enum_alarm_level_e.go](mdc:common/c_enum/c_enum_alarm_level_e.go) - 告警级别枚举

### 2. 字符串类型枚举（不需要 stringer）

**适用场景**：配置值类型、协议类型、设置分组等字符串常量场景

**文件命名规范**：
- 主文件：`c_enum_[name]_e.go`
- 不需要字符串文件

**创建模板**：

```go
package c_enum

// E[EnumName] [枚举描述]
type E[EnumName] string

const (
    E[EnumName][Value1] E[EnumName] = "[value1]" // [值1描述]
    E[EnumName][Value2] E[EnumName] = "[value2]" // [值2描述]
    E[EnumName][Value3] E[EnumName] = "[value3]" // [值3描述]
)
```

**示例参考**：
- [c_enum_config_fields_value_type_e.go](mdc:common/c_enum/c_enum_config_fields_value_type_e.go) - 配置字段值类型
- [c_enum_protocol_type_e.go](mdc:common/c_enum/c_enum_protocol_type_e.go) - 协议类型

### 3. 类型别名枚举

**适用场景**：简单的字符串常量分组

**创建模板**：

```go
package c_enum

// E[EnumName] [枚举描述]
type E[EnumName] = string

const (
    E[EnumName][Value1] E[EnumName] = "[value1]" // [值1描述]
    E[EnumName][Value2] E[EnumName] = "[value2]" // [值2描述]
    E[EnumName][Value3] E[EnumName] = "[value3]" // [值3描述]
)
```

**示例参考**：
- [c_enum_setting_group_e.go](mdc:common/c_enum/c_enum_setting_group_e.go) - 设置分组

## 命名规范

### 枚举类型命名

- **类型名**：`E[EnumName]` - 如 `EAlarmLevel`、`EProtocolType`
- **常量名**：`E[EnumName][Value]` - 如 `EAlarmLevelWarn`、`EProtocolTypeModbusTcp`
- **文件名**：`c_enum_[name]_e.go` - 如 `c_enum_alarm_level_e.go`

### 常量值命名

- **整数枚举**：使用描述性名称，如 `EAlarmLevelWarn`、`EStatusEnable`
- **字符串枚举**：使用下划线分隔的小写字符串，如 `"modbus_tcp"`、`"system"`

## 创建流程

### 1. 确定枚举类型

根据使用场景选择枚举类型：

- **需要数值比较** → 整数类型枚举（使用 stringer）
- **配置常量** → 字符串类型枚举
- **简单分组** → 类型别名枚举

### 2. 创建枚举文件

1. 在 `common/c_enum/` 目录下创建枚举文件
2. 按照模板创建枚举定义
3. 添加必要的解析函数和JSON序列化方法

### 3. 生成字符串方法（仅整数枚举）

对于整数类型枚举，运行 stringer 命令：

```bash
# 在枚举文件所在目录执行
go generate
```

或者手动执行：

```bash
stringer -type=E[EnumName] -trimprefix=E[EnumName] -output=c_enum_[name]_e_string.go
```

### 4. 验证枚举

确保枚举文件能够正常编译和运行：

```bash
go build ./common/c_enum/
```

## 最佳实践

### 1. 枚举设计原则

- **语义明确**：枚举值名称应该清晰表达含义
- **向后兼容**：新增枚举值时保持向后兼容
- **默认值**：为枚举提供合理的默认值
- **解析函数**：为整数枚举提供字符串解析函数

### 2. JSON 序列化

- **整数枚举**：实现 `MarshalJSON` 和 `UnmarshalJSON` 方法
- **字符串枚举**：通常不需要自定义序列化方法
- **类型别名**：使用 Go 默认的序列化行为

### 3. 错误处理

- **解析错误**：提供清晰的错误信息
- **默认值**：为无效输入提供合理的默认值
- **类型检查**：在反序列化时进行类型检查

### 4. 文档注释

- **类型注释**：为枚举类型添加清晰的注释
- **常量注释**：为每个枚举值添加中文注释
- **函数注释**：为解析函数添加使用说明

## 常见错误

### 1. 手动创建字符串文件

❌ **错误做法**：
```go
// 手动创建 c_enum_status_e_string.go
func (s EStatus) String() string {
    switch s {
    case EStatusEnable:
        return "Enable"
    // ...
    }
}
```

✅ **正确做法**：
```go
//go:generate stringer -type=EStatus -trimprefix=EStatus -output=c_enum_status_e_string.go
// 让 stringer 自动生成字符串方法
```

### 2. 字符串枚举使用 stringer

❌ **错误做法**：
```go
//go:generate stringer -type=EProtocolType -trimprefix=EProtocolType
type EProtocolType string
```

✅ **正确做法**：
```go
// 字符串枚举不需要 stringer
type EProtocolType string
```

### 3. 缺少 JSON 序列化方法

❌ **错误做法**：
```go
// 整数枚举缺少 JSON 序列化方法
type EAlarmLevel int
```

✅ **正确做法**：
```go
// 实现 JSON 序列化方法
func (e EAlarmLevel) MarshalJSON() ([]byte, error) {
    return json.Marshal(e.String())
}

func (e *EAlarmLevel) UnmarshalJSON(data []byte) error {
    // 实现反序列化逻辑
}
```

## 相关文件

- [c_enum_status_e.go](mdc:common/c_enum/c_enum_status_e.go) - 状态枚举示例
- [c_enum_alarm_level_e.go](mdc:common/c_enum/c_enum_alarm_level_e.go) - 告警级别枚举示例
- [c_enum_config_fields_value_type_e.go](mdc:common/c_enum/c_enum_config_fields_value_type_e.go) - 配置字段值类型示例
- [c_enum_protocol_type_e.go](mdc:common/c_enum/c_enum_protocol_type_e.go) - 协议类型示例
- [c_enum_setting_group_e.go](mdc:common/c_enum/c_enum_setting_group_e.go) - 设置分组示例

## 注意事项

1. **不要手动创建字符串文件**：使用 `//go:generate stringer` 指令自动生成
2. **字符串枚举不需要 stringer**：只有整数类型枚举才需要
3. **保持命名一致性**：遵循项目的命名规范
4. **添加必要的注释**：为枚举类型和值添加清晰的中文注释
5. **实现 JSON 序列化**：为整数枚举实现自定义的 JSON 序列化方法
6. **提供解析函数**：为整数枚举提供字符串解析函数，支持多种输入格式