---
alwaysApply: true
description: Cursor规则文件创建、管理和使用规范 - 定义规则文件结构、命名规范、分类体系、内容规范、创建流程、维护流程等。适用于所有规则文件的创建、更新、维护等场景。
globs: .cursor/rules/**/*.mdc
---

# Cursor 规则管理规范

## 概述

本规范定义了项目中 Cursor 规则文件的创建、管理和使用标准，确保规则系统的一致性和可维护性。

## 规则文件结构

### 标准格式

所有规则文件必须遵循以下标准格式：

```markdown
---
alwaysApply: true/false
description: 规则描述
globs: 文件匹配模式（可选）
---

# 规则标题

## 概述
规则的整体说明和用途

## 具体规范
详细的规范内容

## 示例
代码示例和使用方法

## 注意事项
重要的注意点和限制
```markdown

### 元数据字段说明

- **alwaysApply**:
  - `true` - 规则始终应用，适用于全局规范
  - `false` - 规则需要手动应用，适用于特定场景
- **description**: 规则的简要描述，用于规则检索
- **globs**: 文件匹配模式，使用 gitignore 风格的模式
  - 示例：`*.go,*.ts,*.tsx` 或 `*config*.go,*Config*.go`

## 规则分类

### 1. 全局规则 (alwaysApply: true)

适用于整个项目的规则，自动应用到所有请求：

- **code-common-library.mdc** - 通用库结构和命名规范
- **mgt-cursor-rules.mdc** - 规则管理规范（本文件）

### 2. 特定文件规则 (globs 模式)

适用于特定文件类型的规则：

- **code-config-struct-tags.mdc** - 配置结构体标签规范
  - 适用文件：`*config*.go`, `*Config*.go`

### 3. 手动应用规则 (alwaysApply: false)

需要手动应用的规则，适用于特定开发场景：

- **arch-project-structure.mdc** - 项目整体结构指南
- **arch-services-architecture.mdc** - 服务架构指南
- **biz-setting-system.mdc** - 系统设置管理规范

## 分类前缀系统

### 1. 分类定义

| 前缀 | 分类 | 说明 | 示例 |
|------|------|------|------|
| `arch-` | 架构设计 | 系统架构、模块设计、整体结构规范 | `arch-project-structure.mdc` |
| `api-` | API开发 | API接口设计、开发规范、测试规范 | `api-generation-standards.mdc` |
| `code-` | 代码规范 | 代码风格、命名规范、结构规范 | `code-common-library.mdc` |
| `biz-` | 业务逻辑 | 业务规则、领域模型、业务流程 | `biz-price-management.mdc` |
| `tech-` | 技术集成 | 技术栈集成、工具配置、部署规范 | `tech-hexlib-cgo.mdc` |
| `style-` | 样式格式 | UI样式、文档格式、展示规范 | `style-ui-components.mdc` |
| `test-` | 测试相关 | 测试规范、测试策略、测试工具 | `test-unit-testing.mdc` |
| `deploy-` | 部署运维 | 部署流程、运维规范、监控配置 | `deploy-docker-config.mdc` |
| `mgt-` | 管理规范 | 规则管理、流程管理、团队协作 | `mgt-cursor-rules.mdc` |

### 2. 分类选择原则

- **架构设计 (arch-)**：涉及系统整体结构、模块关系、设计模式
- **API开发 (api-)**：RESTful API、接口设计、API测试
- **代码规范 (code-)**：编程规范、命名约定、代码结构
- **业务逻辑 (biz-)**：业务规则、领域知识、业务流程
- **技术集成 (tech-)**：第三方库集成、工具链配置、技术栈
- **样式格式 (style-)**：UI设计、文档格式、展示规范
- **测试相关 (test-)**：单元测试、集成测试、测试策略
- **部署运维 (deploy-)**：部署流程、运维规范、监控配置
- **管理规范 (mgt-)**：规则管理、流程管理、团队协作

## 规则文件命名规范

### 1. 基本格式

```text
<分类前缀>-<功能描述>_v<主版本>.<次版本>.mdc
```

### 2. 命名规则

- **分类前缀**：必须使用上述定义的分类前缀
- **功能描述**：使用小写字母和连字符，简洁明了
- **版本号**：使用 `_v<主版本>.<次版本>` 格式（如 `_v1.0`, `_v1.1`, `_v2.0`）
- **文件扩展名**：统一使用 `.mdc`
- **长度限制**：文件名总长度不超过50个字符（包含版本号）

### 3. 版本控制规则

- **新文件创建**：必须以 `_v1.0.mdc` 结尾
- **小版本更新**：修改小数点后的数字（如 `_v1.0` → `_v1.1`）
- **大版本更新**：修改主版本号（如 `_v1.1` → `_v2.0`）
- **向后兼容**：支持无版本号的文件（向后兼容）

### 4. 命名示例

```markdown
# 架构设计规则
arch-project-structure_v1.0.mdc          # 项目结构规范
arch-services-architecture_v1.0.mdc      # 服务架构规范
arch-unified-point-system_v1.0.mdc       # 统一点位系统设计

# API开发规则
api-generation-standards_v1.0.mdc        # API生成规范
api-test-login_v1.0.mdc                 # API测试登录规范

# 代码规范规则
code-common-library_v1.0.mdc             # 通用库规范
code-config-struct-tags_v1.0.mdc        # 配置结构体标签规范
code-enum-creation-standards_v1.0.mdc   # 枚举创建规范

# 业务逻辑规则
biz-price-management_v1.0.mdc           # 电价管理规范
biz-policy-management_v1.0.mdc          # 策略管理规范
biz-setting-system_v1.0.mdc             # 系统设置规范

# 技术集成规则
tech-hexlib-cgo_v1.0.mdc                # hexlib CGO集成规范
tech-tsdb-storage_v1.0.mdc              # TSDB存储插件规范
tech-startup-commands_v1.0.mdc          # 启动命令规范

# 管理规范规则
mgt-cursor-rules_v1.1.mdc               # Cursor规则管理规范
```

### 5. 命名原则

1. **功能导向** - 文件名应清楚表达规则的功能
2. **简洁明了** - 使用简洁的英文描述
3. **避免重复** - 确保文件名在规则目录中唯一
4. **语义化** - 使用有意义的词汇组合

## 文件组织规范

### 1. 目录结构

```text
.cursor/rules/
├── arch-*_v*.mdc          # 架构设计规则
├── api-*_v*.mdc           # API开发规则
├── code-*_v*.mdc         # 代码规范规则
├── biz-*_v*.mdc          # 业务逻辑规则
├── tech-*_v*.mdc         # 技术集成规则
├── style-*_v*.mdc        # 样式格式规则
├── test-*_v*.mdc         # 测试相关规则
├── deploy-*_v*.mdc       # 部署运维规则
└── mgt-*_v*.mdc          # 管理规范规则
```

### 2. 文件排序

在目录列表中，文件按以下顺序排列：

1. `mgt-` - 管理规范（最重要，包含规则管理本身）
2. `arch-` - 架构设计
3. `code-` - 代码规范
4. `api-` - API开发
5. `biz-` - 业务逻辑
6. `tech-` - 技术集成
7. `style-` - 样式格式
8. `test-` - 测试相关
9. `deploy-` - 部署运维

## 规则内容规范

### 1. 结构要求

每个规则文件应包含以下标准结构：

```markdown
# 规则标题

## 概述
- 规则的用途和适用范围
- 解决的问题和达到的目标

## 核心规范
- 具体的规范要求
- 详细的实施标准

## 示例
- 代码示例
- 使用场景演示

## 最佳实践
- 推荐的做法
- 常见错误和避免方法

## 注意事项
- 重要的限制和约束
- 特殊情况处理

## 相关文件
- 相关的代码文件引用
- 其他相关规则文件
```markdown

### 2. 内容质量要求

- **准确性** - 规则内容必须准确反映项目实际情况
- **完整性** - 规则应覆盖相关主题的所有重要方面
- **可操作性** - 规则应提供具体的操作指导
- **一致性** - 规则之间不应存在冲突
- **时效性** - 规则应及时更新以反映项目变化

### 3. 代码示例规范

规则中的代码示例应：

- 使用真实的项目代码风格
- 包含完整的上下文信息
- 提供正确和错误的对比示例
- 使用项目中的实际文件路径引用

## 规则创建流程

### 1. 需求分析

在创建新规则前，需要：

- 确定规则要解决的问题
- 分析规则的适用范围
- 评估规则的必要性和优先级

### 2. 规则设计

- 选择合适的规则类型（全局/特定文件/手动应用）
- 设计规则的结构和内容
- 确定规则的文件名和位置

### 3. 规则实现

- 创建规则文件（使用 `_v1.0.mdc` 格式）
- 编写规则内容
- 添加必要的代码示例
- 设置正确的元数据
- 添加版本信息到文件末尾

### 4. 规则验证

- 检查规则内容的准确性
- 验证代码示例的正确性
- 确保规则格式符合标准

### 5. 规则发布

- 将规则文件提交到版本控制
- 通知团队成员新规则的存在
- 更新相关文档

## 规则维护流程

### 1. 定期审查

- 定期检查规则的时效性
- 评估规则的有效性
- 收集使用反馈

### 2. 规则更新

当项目发生变化时：

- 识别需要更新的规则
- 修改规则内容
- 更新相关示例
- **递增版本号**：
  - 小版本更新（内容微调）：`_v1.0` → `_v1.1`
  - 大版本更新（结构变更）：`_v1.1` → `_v2.0`
- 更新文件末尾的版本信息
- 保持版本控制记录

### 3. 规则废弃

对于不再适用的规则：

- 标记为废弃状态
- 提供迁移指导
- 在适当时机删除

## 规则使用指南

### 1. 规则检索

使用 `fetch_rules` 工具检索相关规则：

```go
// 检索特定规则
fetch_rules(["common-library", "project-structure"])

// 检索所有规则
fetch_rules(["*"])
```markdown

### 2. 规则应用

- **自动应用规则** - 系统自动应用，无需手动操作
- **文件匹配规则** - 在编辑匹配文件时自动应用
- **手动应用规则** - 需要明确请求时应用

### 3. 规则优先级

当多个规则冲突时，按以下优先级处理：

1. 特定文件规则（globs 匹配）
2. 手动应用规则
3. 全局规则

## 规则文件引用规范

### 文件引用格式

在规则文件中引用其他文件时，使用以下格式：

```markdown
[文件名](mdc:相对路径)
```markdown

### 引用示例

```markdown
- [common-library.mdc](mdc:.cursor/rules/common-library.mdc)
- [main.go](mdc:application/main.go)
- [s_db_basic_interface.go](mdc:services/s_db/s_db_basic/s_db_basic_interface.go)
```markdown

### 引用原则

- 使用相对路径从工作区根目录开始
- 确保引用的文件存在且路径正确
- 优先引用项目内的文件
- 避免引用外部依赖文件

## 规则系统最佳实践

### 1. 规则设计原则

- **单一职责** - 每个规则专注于一个特定主题
- **适度粒度** - 规则不应过于细碎或过于宽泛
- **可扩展性** - 规则应支持未来的扩展和修改
- **向后兼容** - 规则更新时应保持向后兼容性

### 2. 规则组织原则

- **逻辑分组** - 相关规则应组织在一起
- **层次清晰** - 规则之间应有清晰的层次关系
- **易于查找** - 规则应易于发现和检索
- **避免重复** - 避免规则内容重复

### 3. 规则维护原则

- **及时更新** - 项目变化时及时更新规则
- **版本控制** - 规则变更应有完整的版本记录
- **团队协作** - 规则变更应经过团队讨论
- **文档同步** - 规则更新时同步更新相关文档

### 4. 命名最佳实践

- **简洁明了**：文件名应清楚表达规则的功能
- **避免冗余**：不要重复分类前缀中的信息
- **保持一致性**：同类规则使用相似的命名模式
- **便于搜索**：使用有意义的词汇组合

### 5. 分类最佳实践

- **单一职责**：每个规则文件专注于一个特定主题
- **适度粒度**：规则不应过于细碎或过于宽泛
- **逻辑清晰**：分类应该符合直觉和项目结构
- **易于维护**：分类应该便于后续的维护和扩展

### 6. 组织最佳实践

- **按分类排序**：在目录中按分类前缀排序
- **及时重命名**：创建新规则时立即使用正确的命名
- **定期整理**：定期检查规则文件的分类和命名
- **文档同步**：规则变更时同步更新相关文档

### 7. 版本控制最佳实践

- **版本号递增**：每次内容更新必须递增版本号
- **小版本更新**：内容微调、错误修正、示例更新使用次版本号（如 `_v1.0` → `_v1.1`）
- **大版本更新**：结构变更、重大功能调整使用主版本号（如 `_v1.1` → `_v2.0`）
- **版本记录**：在文件末尾添加更新日志，记录版本变更内容
- **向后兼容**：保持对无版本号文件的兼容性支持

### 8. 文档同步要求

- **强制同步**：当规则文件名发生变化时，必须立即在 `CLAUDE.md` 中更新对应的文件名引用
- **新增规则**：创建新的规则文件时，必须同时在 `CLAUDE.md` 的规则文件索引表中添加对应的条目
- **删除规则**：删除规则文件时，必须从 `CLAUDE.md` 中移除对应的条目
- **重命名规则**：重命名规则文件时，必须更新 `CLAUDE.md` 中的文件名、路径和描述信息
- **版本更新**：规则文件版本更新时，必须同步更新 `CLAUDE.md` 中的版本信息
- **验证机制**：每次规则文件变更后，必须验证 `CLAUDE.md` 中的索引表与实际文件列表的一致性

## 规则系统限制

### 1. 技术限制

- 规则文件大小限制
- 规则数量限制
- 文件引用深度限制

### 2. 使用限制

- 规则不能替代代码审查
- 规则不能强制执行所有规范
- 规则需要人工验证和调整

### 3. 维护限制

- 规则需要定期维护
- 规则更新需要团队协调
- 规则废弃需要迁移计划

## 相关文件

- [.cursor/rules/](mdc:.cursor/rules/) - 规则文件目录
- [code-common-library_v1.0.mdc](mdc:.cursor/rules/code-common-library_v1.0.mdc) - 通用库规范
- [arch-project-structure_v1.0.mdc](mdc:.cursor/rules/arch-project-structure_v1.0.mdc) - 项目结构指南
- [arch-services-architecture_v1.0.mdc](mdc:.cursor/rules/arch-services-architecture_v1.0.mdc) - 服务架构规范
- [code-config-struct-tags_v1.0.mdc](mdc:.cursor/rules/code-config-struct-tags_v1.0.mdc) - 配置标签规范
- [biz-setting-system_v1.0.mdc](mdc:.cursor/rules/biz-setting-system_v1.0.mdc) - 系统设置规范

## 更新日志

- **v1.0** - 初始版本，定义规则管理规范
- **v1.1** - 合并规则文件命名规范，添加分类前缀系统、文件组织规范、命名最佳实践等完整内容

---

**注意**: 本规范是动态文档，会根据项目发展和团队反馈持续更新。所有规则创建和维护都应遵循本规范的要求。