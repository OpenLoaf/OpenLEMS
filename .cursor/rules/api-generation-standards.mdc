---
alwaysApply: false
description: API 接口生成规范 - 定义 API 接口开发、文件拆分、命名规范和设计原则
globs: application/api/**/*.go
---

# API 接口生成规范

本规范定义了 EMS 项目中 API 接口的生成、拆分、命名和设计标准，确保 API 开发的一致性和可维护性。

## 核心原则

### 1. 一个接口一个文件原则

**拆分原则**：

- 每个 API 接口必须独立成文件
- 公共 DTO 结构体单独放在 `*_common.go` 文件中
- 文件命名格式：`[module]_[action].go`

**文件拆分示例**：
```text
application/api/policy/v1/
├── energy_storage_strategy_common.go      # 公共 DTO
├── energy_storage_strategy_create.go      # 创建接口
├── energy_storage_strategy_update.go      # 更新接口
├── energy_storage_strategy_delete.go      # 删除接口
├── energy_storage_strategy_list.go        # 列表查询接口
├── energy_storage_strategy_detail.go      # 详情查询接口
├── energy_storage_strategy_conflicts.go   # 冲突检测接口
└── energy_storage_strategy_activate.go    # 激活接口
```

### 2. 文件命名规范

**命名格式**：`[module]_[action].go`

**常见动作命名**：

- `create` - 创建接口
- `update` - 更新接口
- `delete` - 删除接口
- `get` - 单个查询接口
- `list` - 列表查询接口
- `page` - 分页查询接口
- `detail` - 详情查询接口
- `toggle` - 开关操作接口
- `activate` - 激活接口
- `conflicts` - 冲突检测接口

**特殊文件**：

- `*_common.go` - 公共 DTO 结构体
- `*_types.go` - 类型定义（如需要）

## 接口设计规范

### 1. RESTful API 设计

**标准接口映射**：

- 创建资源: `POST /resource`
- 资源列表: `GET /resource` 或 `GET /resource/page`
- 资源详情: `GET /resource/{id}`
- 修改资源: `PUT /resource/{id}`
- 删除资源: `DELETE /resource/{id}`
- 特殊操作: `POST /resource/{id}/action`

**路由配置规范**：
```go
type CreateResourceReq struct {
    g.Meta `path:"/resource" method:"post" tags:"模块相关" summary:"创建资源" role:"admin"`
    // 请求字段
}

type GetResourceListReq struct {
    g.Meta `path:"/resource" method:"get" tags:"模块相关" summary:"获取资源列表"`
    // 查询参数
}
```

### 2. 请求结构体命名

**命名格式**：`[Action][Resource]Req`

**示例**：

- `CreateEnergyStorageStrategyReq`
- `UpdateEnergyStorageStrategyReq`
- `DeleteEnergyStorageStrategyReq`
- `GetEnergyStorageStrategyListReq`
- `GetEnergyStorageStrategyDetailReq`

### 3. 响应结构体命名

**命名格式**：`[Action][Resource]Res`

**示例**：

- `CreateEnergyStorageStrategyRes`
- `UpdateEnergyStorageStrategyRes`
- `DeleteEnergyStorageStrategyRes`
- `GetEnergyStorageStrategyListRes`
- `GetEnergyStorageStrategyDetailRes`

## 响应结构体设计规范

### 1. 操作类接口（创建、更新、删除）

**设计原则**：

- 使用空响应结构体
- 成功时返回空结构体
- 失败时通过 `err` 返回

**示例**：
```go
type CreateResourceRes struct {
    // 空响应结构体（操作类接口）
}

type UpdateResourceRes struct {
    // 空响应结构体（操作类接口）
}

type DeleteResourceRes struct{}
```

### 2. 查询类接口

**设计原则**：

- 返回具体的数据结构
- 时间字段使用 `*time.Time` 类型
- 支持 `nil` 值表示未设置状态

**示例**：
```go
type GetResourceDetailRes struct {
    Id        string     `json:"id"`
    Name      string     `json:"name"`
    CreatedAt *time.Time `json:"createdAt"`
    UpdatedAt *time.Time `json:"updatedAt"`
}

type GetResourceListRes struct {
    List  []*Resource `json:"list"`
    Total int         `json:"total"`
}
```

### 3. 时间字段处理

**统一规范**：

- 使用 `*time.Time` 类型
- 支持 `nil` 值表示未设置状态
- 从 `gtime.Time` 转换为 `*time.Time`：`&model.CreatedAt.Time`

## 参数验证规范

### 1. 非必填参数使用指针类型

**设计原则**：

- 非必填参数必须使用指针类型（`*Type`）
- 指针类型支持 `nil` 值，明确表示参数未传递
- 避免使用零值判断参数是否传递的问题

**示例**：
```go
type GetResourceListReq struct {
    g.Meta   `path:"/resource" method:"get" tags:"模块相关" summary:"获取资源列表"`
    Page     int      `json:"page" dc:"页码"`           // 必填参数
    PageSize int      `json:"pageSize" dc:"每页数量"`   // 必填参数
    Status   *string  `json:"status" dc:"状态筛选"`     // 非必填参数，使用指针
    Keyword  *string  `json:"keyword" dc:"关键词"`     // 非必填参数，使用指针
    Priority *int     `json:"priority" dc:"优先级"`     // 非必填参数，使用指针
}
```

**使用示例**：
```go
func (c *Controller) GetResourceList(ctx context.Context, req *v1.GetResourceListReq) (*v1.GetResourceListRes, error) {
    filters := map[string]interface{}{}
    
    // 检查非必填参数是否传递
    if req.Status != nil {
        filters["status"] = *req.Status
    }
    
    if req.Keyword != nil {
        filters["keyword"] = *req.Keyword
    }
    
    if req.Priority != nil {
        filters["priority"] = *req.Priority
    }
    
    // 调用服务层
    resources, total, err := s_db.GetResourceService().GetResourcePage(ctx, req.Page, req.PageSize, filters)
    if err != nil {
        return nil, gerror.WrapCode(gcode.CodeInternalError, err, "获取资源列表失败")
    }
    
    return &v1.GetResourceListRes{
        List:  resources,
        Total: total,
    }, nil
}
```

### 2. GoFrame 验证标签规范

**验证标签格式**：`v:"rule1|rule2|rule3"`

**常用验证规则**：

- `required` - 必填字段
- `min:value` - 最小值
- `max:value` - 最大值
- `between:min,max` - 范围值
- `length:min,max` - 字符串长度范围
- `integer` - 整数类型
- `string` - 字符串类型
- `email` - 邮箱格式
- `phone` - 手机号格式
- `url` - URL格式
- `date` - 日期格式
- `in:value1,value2` - 枚举值验证

**验证标签示例**：
```go
type GetResourceListReq struct {
    g.Meta   `path:"/resource" method:"get" tags:"模块相关" summary:"获取资源列表"`
    Page     int      `json:"page" dc:"页码" v:"required|min:1"`
    PageSize int      `json:"pageSize" dc:"每页数量" v:"required|min:1|max:100"`
    Status   *string  `json:"status" dc:"状态筛选" v:"in:active,inactive,all"`
    Keyword  *string  `json:"keyword" dc:"关键词" v:"length:0,50"`
    Email    *string  `json:"email" dc:"邮箱" v:"email"`
    Phone    *string  `json:"phone" dc:"手机号" v:"phone"`
    Priority *int     `json:"priority" dc:"优先级" v:"between:1,5"`
}
```

**验证标签最佳实践**：

1. **必填参数**：使用 `required` 标签
2. **数值范围**：使用 `min`、`max`、`between` 标签
3. **字符串长度**：使用 `length` 标签
4. **枚举值**：使用 `in` 标签
5. **格式验证**：使用 `email`、`phone`、`url`、`date` 标签
6. **类型验证**：使用 `integer`、`string` 标签

### 3. 基于 Model 定义的参数验证

**验证原则**：

- 判断传入参数是否为必填时，必须先查看对应的 Model 定义
- 如果 Model 中定义了必填字段，则该参数为必填
- 使用 `s_db_model` 中的字段常量进行验证

**验证示例**：
```go
func (c *Controller) CreateDevice(ctx context.Context, req *v1.CreateDeviceReq) (*v1.CreateDeviceRes, error) {
    // 查看 s_db_model.SDeviceModel 的定义
    // 如果 Model 中 Name 字段有 required 标签或非空约束，则验证必填
    if req.Name == "" {
        return nil, errors.New("设备名称不能为空")
    }
    
    // 如果 Model 中 Type 字段有 required 标签，则验证必填
    if req.Type == "" {
        return nil, errors.New("设备类型不能为空")
    }
    
    deviceId, err := s_db.GetDeviceService().CreateDevice(ctx, req.Name, req.Type, req.Enabled)
    if err != nil {
        return nil, gerror.WrapCode(gcode.CodeInternalError, err, "创建设备失败")
    }
    
    return &v1.CreateDeviceRes{}, nil
}
```

### 2. 参数验证检查清单

- [ ] **查看对应 Model 定义**：检查 `s_db_model` 中的相关 Model 结构体
- [ ] **检查结构体标签**：查看 `required`、`default` 等标签
- [ ] **检查数据库约束**：查看 SQL 创建语句中的 `NOT NULL`、`DEFAULT` 约束
- [ ] **检查字段类型**：指针类型通常表示可选，非指针类型通常表示必填
- [ ] **验证必填参数**：根据 Model 定义验证必填参数
- [ ] **处理可选参数**：根据 Model 定义处理可选参数

## 错误处理规范

### 1. 错误处理原则

**处理规则**：
- 参数验证错误：直接返回 `errors.New()`，让 GoFrame 自动处理错误响应
- 业务逻辑错误：使用 `gerror.WrapCode()` 包装并记录日志用于系统监控
- 成功操作：记录成功日志用于业务监控
- 避免重复日志：不要在返回错误前重复记录日志，GoFrame 会自动处理

**错误处理示例**：
```go
// ✅ 正确 - 参数验证错误，直接返回，不记录日志
if req.Id <= 0 {
    return nil, errors.New("自动化任务ID必须大于0")
}

// ✅ 正确 - 业务逻辑错误，记录日志并包装错误
err = s_db.GetAutomationService().UpdateAutomation(ctx, req.Id, updateData)
if err != nil {
    c_log.Errorf(ctx, "更新自动化任务失败: %+v", err)
    return nil, gerror.WrapCode(gcode.CodeInternalError, err, "更新自动化任务失败")
}

// ❌ 错误 - 不要在返回错误前重复记录日志
if req.Id <= 0 {
    c_log.Error(ctx, "自动化任务ID必须大于0")  // 不需要这行
    return nil, errors.New("自动化任务ID必须大于0")
}
```

### 2. 错误处理检查清单

- [ ] **参数验证错误**：直接返回 `errors.New()`，不记录日志
- [ ] **业务逻辑错误**：使用 `gerror.WrapCode()` 包装并记录日志
- [ ] **避免重复日志**：不要在返回错误前重复记录日志
- [ ] **统一错误处理**：使用 `gerror` 进行错误处理
- [ ] **错误信息支持多语言**：如需要

## 数据库字段常量使用规范

### 1. 字段常量使用原则

**重要**：在查询、更新、创建操作中，必须使用 `s_db_model` 中定义的字段常量，而不是直接使用字符串。

**使用示例**：
```go
// ✅ 正确 - 使用字段常量
filters := map[string]interface{}{
    s_db_model.FieldId:      id,
    s_db_model.FieldName:    name,
    s_db_model.FieldEnabled: enabled,
}

// ❌ 错误 - 直接使用字符串
filters := map[string]interface{}{
    "id":      id,      // 应该使用 s_db_model.FieldId
    "name":    name,    // 应该使用 s_db_model.FieldName
    "enabled": enabled, // 应该使用 s_db_model.FieldEnabled
}
```

### 2. 字段常量使用检查清单

- [ ] **查询操作中使用字段常量**：所有查询条件都使用 `s_db_model` 中的字段常量
- [ ] **更新操作中使用字段常量**：所有更新数据的 Map 都使用字段常量作为 key
- [ ] **创建操作中使用字段常量**：创建时传递的参数使用字段常量
- [ ] **分页查询中使用字段常量**：分页查询的过滤条件使用字段常量
- [ ] **导入字段常量包**：正确导入 `s_db/s_db_model` 包
- [ ] **避免硬编码字符串**：不使用硬编码的数据库字段名字符串

## 文件结构规范

### 1. 目录结构

```text
application/api/
├── [module]/                    # 模块目录
│   ├── [module].go             # 模块接口定义
│   └── v1/                      # 版本目录
│       ├── [module]_common.go   # 公共 DTO
│       ├── [module]_create.go   # 创建接口
│       ├── [module]_update.go   # 更新接口
│       ├── [module]_delete.go   # 删除接口
│       ├── [module]_list.go     # 列表查询接口
│       ├── [module]_detail.go   # 详情查询接口
│       └── [module]_[action].go # 其他特殊接口
```

### 2. 文件内容结构

**标准文件结构**：
```go
package v1

import (
    // 必要的导入
    "github.com/gogf/gf/v2/frame/g"
    // 其他导入
)

// [Action][Resource]Req 请求结构体
type [Action][Resource]Req struct {
    g.Meta `path:"/resource" method:"post" tags:"模块相关" summary:"操作描述" role:"admin"`
    // 请求字段
}

// [Action][Resource]Res 响应结构体
type [Action][Resource]Res struct {
    // 响应字段
}
```

## 开发检查清单

### 创建新 API 接口时

- [ ] **文件拆分**：一个接口一个文件
- [ ] **命名规范**：文件命名、结构体命名符合规范
- [ ] **路由配置**：正确配置路由、方法、标签、权限
- [ ] **响应结构体设计**：操作类接口使用空响应，查询类接口返回具体数据
- [ ] **非必填参数设计**：非必填参数使用指针类型（`*Type`）
- [ ] **验证标签设计**：为所有参数添加适当的 GoFrame 验证标签
- [ ] **参数验证**：基于 Model 定义进行参数验证
- [ ] **错误处理**：统一使用 `gerror` 进行错误处理
- [ ] **字段常量使用**：使用 `s_db_model` 中的字段常量
- [ ] **时间字段处理**：使用 `*time.Time` 类型
- [ ] **数据转换优化**：使用 `gconv.Map()` 和 `UnmarshalValue` 方法
- [ ] **测试功能正确性**：确保接口功能正常

### 文件拆分时

- [ ] **识别所有接口**：列出所有需要拆分的接口
- [ ] **创建公共 DTO 文件**：将公共结构体放在 `*_common.go` 中
- [ ] **按接口拆分文件**：每个接口一个文件
- [ ] **保持原有功能**：确保所有路由配置、验证规则保持不变
- [ ] **检查语法错误**：确保所有文件没有语法错误
- [ ] **删除原始文件**：拆分完成后删除原始文件

### 数据转换优化时

- [ ] **实现 UnmarshalValue 方法**：在 DTO 结构体中实现统一的数据转换方法
- [ ] **使用 gconv.Map() 转换请求**：避免手动处理每个字段
- [ ] **查询参数自动处理**：使用 gconv.Map() 处理查询参数，自动移除 nil 值
- [ ] **移除重复转换代码**：Controller 中只调用 UnmarshalValue 方法
- [ ] **统一错误处理**：转换失败时返回具体错误信息
- [ ] **性能优化**：避免不必要的类型断言和字符串拼接
- [ ] **类型安全**：确保所有转换都有适当的类型检查

## 最佳实践示例

### 完整的模块拆分示例

**原始文件**：`energy_storage_strategy.go`（包含 8 个接口）

**拆分后**：
1. `energy_storage_strategy_common.go` - 公共 DTO
2. `energy_storage_strategy_create.go` - 创建接口
3. `energy_storage_strategy_update.go` - 更新接口
4. `energy_storage_strategy_delete.go` - 删除接口
5. `energy_storage_strategy_list.go` - 列表查询接口
6. `energy_storage_strategy_detail.go` - 详情查询接口
7. `energy_storage_strategy_conflicts.go` - 冲突检测接口
8. `energy_storage_strategy_activate.go` - 激活接口

### 数据转换优化示例

**优化前的问题**：
```go
// Controller 中重复的转换逻辑
dto := &v1.EnergyStorageStrategy{
    Id:          m.Id,
    Name:        m.Name,
    Description: m.Description,
    Priority:    m.Priority,
    Status:      c_enum.ParseStatus(m.Status),
    CreatedAt:   &m.CreatedAt.Time,
    UpdatedAt:   &m.UpdatedAt.Time,
    IsDefault:   m.IsDefault,
}
// 反序列化配置结构体
var dateRange p_energy_manage.SDateRange
_ = gjson.DecodeTo(m.DateRange, &dateRange)
dto.DateRange = &dateRange
// ... 更多重复代码
```

**优化后的解决方案**：
```go
// 1. 在 DTO 中实现 UnmarshalValue 方法
func (s *EnergyStorageStrategy) UnmarshalValue(value interface{}) error {
    if model, ok := value.(*s_db_model.SEnergyStorageStrategyModel); ok {
        // 统一处理所有转换逻辑
        s.Id = model.Id
        s.Name = model.Name
        // ... 其他字段转换
        return nil
    }
    return errors.Errorf(`unsupported value type: %v`, reflect.TypeOf(value))
}

// 2. Controller 中简化调用
dto := &v1.EnergyStorageStrategy{}
if err := dto.UnmarshalValue(model); err != nil {
    return nil, err
}
```

### 参考文件

- [application/api/automation/v1/](mdc:application/api/automation/v1/) - 自动化模块 API 示例
- [application/api/setting/v1/](mdc:application/api/setting/v1/) - 设置模块 API 示例
- [application/api/policy/v1/](mdc:application/api/policy/v1/) - 策略模块 API 示例
- [application/internal/model/entity/device.go](mdc:application/internal/model/entity/device.go) - 实体模型 UnmarshalValue 方法示例

## Controller 实现规范

### 1. Controller 结构体定义

**命名规范**：
- Controller 结构体：`[Module]ControllerV1` 或 `ControllerV1`
- 方法命名：与 API 接口方法名保持一致

**结构体定义示例**：
```go
type EnergyStorageStrategyControllerV1 struct{}

// 或者使用通用命名
type ControllerV1 struct{}
```

### 2. Controller 方法实现规范

**方法签名规范**：
```go
func (c *ControllerV1) [Action][Resource](ctx context.Context, req *v1.[Action][Resource]Req) (res *v1.[Action][Resource]Res, err error)
```

**方法实现结构**：
```go
func (c *ControllerV1) CreateResource(ctx context.Context, req *v1.CreateResourceReq) (res *v1.CreateResourceRes, err error) {
    // 1. 参数验证
    // 2. 业务验证（如需要）
    // 3. 数据转换
    // 4. 调用服务层
    // 5. 构建响应
    // 6. 返回结果
}
```

### 3. 数据转换规范

**核心原则**：
- 使用 `gconv.Map(req)` 自动转换结构体为 Map
- 实现 `UnmarshalValue` 方法统一处理数据转换
- 避免在Controller中重复编写转换逻辑

#### 3.1 请求数据转换

**使用 `gconv.Map()` 自动转换**：
```go
// ✅ 正确 - 使用 gconv.Map 自动转换
data := gconv.Map(req)
delete(data, "id") // 移除不需要的字段

// ❌ 错误 - 手动处理每个字段
data := map[string]interface{}{
    "name": req.Name,
    "type": req.Type,
    // ... 大量重复代码
}
```

**查询参数处理**：
```go
// ✅ 正确 - 使用 gconv.Map 处理查询参数
filters := gconv.Map(req)
delete(filters, "page")
delete(filters, "pageSize")

// 移除 nil 值
for key, value := range filters {
    if value == nil {
        delete(filters, key)
    }
}

// ❌ 错误 - 手动处理每个查询参数
filters := map[string]interface{}{}
if req.Status != nil {
    filters["status"] = req.Status.String()
}
if req.Priority != nil {
    filters["priority"] = *req.Priority
}
```

#### 3.2 响应数据转换

**实现 `UnmarshalValue` 方法**：
```go
// 在 DTO 结构体中实现 UnmarshalValue 方法
func (s *EnergyStorageStrategy) UnmarshalValue(value interface{}) error {
    if model, ok := value.(*s_db_model.SEnergyStorageStrategyModel); ok {
        // 基础字段映射
        s.Id = model.Id
        s.Name = model.Name
        s.Description = model.Description
        s.Priority = model.Priority
        s.Status = c_enum.ParseStatus(model.Status)
        
        // 时间字段转换
        if model.CreatedAt != nil {
            s.CreatedAt = &model.CreatedAt.Time
        }
        if model.UpdatedAt != nil {
            s.UpdatedAt = &model.UpdatedAt.Time
        }
        
        // JSON字段反序列化
        if model.DateRange != "" {
            var dateRange p_energy_manage.SDateRange
            if err := gjson.DecodeTo(model.DateRange, &dateRange); err == nil {
                s.DateRange = &dateRange
            }
        }
        
        return nil
    }
    return errors.Errorf(`unsupported value type for UnmarshalValue: %v`, reflect.TypeOf(value))
}
```

**Controller中使用 `UnmarshalValue`**：
```go
// ✅ 正确 - 使用 UnmarshalValue 方法
dto := &v1.EnergyStorageStrategy{}
if err := dto.UnmarshalValue(model); err != nil {
    return nil, err
}

// ❌ 错误 - 手动转换每个字段
dto := &v1.EnergyStorageStrategy{
    Id:          model.Id,
    Name:        model.Name,
    Description: model.Description,
    // ... 大量重复代码
}
// 反序列化配置结构体
var dateRange p_energy_manage.SDateRange
_ = gjson.DecodeTo(model.DateRange, &dateRange)
dto.DateRange = &dateRange
```

#### 3.3 数据转换最佳实践

**转换逻辑统一管理**：
- 所有数据转换逻辑集中在 `UnmarshalValue` 方法中
- Controller只负责调用转换方法
- 避免在Controller中重复编写转换代码

**错误处理**：
- 转换失败时返回具体错误信息
- 使用 `errors.Errorf` 提供详细的错误上下文
- 支持类型检查，确保转换的类型正确

**性能优化**：
- 使用 `gconv.Map()` 自动处理类型转换
- 避免不必要的字符串拼接和类型断言
- 统一处理 JSON 反序列化

### 4. 日志记录规范

**日志级别使用**：
- `g.Log().Info()` - 正常操作日志
- `g.Log().Error()` - 错误日志
- `c_log.BizInfo()` - 业务操作日志
- `c_log.BizError()` - 业务错误日志

**日志记录示例**：
```go
// 操作开始日志
g.Log().Infof(ctx, "创建资源 - 名称: %s", req.Name)

// 成功日志
g.Log().Infof(ctx, "成功创建资源 - ID: %s", resourceId)

// 错误日志
g.Log().Errorf(ctx, "创建资源失败: %+v", err)
```

## 验证规范

### 1. 参数验证规范

**验证原则**：
- 必填参数验证：使用 `errors.New()` 直接返回
- 参数格式验证：使用 GoFrame 的验证标签
- 业务参数验证：使用专门的验证函数

**验证示例**：
```go
// 必填参数验证
if req.Id == "" {
    return nil, errors.New("资源ID不能为空")
}

// 数值范围验证
if req.Id <= 0 {
    return nil, errors.New("资源ID必须大于0")
}

// 业务参数验证
if err := validateBusinessRule(req); err != nil {
    return nil, err
}
```

### 2. 数据转换规范

**转换原则**：
- 使用 `gconv.Map()` 进行结构体到 Map 的转换
- 枚举类型转换为字符串：`req.Status.String()`
- 时间类型转换：`&req.StartTime.Time`
- 移除不需要的字段：`delete(data, "id")`

**转换示例**：
```go
// 结构体转 Map
data := gconv.Map(req)

// 枚举转字符串
data["status"] = req.Status.String()

// 时间转换
if req.StartTime != nil {
    data["startTime"] = &req.StartTime.Time
}

// 移除字段
delete(data, "id")
```

### 3. 验证检查清单

- [ ] **必填参数验证**：检查所有必填参数
- [ ] **参数格式验证**：检查参数格式和类型
- [ ] **业务规则验证**：调用业务验证函数
- [ ] **数据转换**：正确转换请求数据
- [ ] **字段清理**：移除不需要的字段

## Controller 到数据库的规范流程

### 1. 标准流程步骤

**完整流程**：
1. **参数验证** - 验证请求参数
2. **业务验证** - 验证业务规则
3. **数据转换** - 转换请求数据格式
4. **服务调用** - 调用数据库服务层
5. **错误处理** - 处理服务层错误
6. **响应构建** - 构建响应数据
7. **日志记录** - 记录操作日志

### 2. 服务层调用规范

**调用模式**：
```go
// 创建操作
id, err := s_db.GetResourceService().CreateResource(ctx, data)
if err != nil {
    return nil, gerror.WrapCode(gcode.CodeInternalError, err, "创建资源失败")
}

// 更新操作
err = s_db.GetResourceService().UpdateResource(ctx, req.Id, data)
if err != nil {
    return nil, gerror.WrapCode(gcode.CodeInternalError, err, "更新资源失败")
}

// 查询操作
resource, err := s_db.GetResourceService().GetResourceById(ctx, req.Id)
if err != nil {
    return nil, gerror.WrapCode(gcode.CodeInternalError, err, "获取资源失败")
}
```

### 3. 错误处理规范

**错误处理层次**：
1. **参数验证错误**：直接返回 `errors.New()`
2. **业务验证错误**：返回业务错误信息
3. **服务层错误**：使用 `gerror.WrapCode()` 包装
4. **数据转换错误**：记录日志并返回内部错误

**错误处理示例**：
```go
// 参数验证错误
if req.Id == "" {
    return nil, errors.New("资源ID不能为空")
}

// 业务验证错误
if err := validateBusinessRule(req); err != nil {
    return nil, err
}

// 服务层错误
resource, err := s_db.GetResourceService().GetResourceById(ctx, req.Id)
if err != nil {
    g.Log().Errorf(ctx, "获取资源失败: %+v", err)
    return nil, gerror.WrapCode(gcode.CodeInternalError, err, "获取资源失败")
}

// 数据转换错误
if err := resourceEntity.UnmarshalValue(resource); err != nil {
    g.Log().Errorf(ctx, "转换资源实体失败: %+v", err)
    return nil, gerror.WrapCode(gcode.CodeInternalError, err, "转换资源实体失败")
}
```

### 4. 响应构建规范

**响应构建原则**：
- 操作类接口：返回空结构体
- 查询类接口：返回具体数据
- 实体转换：使用 `UnmarshalValue()` 方法
- 时间字段：使用 `*time.Time` 类型

**响应构建示例**：
```go
// 操作类接口响应
return &v1.CreateResourceRes{}, nil

// 查询类接口响应
res = &v1.GetResourceRes{
    Resource: &resourceEntity,
}

// 列表接口响应
res = &v1.GetResourceListRes{
    List:  resourceList,
    Total: total,
}
```

### 5. 数据库操作检查清单

- [ ] **参数验证完成**：所有必填参数已验证
- [ ] **业务验证通过**：业务规则验证通过
- [ ] **数据转换正确**：请求数据正确转换
- [ ] **服务调用成功**：数据库操作成功
- [ ] **错误处理完整**：所有错误情况已处理
- [ ] **响应构建正确**：响应数据格式正确
- [ ] **日志记录完整**：关键操作已记录日志

## 注意事项

1. **不向前兼容**：完全移除 v1 兼容性，直接使用新结构
2. **数据不可变**：点位创建后不允许修改
3. **启动验证**：系统启动时验证所有必填字段
4. **接口继承**：子结构体不重复实现接口方法
5. **预定义优先**：优先使用 `c_default` 包配置

---

**注意**: 本规范是动态文档，会根据项目发展和团队反馈持续更新。所有 API 开发都应遵循本规范的要求。