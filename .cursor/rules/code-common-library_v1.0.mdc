---
alwaysApply: true
description: Common library structure and conventions for the EMS plan project - 定义通用库结构、命名规范、代码注解规范、枚举使用规范、错误处理规范等。适用于所有 common/ 目录下的代码开发、接口定义、结构体设计、枚举创建等场景。
globs: common/**/*.go
---

# Common Library 结构指南

`common/` 目录是项目的核心共享库，包含所有模块间共享的接口、类型定义和基础功能。

## 目录结构

### 核心模块

- **c_base/**: 基础接口和类型定义
- **c_device/**: 设备相关实现
- **c_proto/**: 协议相关
- **c_func/**: 聚合函数
- **c_chart/**: 图表数据结构
- **c_status/**: 状态管理
- **c_alarm/**: 告警管理
- **c_log/**: 日志接口
- **c_timer/**: 定时器功能
- **c_enum/**: 枚举定义和常量

## 命名约定

### 文件命名规范

- `c_*_i.go`: 接口定义 (Interface)
- `c_*_s.go`: 结构体定义 (Struct)
- `c_*_e.go`: 枚举定义 (Enum)
- `c_*_f.go`: 函数定义 (Function)
- `c_*_c.go`: 常量定义 (Constant)
- `*_test.go`: 测试文件

**特殊文件命名**:

- `*_si.go`: 单例接口
- `*_impl_s.go`: 实现结构体
- `*_config_s.go`: 配置结构体
- `*_e_string.go`: 枚举字符串方法 (由 stringer 工具生成)

### 类型命名规范

- **接口**: `I` 前缀 - 如 `IDevice`, `IDriver`, `IStorage`
- **结构体**: `S` 前缀 - 如 `SDeviceConfig`, `SDriverInfo`
- **枚举**: `E` 前缀 - 如 `EDeviceType`, `EProtocolType`, `EAlarmLevel`
- **常量**: `C` 前缀 - 如 `CDeviceTypeAmmeter`

### 方法命名规范

- **Getter方法**: `Get + 属性名` - 如 `GetConfig()`
- **Setter方法**: `Set + 属性名` - 如 `SetConfig()`
- **判断方法**: `Is + 状态描述` - 如 `IsVirtualDevice()`
- **操作方法**: 动词 + 对象 - 如 `Start()`, `Shutdown()`
- **构造函数**: `New + 类型名` - 如 `NewDevice()`

### 变量命名规范

- **局部变量**: 小写开头的驼峰命名 - 如 `deviceId`
- **包级别变量**: 小写开头的驼峰命名 - 如 `deviceManager`
- **常量**: 全大写字母和下划线 - 如 `DEFAULT_TIMEOUT`

## 代码注解规范

### 方法注解规范

所有公共方法都应该添加简洁明了的注释，说明方法的功能和用途：

```go
// Start 启动管理器
Start(ctx context.Context, interval time.Duration) error

// GetDeviceById 根据设备ID获取设备实例
GetDeviceById(deviceId string) c_base.IDevice

// IsVirtualDevice 判断是否为虚拟设备
IsVirtualDevice() bool

// CreateDevice 创建设备实例
CreateDevice(config *SDeviceConfig) (c_base.IDevice, error)
```

**方法注解要求**：

- **格式**: `// 方法名 功能描述`
- **语言**: 使用中文描述，简洁明了
- **内容**: 说明方法的主要功能和用途
- **位置**: 紧贴方法定义上方
- **一致性**: 所有公共方法都必须有注解

### 字段注解规范

结构体字段的注解应该放在字段后面，使用 `json` 和 `orm` 标签：

```go
type SDeviceConfig struct {
    DeviceId    string      `json:"deviceId" orm:"device_id"`           // 设备唯一标识
    DeviceType  EDeviceType `json:"deviceType" orm:"device_type"`       // 设备类型
    DeviceName  string      `json:"deviceName" orm:"device_name"`       // 设备名称
    IsEnabled   bool        `json:"isEnabled" orm:"is_enabled"`         // 是否启用
    CreatedAt   *gtime.Time `json:"createdAt" orm:"created_at"`         // 创建时间
    UpdatedAt   *gtime.Time `json:"updatedAt" orm:"updated_at"`         // 更新时间
}
```

**字段注解要求**：

- **位置**: 放在字段定义行的末尾
- **格式**: `// 字段功能描述`
- **语言**: 使用中文描述
- **内容**: 简洁说明字段的用途和含义
- **一致性**: 所有公共字段都应该有注解

### 接口注解规范

接口定义应该包含整体功能说明：

```go
// IDeviceManager 设备管理器接口，负责设备的生命周期管理
type IDeviceManager interface {
    // Start 启动管理器
    Start(ctx context.Context, interval time.Duration) error
    
    // Shutdown 关闭管理器
    Shutdown(ctx context.Context) error
    
    // GetDeviceById 根据设备ID获取设备实例
    GetDeviceById(deviceId string) c_base.IDevice
}
```

### 结构体注解规范

结构体定义应该包含用途说明：

```go
// SDeviceConfig 设备配置结构体，包含设备的基本配置信息
type SDeviceConfig struct {
    DeviceId   string      `json:"deviceId" orm:"device_id"`     // 设备唯一标识
    DeviceType EDeviceType `json:"deviceType" orm:"device_type"` // 设备类型
    // ... 其他字段
}
```

### 常量注解规范

常量定义应该说明其用途：

```go
const (
    // CDeviceTypeAmmeter 电表设备类型
    CDeviceTypeAmmeter = "ammeter"
    
    // CDeviceTypePCS 储能变流器设备类型
    CDeviceTypePCS = "pcs"
    
    // CDefaultTimeout 默认超时时间（秒）
    CDefaultTimeout = 30
)
```

### 注解最佳实践

1. **简洁明了**: 注解应该简洁，避免冗长的描述
2. **中文优先**: 使用中文进行注解，便于团队理解
3. **功能导向**: 重点说明"做什么"而不是"怎么做"
4. **一致性**: 保持注解风格的一致性
5. **及时更新**: 代码修改时同步更新注解

## 常用枚举

### 设备相关枚举

- **EDeviceType**: 设备类型枚举，定义系统中支持的各种设备类型（如电表、储能、光伏等）
- **EBmsStatus**: 电池管理系统状态，用于表示BMS设备的运行状态
- **EPvStatus**: 光伏设备状态，用于表示光伏设备的连接和运行状态
- **ECoolingI**: 制冷设备状态，用于表示空调和液冷机组的运行状态

### 协议相关枚举

- **EProtocolType**: 协议类型，定义支持的通信协议（如Modbus TCP/RTU、CAN总线等）
- **EDataFormat**: 数据格式，定义Modbus等协议中数据的编码格式
- **EEndianness**: 字节序，定义数据的大端/小端存储方式
- **EByteEndian**: 字节序类型，用于数据解析时的字节序处理
- **EWordOrder**: 字序，定义多字节数据的排列顺序

### 系统状态枚举

- **EServerState**: 服务器状态，定义各种Manager服务的运行状态
- **EAlarmLevel**: 告警级别，定义告警的严重程度等级
- **EAlarmAction**: 告警动作，定义告警触发时的处理动作
- **ELogLevel**: 日志级别，定义日志输出的详细程度

### 数据存储枚举

- **EStorageType**: 存储类型，定义支持的数据存储后端
- **EValueType**: 值类型，定义数据在系统中的标准类型
- **EPointSystemType**: 点位系统类型，定义数据点的系统分类
- **EPointDirection**: 点位方向，定义数据点的读写方向

### 配置相关枚举

- **ESettingGroup**: 设置分组，定义系统配置的分组类型
- **EConfigFieldsValueType**: 配置字段值类型，定义配置项的数据类型
- **EConfigFieldsComponentType**: 配置字段组件类型，定义前端组件的类型

### 枚举使用规范

1. **文件命名**: 枚举文件使用 `c_enum_<描述>_e.go` 格式
2. **类型命名**: 枚举类型使用 `E` 前缀，如 `EDeviceType`
3. **字符串方法**: 使用 `//go:generate stringer` 自动生成字符串方法
4. **JSON序列化**: 支持自定义JSON序列化/反序列化方法
5. **常量定义**: 枚举值使用有意义的常量名，并添加中文注释

### 枚举最佳实践

- 所有枚举都应该有清晰的中文注释说明用途
- 使用 `stringer` 工具生成字符串方法便于调试
- 对于需要JSON序列化的枚举，实现自定义的 `MarshalJSON` 和 `UnmarshalJSON` 方法
- 枚举值应该具有明确的语义，避免使用魔法数字
- 新增枚举值时保持向后兼容性

## 核心接口

### 设备管理

```go
type IDeviceManager interface {
    Start()
    Shutdown()
    GetDeviceById(deviceId string) c_base.IDevice
    IteratorAllDevices(func(config *c_base.SDeviceConfig, device c_base.IDevice) bool)
}
```

### 设备接口

```go
type IDevice interface {
    IAlarm
    GetConfig() *SDeviceConfig
    GetProtocolStatus() c_status.EProtocolStatus
    GetMetaValueList() []*MetaValueWrapper
    IsVirtualDevice() bool
}
```

### 存储接口

```go
type IStorage interface {
    SaveDevices(deviceId string, deviceType EDeviceType, fields map[string]any) error
    SaveProtocolMetrics(protocolConfig *SProtocolConfig, deviceConfig *SDeviceConfig, metrics map[string]any) error
    GetStorageData(storageType StorageType, id string, pointKey []string, startTime, endTime *int, step int) (*c_chart.ChartData, error)
    GetStorageStats() (*StorageStats, error)
}
```

## 使用模式

### 单例注册模式

```go
func RegisterDeviceManager(cmd IDeviceManager) {
    deviceManager = cmd
}

func GetDeviceManager() IDeviceManager {
    if deviceManager == nil {
        panic("device manager is nil !")
    }
    return deviceManager
}
```

### 泛型聚合函数

```go
func avgAggregate[T c_base.Number](values []any, convert func(interface{}) (T, error)) (T, error)
```

## 日志使用规范

### 日志分类

- **系统日志**: `c_log.级别名(ctx, 参数...)` - 用于系统运行状态、错误信息等
- **业务日志**: `c_log.Biz级别名(ctx, 参数...)` - 用于业务操作记录、用户行为等

### 日志级别

- `Debug`, `Info`, `Notice`, `Warning`, `Error`, `Critical`, `Panic`, `Fatal`

### 使用示例

```go
// 系统日志
c_log.Info(ctx, "系统启动完成")
c_log.Infof(ctx, "设备 %s 连接成功", deviceId)
c_log.Error(ctx, "Modbus通信失败", err)

// 业务日志
c_log.BizInfo(ctx, "用户登录", userId)
c_log.BizInfof(ctx, "设备配置更新: 设备ID=%s", deviceId)
```

## 错误处理规范

### 统一使用 `github.com/pkg/errors`

```go
import "github.com/pkg/errors"
```

### 错误处理规则

1. **错误包装**: 使用 `errors.Wrap()` 或 `errors.Wrapf()` 添加上下文
2. **错误创建**: 使用 `errors.New()` 或 `errors.Errorf()`
3. **错误判断**: 使用 `errors.Is()` 和 `errors.As()`
4. **错误链**: 保持错误链的完整性

### 错误处理示例

```go
// ✅ 正确
func ReadConfig(filename string) (*Config, error) {
    data, err := os.ReadFile(filename)
    if err != nil {
        return nil, errors.Wrap(err, "读取配置文件失败")
    }
    return &config, nil
}

// ❌ 错误
func ReadConfig(filename string) (*Config, error) {
    data, err := os.ReadFile(filename)
    if err != nil {
        return nil, err // 缺少上下文信息
    }
    return &config, nil
}
```

## 依赖关系

- `github.com/shockerli/cvt` - 类型转换
- `github.com/pkg/errors` - 错误处理
- `gopkg.in/yaml.v3` - YAML 配置解析

## 构建标签 (Build Tags)

### 开发环境构建

项目使用 Go 的构建标签 (build tags) 来区分开发环境和生产环境：

```bash
# 开发环境构建 - 必须添加 -tags=dev 参数
go build -tags=dev -o ems main.go

# 生产环境构建 - 不添加 dev 标签
go build -o ems main.go
```

### 构建标签的作用

- **`dev` 标签**: 启用开发环境功能
  - 直接加载驱动插件而非从配置文件读取
  - 启用调试日志和开发工具
  - 包含开发专用的代码路径

- **无标签**: 生产环境构建
  - 从配置文件加载驱动
  - 优化性能和安全性
  - 移除调试代码

### 相关文件

- `services/s_driver/internal/s_driver_device_get_dev.go` - 开发环境驱动加载
- `application/manifest/config/config.yaml` - 构建配置中的 `extra: "-tags=dev"`
- `application/build_with_gui.sh` - 构建脚本中的标签使用
- `build_armv7l.sh` - ARM 交叉编译脚本

### 构建脚本示例

```bash
# 开发环境构建
go build -tags=dev -o ems main.go

# 带 GUI 的开发环境构建
go build -tags="dev,cgo" -o ems main.go

# 生产环境构建
go build -o ems main.go
```

## 测试模式

### 测试模式功能

项目提供了测试模式功能，用于快速验证程序是否能正常启动，特别适用于 Cursor AI 等自动化测试场景。

### 使用方法

```bash
# 启动测试模式 - 程序将在3秒后自动关闭
./ems --test=true

# 或使用短参数
./ems -t

# 结合其他参数使用
./ems --test=true --profile dev
```

### 测试模式特性

- **自动关闭**: 程序启动3秒后自动关闭，无需手动干预
- **完整启动**: 执行完整的系统初始化流程，包括：
  - 配置加载
  - 数据库初始化
  - 服务启动
  - 设备管理器启动
- **日志记录**: 记录完整的启动和关闭日志
- **安全退出**: 使用标准的关闭信号处理机制

### 测试模式实现

测试模式通过以下方式实现：

1. **参数定义**: 在 `application/internal/cmd/main.go` 中定义 `ArgTest` 常量
2. **命令行参数**: 添加 `--test` 或 `-t` 参数支持
3. **自动关闭逻辑**: 启动后检查测试模式标志，如果启用则启动定时器
4. **优雅关闭**: 使用 `context.CancelFunc` 触发优雅关闭流程

### 适用场景

- **CI/CD 测试**: 自动化构建流水线中的启动测试
- **开发环境验证**: 快速验证代码修改后程序能否正常启动
- **Cursor AI 测试**: AI 助手验证程序启动状态
- **部署前检查**: 生产环境部署前的启动验证

### 测试模式注意事项

- 测试模式仅验证启动流程，不执行完整的业务逻辑
- 3秒的延迟确保所有服务都有足够时间完成初始化
- 测试模式会创建临时文件（如PID文件），程序关闭时会自动清理

## 开发指南

### 新增功能开发流程

1. 在对应的枚举文件中添加枚举值
2. 在 `c_*_i.go` 文件中定义接口
3. 在 `c_*_s.go` 文件中实现
4. 添加对应的 `*_test.go` 测试文件

### 代码审查要点

1. **命名一致性**: 遵循项目的命名规范
2. **类型安全**: 确保泛型使用正确
3. **错误处理**: 统一使用 `github.com/pkg/errors`
4. **接口实现**: 确保所有接口都有对应实现
5. **日志使用**: 正确使用 `c_log` 进行日志记录
6. **注解规范**: 确保所有公共方法、字段、接口和结构体都有适当的注解

## 注意事项

### 代码质量要求

- 所有接口都应该有对应的实现
- 使用泛型时确保类型安全
- 错误处理要统一使用 `github.com/pkg/errors`
- 单例模式要确保线程安全
- 配置结构体要支持 YAML 序列化
- 所有公共方法、字段、接口和结构体都必须有清晰的中文注解
- 注解要简洁明了，重点说明功能和用途

### 命名规范要求

- **严格遵循前缀规则**: 接口 `I`、结构体 `S`、枚举 `E`、常量 `C`
- **文件名一致性**: 必须使用正确的类型后缀
- **避免命名冲突**: 确保类型名在包内唯一
- **语义化命名**: 所有命名都应具有明确的语义
- **向后兼容**: 修改现有类型名时要考虑向后兼容性

### 构建标签要求

- **开发环境构建**: 必须使用 `-tags=dev` 参数
- **生产环境构建**: 不要使用 `dev` 标签
- **构建脚本**: 确保构建脚本正确设置标签参数
- **配置文件**: 检查 `config.yaml` 中的 `extra: "-tags=dev"` 设置
- **交叉编译**: ARM 等交叉编译也要包含正确的构建标签
