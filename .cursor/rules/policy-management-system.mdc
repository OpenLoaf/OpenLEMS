---
alwaysApply: false
description: 策略统一管理系统规范 - 策略插件开发、管理和使用指南
---

# 策略统一管理系统规范

## 概述

策略统一管理系统提供了类似驱动管理器的策略插件架构，支持多种策略的注册、切换和定时执行。系统每分钟自动执行激活的策略，策略可以从 setting 表加载配置并执行相应的业务逻辑。

## 核心架构

### 1. 策略接口定义

**文件**: [c_base_policy_i.go](mdc:common/c_base/c_base_policy_i.go)

所有策略插件必须实现 `IPolicy` 接口：

```go
type IPolicy interface {
    Init(ctx context.Context) error              // 初始化策略
    Shutdown()                                    // 关闭策略
    Run(ctx context.Context) error               // 执行策略（每分钟调用一次）
    GetPolicyId() string                         // 获取策略ID
    GetPolicyName() string                       // 获取策略名称
    GetConfig() interface{}                      // 获取策略配置
    GetPolicyInfo() *SPolicyInfo                 // 获取策略详细信息
}
```

### 2. 策略管理器

**文件**: [c_policy_manager_si.go](mdc:common/c_policy_manager_si.go)

策略管理器单例接口：

```go
type IPolicyManager interface {
    Start(ctx context.Context) error             // 启动策略管理器
    Shutdown()                                    // 关闭策略管理器
    Status() c_enum.EServerState                 // 获取运行状态
    GetActivePolicyId() string                   // 获取当前激活的策略ID
    GetActivePolicy() c_base.IPolicy             // 获取当前激活的策略实例
    SwitchPolicy(ctx context.Context, policyId string) error  // 切换策略
    RegisterPolicy(policyId string, policy c_base.IPolicy) error  // 注册策略插件
    GetAllRegisteredPolicies() []*c_base.SPolicyInfo  // 获取所有已注册的策略信息
}
```

**实现文件**: [s_policy_manager_impl_s.go](mdc:services/s_policy/internal/s_policy_manager_impl_s.go)

### 3. 策略配置存储

- **激活的策略ID**: 存储在 setting 表中，ID 为 `"active_policy_id"`
- **策略配置**: 存储在 setting 表中，ID 为策略ID本身（如 `"policy_microgrid"`, `"policy_ess"`）
- **配置格式**: JSON 字符串
- **配置为空**: 策略初始化成功但不执行业务逻辑

## 策略插件开发指南

### 1. 创建策略插件结构

**目录结构**:
```
plugins/plug_policy/p_policy_[name]/
├── internal/
│   ├── p_policy_[name]_config_s.go     # 策略配置结构体
│   └── p_policy_[name]_main_s_f.go     # 策略实现
├── export_policy_[name]_f.go           # 导出函数
└── go.mod                               # 模块依赖
```

### 2. 定义策略配置结构体

**文件**: `internal/p_policy_[name]_config_s.go`

```go
package internal

type SPolicyConfig struct {
    // 策略配置字段
    DeviceIds []string `json:"deviceIds" name:"设备列表" ct:"multiSelect"`
    Mode      string   `json:"mode" name:"模式" ct:"singleSelect" opts:"auto:自动,manual:手动"`
    // ... 其他配置字段
}
```

### 3. 实现策略主逻辑

**文件**: `internal/p_policy_[name]_main_s_f.go`

```go
package internal

import (
    "common/c_base"
    "common/c_log"
    "context"
    "encoding/json"
    "s_db"
    
    "github.com/pkg/errors"
)

type sPolicyImpl struct {
    policyId   string
    policyName string
    config     *SPolicyConfig
    ctx        context.Context
}

// NewPolicy 创建策略实例
func NewPolicy() c_base.IPolicy {
    return &sPolicyImpl{
        policyId:   "policy_[name]",
        policyName: "[策略名称]",
        config:     &SPolicyConfig{},
    }
}

// Init 初始化策略
func (s *sPolicyImpl) Init(ctx context.Context) error {
    s.ctx = ctx
    c_log.Infof(ctx, "正在初始化[策略名称]...")
    
    // 从setting表加载配置
    configStr := s_db.GetSettingService().GetSettingValueById(ctx, s.policyId)
    if configStr == "" {
        c_log.Warning(ctx, "[策略名称]配置为空，策略将不执行")
        s.config = nil
        return nil
    }
    
    // 解析JSON配置
    err := json.Unmarshal([]byte(configStr), s.config)
    if err != nil {
        return errors.Wrap(err, "解析[策略名称]配置失败")
    }
    
    c_log.Infof(ctx, "[策略名称]初始化成功")
    return nil
}

// Run 执行策略（每分钟调用一次）
func (s *sPolicyImpl) Run(ctx context.Context) error {
    // 配置为空时不执行
    if s.config == nil {
        c_log.Debug(ctx, "[策略名称]配置为空，跳过执行")
        return nil
    }
    
    c_log.Debug(ctx, "[策略名称]开始执行...")
    
    // TODO: 实现具体业务逻辑
    
    c_log.Debug(ctx, "[策略名称]执行完成")
    return nil
}

// Shutdown 关闭策略
func (s *sPolicyImpl) Shutdown() {
    if s.ctx != nil {
        c_log.Info(s.ctx, "[策略名称]关闭")
    }
}

// GetPolicyId 获取策略ID
func (s *sPolicyImpl) GetPolicyId() string {
    return s.policyId
}

// GetPolicyName 获取策略名称
func (s *sPolicyImpl) GetPolicyName() string {
    return s.policyName
}

// GetConfig 获取策略配置
func (s *sPolicyImpl) GetConfig() interface{} {
    return s.config
}

// GetPolicyInfo 获取策略详细信息
func (s *sPolicyImpl) GetPolicyInfo() *c_base.SPolicyInfo {
    return &c_base.SPolicyInfo{
        PolicyId:           s.policyId,
        PolicyName:         s.policyName,
        Description:        "[策略描述]",
        ConfigStructFields: nil,
    }
}
```

### 4. 导出策略创建函数

**文件**: `export_policy_[name]_f.go`

```go
package p_policy_[name]

import (
    "common/c_base"
    "p_policy_[name]/internal"
)

// NewPolicy[Name] 创建策略实例
func NewPolicy[Name]() c_base.IPolicy {
    return internal.NewPolicy()
}
```

### 5. 注册策略到系统

在 [ems.go](mdc:application/internal/cmd/ems.go) 的 `InitSystem` 函数中：

```go
// 注册策略插件
err := policyManager.RegisterPolicy("policy_[name]", p_policy_[name].NewPolicy[Name]())
if err != nil {
    g.Log().Errorf(ctx, "注册[策略名称]失败: %+v", err)
}
```

## 现有策略插件

### 1. 微电网策略

**策略ID**: `"policy_microgrid"`

**文件**: 
- [p_policy_mircogrid_config_s.go](mdc:plugins/plug_policy/p_policy_mircogrid/internal/p_policy_mircogrid_config_s.go)
- [p_policy_mircogrid_main_s_f.go](mdc:plugins/plug_policy/p_policy_mircogrid/internal/p_policy_mircogrid_main_s_f.go)

**功能**: 微电网能量管理策略，支持需量控制、削峰填谷、动态扩容等功能

### 2. 储能站策略

**策略ID**: `"policy_ess"`

**文件**:
- [p_policy_energy_storage_config_s.go](mdc:plugins/plug_policy/p_policy_energy_storage/internal/p_policy_energy_storage_config_s.go)
- [p_policy_energy_storage_main_s_f.go](mdc:plugins/plug_policy/p_policy_energy_storage/internal/p_policy_energy_storage_main_s_f.go)

**功能**: 储能站能量管理策略，根据定时任务执行充放电操作

## 策略使用指南

### 1. 快速访问接口

```go
// 获取当前激活的策略ID
policyId := common.GetPolicyManager().GetActivePolicyId()

// 获取当前激活的策略实例
policy := common.GetPolicyManager().GetActivePolicy()

// 获取当前激活的储能定时任务
task, err := s_policy.GetActiveEnergyStorageTask(ctx)

// 切换策略
err := common.GetPolicyManager().SwitchPolicy(ctx, "policy_microgrid")
```

### 2. 策略管理器生命周期

**启动**: 在 [ems.go](mdc:application/internal/cmd/ems.go) 的 `StartServices` 函数中，策略管理器会在设备管理器启动 5 秒后自动启动

**执行**: 每分钟自动执行一次激活策略的 `Run` 方法

**关闭**: 在系统关闭时，策略管理器会自动停止定时器并关闭当前激活的策略

### 3. 策略配置管理

**设置激活的策略**:
```go
s_db.GetSettingService().SetSettingValueById(ctx, "active_policy_id", "policy_microgrid")
```

**设置策略配置**:
```go
configJson := `{"deviceIds":["device1","device2"],"mode":"auto"}`
s_db.GetSettingService().SetSettingValueById(ctx, "policy_microgrid", configJson)
```

**获取策略配置**:
```go
configStr := s_db.GetSettingService().GetSettingValueById(ctx, "policy_microgrid")
```

## 储能定时任务匹配

**文件**: [s_policy_energy_storage_matcher_f.go](mdc:services/s_policy/internal/s_policy_energy_storage_matcher_f.go)

储能定时任务匹配逻辑：

1. **查询条件**: 查询所有 `status` 为 `"Enable"` 或 `"Enabled"` 的任务
2. **时间匹配**: 根据当前时间匹配 `DateRange` 和 `TimeRange`
3. **排序规则**: 按优先级（降序）→ 创建时间（降序）
4. **返回结果**: 返回第一个匹配的任务

**时间范围类型**:
- `"weekday"` - 工作日/周末匹配
- `"custom"` - 自定义日期和月份匹配
- `"monthly"` - 月度日期匹配

## 关键设计原则

### 1. 策略ID命名规范

- 使用字符串ID，格式：`"policy_[name]"`
- 示例：`"policy_microgrid"`, `"policy_ess"`

### 2. 配置为空处理

- 配置为空时，策略初始化成功但 `config` 设置为 `nil`
- `Run` 方法开头检查 `config == nil`，如果为空则跳过执行
- 记录日志但不报错

### 3. 线程安全

- 策略管理器使用读写锁（`sync.RWMutex`）保护并发访问
- 所有状态修改操作都加锁保护

### 4. 错误处理

- 初始化失败：返回错误，阻止策略启动
- 执行失败：记录错误日志，不影响后续执行
- 配置解析失败：返回错误，阻止策略启动

### 5. 定时执行

- 执行间隔：1 分钟
- 执行时机：每分钟的第 0 秒
- 错误处理：执行失败不影响定时器继续运行

## 相关文件

**核心接口**:
- [c_base_policy_i.go](mdc:common/c_base/c_base_policy_i.go) - 策略接口定义
- [c_base_policy_info_s.go](mdc:common/c_base/c_base_policy_info_s.go) - 策略信息结构体
- [c_policy_manager_si.go](mdc:common/c_policy_manager_si.go) - 策略管理器接口

**实现文件**:
- [s_policy_manager_impl_s.go](mdc:services/s_policy/internal/s_policy_manager_impl_s.go) - 策略管理器实现
- [s_policy_energy_storage_matcher_f.go](mdc:services/s_policy/internal/s_policy_energy_storage_matcher_f.go) - 储能任务匹配
- [s_policy_export_f.go](mdc:services/s_policy/s_policy_export_f.go) - 导出函数

**策略插件**:
- [p_policy_mircogrid](mdc:plugins/plug_policy/p_policy_mircogrid/) - 微电网策略
- [p_policy_energy_storage](mdc:plugins/plug_policy/p_policy_energy_storage/) - 储能站策略

**系统集成**:
- [ems.go](mdc:application/internal/cmd/ems.go) - 系统启动和策略管理器集成

## 开发检查清单

创建新策略时，确保完成以下步骤：

- [ ] 在 `plugins/plug_policy/p_policy_[name]/` 目录下创建策略插件
- [ ] 定义策略配置结构体（`internal/p_policy_[name]_config_s.go`）
- [ ] 实现 `IPolicy` 接口（`internal/p_policy_[name]_main_s_f.go`）
- [ ] 创建导出函数（`export_policy_[name]_f.go`）
- [ ] 在 `ems.go` 中注册策略
- [ ] 实现 `Init` 方法：从 setting 表加载配置
- [ ] 实现 `Run` 方法：检查配置是否为空，执行业务逻辑
- [ ] 实现 `Shutdown` 方法：清理资源
- [ ] 实现所有 getter 方法：`GetPolicyId`, `GetPolicyName`, `GetConfig`, `GetPolicyInfo`
- [ ] 配置为空时不执行业务逻辑，只记录日志
- [ ] 测试策略注册、初始化、执行和关闭流程

## 注意事项

1. **不创建默认配置**: 配置为空时不使用默认配置，直接将 `config` 设置为 `nil`
2. **配置ID即策略ID**: 策略配置的 setting ID 就是策略ID本身（如 `"policy_microgrid"`）
3. **线程安全**: 策略实例中的字段访问要考虑线程安全
4. **资源清理**: `Shutdown` 方法必须清理所有资源，避免内存泄漏
5. **错误日志**: 使用 `c_log` 记录错误和关键信息，便于调试和监控
6. **定时执行**: 策略的 `Run` 方法应该在 1 分钟内完成，避免阻塞定时器
