---
alwaysApply: false
description: 项目整体结构和开发规范 - 定义EMS项目的整体架构、目录结构、开发规范、命名约定、技术栈等。适用于项目结构理解、新模块开发、代码组织等场景。
globs: **/*.go,**/*.md,**/*.yaml,**/*.sh
---

# EMS-Plan 项目结构指南

## 项目架构

这是一个基于 Go 的能源管理系统 (EMS)，采用微服务架构和插件化设计。

### 核心目录结构

```text
ems-plan/
├── common/                    # 共享库 - 所有模块间共享的接口和类型
├── services/                  # 服务层 - 核心业务服务
│   ├── s_db/                 # 数据库服务
│   ├── s_driver/             # 驱动服务  
│   └── s_storage/            # 存储服务
├── plugins/                  # 插件系统
│   ├── plug_drivers/         # 设备驱动插件
│   ├── plug_protocols/       # 协议插件
│   ├── plug_storages/        # 存储插件
│   └── plug_policy/          # 策略插件
├── application/              # 主应用程序
└── @cpp/                     # C++ 扩展库
```

## 开发规范

### 1. 命名约定

**详细命名规范请参考**: [common-library.mdc](mdc:.cursor/rules/common-library.mdc)

#### 核心命名原则

- **接口**: `I*` 前缀 (如 `IDevice`, `IDriver`)
- **结构体**: `S*` 前缀 (如 `SDeviceConfig`, `SDriverInfo`)  
- **枚举**: `E*` 前缀 (如 `EDeviceType`, `EProtocolType`)
- **常量**: `C*` 前缀 (如 `CDeviceTypeAmmeter`)

#### 文件命名规范

- `c_*_i.go`: 接口定义 (Interface) - common库使用
- `c_*_s.go`: 结构体定义 (Struct) - common库使用
- `c_*_e.go`: 枚举定义 (Enum) - common库使用
- `c_*_f.go`: 函数定义 (Function) - common库使用
- `c_*_c.go`: 常量定义 (Constant) - common库使用
- `*_test.go`: 测试文件
- `模块名_*_e.go`: 模块内枚举定义 - 非common库使用

### 2. 服务开发流程

**详细服务开发规范请参考**:
- [services-architecture.mdc](mdc:.cursor/rules/services-architecture.mdc) - 服务架构指南
- [s-db-development.mdc](mdc:services/s_db/.cursor/rules/s-db-development.mdc) - 数据库服务开发规则

#### 核心开发流程

1. **服务层开发**: 在 `services/` 目录下创建独立模块
2. **插件开发**: 在 `plugins/` 目录下实现插件功能
3. **应用层开发**: 在 `application/` 目录下实现 Web 接口

### 3. 技术栈

- **GoFrame v2**: Web 框架和 ORM
- **SQLite**: 轻量级数据库
- **Go Modules**: 依赖管理
- **CGO**: C++ 扩展支持
- **Context**: 上下文传递和生命周期管理

### 4. 代码规范

- 使用 `context.Context` 进行上下文传递
- 统一错误处理使用 `github.com/pkg/errors`
- 日志记录使用 `c_log` 包进行统一日志管理
- 单例模式用于服务管理
- 接口分离原则
- 数据库操作使用 `s_db_model` 中的字段常量

### 5. 启动方式

使用项目的 `run_example.sh` 脚本启动示例应用程序。

## 关键文件

- [common/c_base/](mdc:common/c_base/) - 基础接口和类型定义
- [services/s_db/](mdc:services/s_db/) - 数据库服务
- [application/main.go](mdc:application/main.go) - 主程序入口
- [@cpp/run_example.sh](mdc:@cpp/run_example.sh) - 示例启动脚本
