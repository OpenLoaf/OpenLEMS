---
description: 驱动描述文件 build.yaml 生成规范
globs: **/build.yaml
---

# 驱动描述文件 build.yaml 生成规范

## 概述

驱动描述文件 `build.yaml` 通过 `BuildDescriptionFromYaml` 方法生成 `SDriverInfo` 和 `SConfigStructFields` 列表，用于描述驱动的元数据和前端展示方式。

## 文件结构

### 基础信息字段

```yaml
brand: "品牌名称"           # 设备品牌
model: "型号名称"          # 设备型号  
type: "设备类型"           # 设备类型：bms、pcs、ess、gpio等
version: "版本号"          # 驱动版本，如 "v1.0.0"
author: "作者邮箱"         # 作者信息
create: "创建日期"         # 创建日期，格式 "YYYY-MM-DD"
Desc: "备注信息"           # 驱动描述
protocolType: "协议类型"   # 协议类型：modbus、canbus、gpiod、virtual等
```

### 遥测信息 (telemetry)

遥测信息通过反射机制调用 `Get{Telemetry.name}` 方法获取数据。

```yaml
telemetry:
  - key: "遥测名称"                    # 遥测字段名，对应Get方法名
    name: "显示名称"                   # 前端显示名称
    unit: "单位"                       # 数据单位，如 "kW", "kWh", "V", "A", "%"
    valueExplain:                      # 状态映射（用于label类型）
      "0": "未知|#FF5D5D"
      "1": "关机|#9CBF30"
      "2": "待机|#6967EE"
    Desc: "备注信息"                   # 字段说明
```

**数据映射规则：**

1. **无 valueExplain**：前端直接显示原始数据值，适用于数值类型数据
2. **有 valueExplain**：前端使用 label 组件显示，根据 `valueExplain` 进行数据映射

**状态映射 (valueExplain) 规则：**

- **格式**：`"原始值": "显示名称|#颜色代码"`
- **原始值**：设备返回的实际数据值（字符串格式）
- **显示名称**：前端展示的文本内容
- **颜色代码**：十六进制颜色值

**常用颜色代码：**

- `#FF5D5D` - 红色（故障、错误状态）
- `#9CBF30` - 绿色（正常、关机状态）
- `#6967EE` - 蓝色（待机状态）
- `#29A634` - 深绿色（运行、充电状态）
- `#0098FA` - 亮蓝色（工作、放电状态）

### 自定义服务 (service)

自定义服务通过反射执行对应的方法。

```yaml
service:
  - key: "服务方法名"                  # 对应驱动中的方法名
    name: "显示名称"                   # 前端显示名称，支持i18n
    description: "服务描述"            # 服务功能描述
    icon: "power"                     # 图标名称，使用 Lucide 图标库
```

**服务命名规范：**

- 推荐使用 `Custom` 前缀，如 `CustomShutdownService`
- 方法名应具有语义性，如 `SetHigh`、`SetLow`、`Start`、`Stop`
- 支持国际化，使用 `g18n:key` 格式

**图标设置规范：**

- 使用 [Lucide 图标库](https://lucide.dev/icons/) 中的图标名称
- 图标名称应与设备类型和服务功能相关
- 不同设备类型的相同服务应使用不同的图标

**常用图标示例：**

- **电源控制**: `power`、`power-off`、`power-on`
- **开关控制**: `toggle-left`、`toggle-right`、`switch`
- **灯光控制**: `lightbulb`、`lightbulb-off`、`sun`、`moon`
- **温度控制**: `thermometer`、`snowflake`、`flame`
- **风扇控制**: `fan`、`wind`
- **门锁控制**: `lock`、`unlock`、`key`
- **窗帘控制**: `blinds`、`window`
- **通用控制**: `play`、`pause`、`stop`、`refresh`、`settings`

## 反射机制

### 遥测数据获取

系统调用设备实例的 `Get{Telemetry.key}` 方法：

```go
// 示例：telemetry.key = "soc"
// 系统会调用 instance.GetSoc() 方法
functionName := fmt.Sprintf("Get%s", capitalizeFirstLetter(key))
method := instanceValue.MethodByName(functionName)
```

**方法要求：**

- 方法名格式：`Get + 首字母大写的遥测名称`
- 返回值：可以是单个值或 `(value, error)` 格式
- 方法必须是无参数的

### 自定义服务执行

系统调用设备实例的服务方法：

```go
// 示例：service.key = "CustomShutdownService"
// 系统会调用 instance.CustomShutdownService() 方法
method := reflect.ValueOf(instance).MethodByName(functionName)
```

**方法要求：**

- 方法名必须与 `service.key` 完全匹配
- 返回值：可以是 `error` 或 `(result, error)` 格式
- 方法必须是无参数的

## 配置结构体字段

通过 `BuildConfigStructFields` 方法解析设备配置结构体：

```go
type DeviceConfig struct {
    Name   string `json:"name" name:"设备名称" ct:"text" required:"true"`
    Port   int    `json:"port" name:"端口号" ct:"number" min:"1" max:"65535" default:"8080"`
    Enable bool   `json:"enable" name:"启用状态" ct:"switch" default:"true"`
}
```

**标签规范：**

- `json`: JSON序列化字段名（必需）
- `name`: 字段显示名称
- `ct`: 组件类型（必需）- `text`、`number`、`switch`、`singleSelect`、`multiSelect`
- `required`: 是否必填
- `min`/`max`: 数值范围限制
- `default`: 默认值
- `unit`: 单位信息

## 示例文件

### BMS 驱动示例

```yaml
brand: Pylon
model: TechUs108
type: bms
version: v1.0.0
author: "zz@hexems.com"
create: "2024-09-07"
Desc: "派能108kWh风冷电池MBMS"
protocolType: "modbus"
telemetry:
  # 状态类数据 - 使用 valueExplain 映射
  - key: bmsStatus
    name: "电池状态"
    valueExplain:
      "0": "未知|#FF5D5D"
      "1": "关机|#9CBF30"
      "2": "待机|#6967EE"
      "3": "充电中|#29A634"
      "4": "放电中|#0098FA"
      "5": "故障|#FF5D5D"
  # 数值类数据 - 无 valueExplain，直接显示数值
  - key: soc
    unit: "%"
    name: "SOC"
  - key: capacity
    unit: "kWh"
    name: "电池容量"
  - key: dcPower
    unit: "kW"
    name: "直流功率"
service:
  - key: CustomDcOffService
    name: "g18n:dc-off"
    description: "电池下电指令"
    icon: "power-off"
  - key: CustomDcOnService
    name: "g18n:dc-on"
    description: "电池上电指令"
    icon: "power"
```

### GPIO 驱动示例

```yaml
brand: Hex
model: Basic-GPIO
name: "基础GPIO驱动"
version: "v1.0.0"
author: "zz@hexems.com"
create: "2025-09-12"
protocolType: "gpiod"
Desc: "基础GPIO驱动，支持GPIO输入输出控制"
telemetry:
  # 布尔值状态 - 使用布尔值映射
  - key: gpioStatus
    name: "状态"
    valueExplain:
      "false": "关|#9CBF30"
      "true": "开|#0098FA"
  # 数值数据 - 无 valueExplain，直接显示
  - key: voltage
    unit: "V"
    name: "电压"
service:
  - key: SetLow
    name: "低电平"
    description: "设置为低电平"
    icon: "toggle-left"
  - key: SetHigh
    name: "高电平"
    description: "设置为高电平"
    icon: "toggle-right"
  - key: CustomToggleService
    name: "g18n:toggle"
    description: "切换状态"
    icon: "switch"
```

## 最佳实践

### 遥测设计

1. **数据类型选择**:
   - 状态类数据使用 `valueExplain` 映射
   - 数值类数据不设置 `valueExplain`，直接显示原始值
   - 布尔值状态使用 `valueExplain` 配合布尔值映射
2. **状态映射设计**:
   - 为所有可能的状态值提供完整的映射关系
   - 使用语义化的显示名称，避免技术术语
   - 颜色选择应符合用户直觉（绿色=正常，红色=故障等）

### 服务设计

1. **功能明确**: 每个服务应有明确的功能定义
2. **命名规范**: 推荐使用 `Custom` 前缀和语义化命名
3. **国际化支持**: 使用 `g18n:key` 格式支持多语言
4. **图标选择**: 根据设备类型和服务功能选择合适的图标

### 配置字段设计

1. **必需标签**: 所有字段必须包含 `json` 和 `ct` 标签
2. **验证规则**: 为数值字段设置合理的 `min`/`max` 范围
3. **默认值**: 为常用字段设置合理的默认值

## 注意事项

1. **反射依赖**: 遥测和服务方法必须严格按照命名规范实现
2. **valueExplain 使用**:
   - `valueExplain` 用于状态映射，前端会根据映射关系显示对应的标签
   - `valueExplain` 中的键值必须是字符串格式，即使原始数据是数字
   - 确保 `valueExplain` 覆盖所有可能的返回值，避免显示空白
   - 颜色代码必须是有效的十六进制格式
3. **图标使用**:
   - 图标名称必须使用 Lucide 图标库中存在的图标名称
   - 图标选择应考虑设备类型和功能语义，避免使用不相关的图标
   - 相同功能在不同设备类型中可以使用不同的图标来区分
   - 图标名称区分大小写，建议使用小写字母和连字符
4. **性能考虑**: 避免在遥测方法中执行耗时操作
5. **向后兼容**: 修改现有字段时考虑向后兼容性

## 统一点位管理系统支持

### 概述

统一点位管理系统通过结构体嵌套和类型判断实现统一管理，支持反射遥测点位、协议点位、配置点位的统一管理。系统不再依赖YAML文件中的telemetry配置，改为使用代码化的点位定义。

### 新的点位结构体层次

```text
SPoint (基础点位信息)
├── SReflectPoint (反射遥测点位)
├── SProtocolPoint (协议点位基础)
│   ├── SModbusPoint (Modbus协议点位)
│   ├── SCanbusPoint (CAN总线协议点位)
│   └── SDidioPoint (DIDO协议点位)
└── SConfigPoint (配置点位)
```

### 遥测点位代码化

#### 旧方式（YAML配置）

```yaml
# build.yaml
telemetry:
  - key: soc
    name: "SOC"
    unit: "%"
    valueExplain:
      "0": "未知|#FF5D5D"
      "1": "正常|#52c41a"
```

#### 新方式（代码化定义）

```go
// 包级别点位定义，优先使用预定义配置
var (
    // 遥测点位定义 - 直接创建，启动时验证SPoint字段
    telemetrySocPoint = &SReflectPoint{
        SPoint: &SPoint{
            Key:       "soc",
            Name:      "SOC",
            Unit:      "%",
            ValueType: c_enum.EFloat32,
            Desc:      "电池电量",
            Min:       0,
            Max:       100,
            ValueExplain: []*SFieldExplain{
                {
                    Key:       "0",
                    Value:     "未知",
                    Color:     "#FF5D5D",
                    FromParam: false,
                },
                {
                    Key:       "1",
                    Value:     "正常",
                    Color:     "#52c41a",
                    FromParam: false,
                },
            },
        },
        MethodName: "GetSoc",
    }
)
```

#### 完整驱动示例

```go
// bms_pylon_tech_us108_impl_s.go
package main

import (
    "c_base"
    "c_enum"
    "c_default"
)

// 包级别点位定义
var (
    // 遥测点位定义
    telemetrySocPoint = &c_base.SReflectPoint{
        SPoint: &c_base.SPoint{
            Key:       "soc",
            Name:      "SOC",
            Unit:      "%",
            ValueType: c_enum.EFloat32,
            Desc:      "电池电量",
            Min:       0,
            Max:       100,
        },
        MethodName: "GetSoc",
    }

    telemetryVoltagePoint = &c_base.SReflectPoint{
        SPoint: &c_base.SPoint{
            Key:       "voltage",
            Name:      "电压",
            Unit:      "V",
            ValueType: c_enum.EFloat32,
            Desc:      "电池电压",
            Min:       0,
            Max:       1000,
        },
        MethodName: "GetVoltage",
    }

    telemetryStatusPoint = &c_base.SReflectPoint{
        SPoint: &c_base.SPoint{
            Key:       "status",
            Name:      "状态",
            ValueType: c_enum.EInt32,
            Desc:      "电池状态",
            ValueExplain: []*c_base.SFieldExplain{
                {
                    Key:       "0",
                    Value:     "未知",
                    Color:     "#FF5D5D",
                    FromParam: false,
                },
                {
                    Key:       "1",
                    Value:     "关机",
                    Color:     "#9CBF30",
                    FromParam: false,
                },
                {
                    Key:       "2",
                    Value:     "待机",
                    Color:     "#6967EE",
                    FromParam: false,
                },
                {
                    Key:       "3",
                    Value:     "充电中",
                    Color:     "#29A634",
                    FromParam: false,
                },
                {
                    Key:       "4",
                    Value:     "放电中",
                    Color:     "#0098FA",
                    FromParam: false,
                },
            },
        },
        MethodName: "GetStatus",
    }

    // 协议点位定义
    protocolCurrentPoint = &c_proto.SModbusPoint{
        SProtocolPoint: &c_base.SProtocolPoint{
            SPoint:     c_default.VPointIa,        // 使用预定义A相电流点位
            DataAccess: c_default.VDataAccessFloat32, // 使用预定义浮点数数据访问配置
        },
        Addr: 0x1001,
    }

    protocolVoltagePoint = &c_proto.SModbusPoint{
        SProtocolPoint: &c_base.SProtocolPoint{
            SPoint:     c_default.VPointUa,         // 使用预定义A相电压点位
            DataAccess: c_default.VDataAccessFloat32, // 使用预定义浮点数数据访问配置
        },
        Addr: 0x1002,
    }
)

// 设备实现
type BmsPylonTechUs108Impl struct {
    // 设备字段
}

// 实现遥测方法
func (b *BmsPylonTechUs108Impl) GetSoc() (*float32, error) {
    // 遥测获取逻辑
    return b.getFromProtocolPoint(protocolCurrentPoint)
}

func (b *BmsPylonTechUs108Impl) GetVoltage() (*float32, error) {
    // 遥测获取逻辑
    return b.getFromProtocolPoint(protocolVoltagePoint)
}

func (b *BmsPylonTechUs108Impl) GetStatus() (*int32, error) {
    // 状态获取逻辑
    return b.getStatusFromProtocol()
}

// 实现IDevice接口方法
func (b *BmsPylonTechUs108Impl) GetPoints() []c_base.IPoint {
    return []c_base.IPoint{
        telemetrySocPoint,
        telemetryVoltagePoint,
        telemetryStatusPoint,
    }
}

func (b *BmsPylonTechUs108Impl) GetProtocolPoints() []c_base.IPoint {
    return []c_base.IPoint{
        protocolCurrentPoint,
        protocolVoltagePoint,
    }
}

func (b *BmsPylonTechUs108Impl) GetConfigPoints() []c_base.IPoint {
    // 从配置结构体转换而来
    return []c_base.IPoint{
        // 配置点位定义
    }
}
```

### 协议点位定义

#### 使用预定义配置

```go
// 协议点位定义 - 直接创建，启动时验证SPoint字段
var (
    protocolCurrentPoint = &SModbusPoint{
        SProtocolPoint: &SProtocolPoint{
            SPoint:     c_default.VPointIa,        // 使用预定义A相电流点位
            DataAccess: c_default.VDataAccessFloat32, // 使用预定义浮点数数据访问配置
        },
        Addr: 0x1001,
    }
)
```

#### 自定义协议点位

```go
// 自定义协议点位
var (
    customVoltagePoint = &SModbusPoint{
        SProtocolPoint: &SProtocolPoint{
            SPoint: &SPoint{
                Key:       "customVoltage",
                Name:      "自定义电压",
                Unit:      "V",
                ValueType: c_enum.EFloat32,
                Desc:      "自定义电压点位",
                Min:       0,
                Max:       1000,
            },
            DataAccess: &SDataAccess{
                DataFormat: c_enum.EDataFormatFloat32,
                ByteEndian: c_enum.EByteEndianBig,
                WordOrder:  c_enum.EWordOrderHighLow,
            },
        },
        Addr: 0x2001,
    }
)
```

### 配置点位定义

#### 从配置结构体转换

```go
// 配置结构体定义
type DeviceConfig struct {
    Name   string `code:"name" name:"设备名称" ct:"text" v:"required|length:2,50"`
    Port   int    `code:"port" name:"端口号" ct:"number" v:"required|between:1,65535"`
    Email  string `code:"email" name:"邮箱" ct:"text" v:"required|email"`
    Enable bool   `code:"enable" name:"启用状态" ct:"switch" v:"required"`
}

// 转换为配置点位
func (d *DeviceImpl) GetConfigPoints() []*SConfigPoint {
    return []*SConfigPoint{
        {
            SPoint: &SPoint{
                Key:       "name",
                Name:      "设备名称",
                ValueType: c_enum.EString,
                Desc:      "设备的显示名称",
            },
            Required: true,
        },
        {
            SPoint: &SPoint{
                Key:       "port",
                Name:      "端口号",
                ValueType: c_enum.EInt32,
                Unit:      "",
                Desc:      "设备通信端口",
                Min:       1,
                Max:       65535,
            },
            Required: true,
            Default:  stringPtr("502"),
        },
    }
}
```

### 驱动实现示例

#### 实现新的IDevice接口方法

```go
// 在驱动中实现IDevice接口的新方法
func (d *DeviceImpl) GetPoints() []*SReflectPoint {
    return []*SReflectPoint{
        telemetrySocPoint,
        telemetryVoltagePoint,
    }
}

func (d *DeviceImpl) GetProtocolPoints() []*SProtocolPoint {
    return []*SProtocolPoint{
        protocolCurrentPoint.SProtocolPoint,
        protocolVoltagePoint.SProtocolPoint,
    }
}

func (d *DeviceImpl) GetConfigPoints() []*SConfigPoint {
    return []*SConfigPoint{
        // 配置点位定义
    }
}
```

### 预定义配置使用规范

#### 优先使用预定义配置

在创建点位时，应优先使用 `c_default` 包中的预定义配置：

```go
// ✅ 推荐 - 使用预定义配置
protocolCurrentPoint = &SModbusPoint{
    SProtocolPoint: &SProtocolPoint{
        SPoint:     c_default.VPointIa,        // 使用预定义A相电流点位
        DataAccess: c_default.VDataAccessFloat32, // 使用预定义浮点数数据访问配置
    },
    Addr: 0x1001,
}

// ❌ 不推荐 - 手动创建配置
protocolCurrentPoint = &SModbusPoint{
    SProtocolPoint: &SProtocolPoint{
        SPoint: &SPoint{
            Key:       "current",
            Name:      "电流",
            Unit:      "A",
            ValueType: c_enum.EFloat32,
            // ... 手动定义所有字段
        },
        DataAccess: &SDataAccess{
            DataFormat: c_enum.EDataFormatFloat32,
            // ... 手动定义所有字段
        },
    },
    Addr: 0x1001,
}
```

#### 预定义配置优势

1. **标准化**：使用经过验证的标准配置
2. **一致性**：确保相同类型点位使用相同配置
3. **维护性**：集中管理，便于更新和维护
4. **告警功能**：预定义点位已包含告警触发逻辑

### 启动时验证

#### 验证机制

系统启动时会验证所有点位的SPoint字段是否设置：

```go
// 在系统启动时调用
func ValidateAllPoints(deviceManager IDeviceManager) error {
    // 遍历所有设备
    // 验证遥测点位、协议点位、配置点位的SPoint字段是否设置
    // 如果未设置，返回错误信息
}
```

#### 验证要求

1. **SPoint字段必须设置**：所有点位的SPoint字段都必须在创建时设置
2. **不可变设计**：点位创建后不允许修改，确保数据一致性
3. **类型安全**：通过类型判断确定获取方式，避免配置错误

### 迁移指南

#### 从YAML配置迁移到代码化定义

1. **分析现有YAML配置**：
   - 识别telemetry配置中的遥测点位
   - 分析现有的协议点位定义
   - 理解配置结构体的字段定义

2. **创建代码化点位定义**：
   - 将YAML中的telemetry转换为SReflectPoint定义
   - 将现有的协议点位更新为SProtocolPoint结构
   - 将配置结构体转换为SConfigPoint定义

3. **实现新接口方法**：
   - 实现GetPoints()方法
   - 实现GetProtocolPoints()方法
   - 实现GetConfigPoints()方法

4. **验证和测试**：
   - 运行启动时验证，确保所有SPoint字段都已设置
   - 测试遥测数据获取功能
   - 测试协议通信功能
   - 测试配置功能

### 最佳实践

#### 1. 点位定义规范

```go
// ✅ 正确 - 完整的点位定义
var (
    telemetrySocPoint = &SReflectPoint{
        SPoint: &SPoint{
            Key:       "soc",
            Name:      "SOC",
            Unit:      "%",
            ValueType: c_enum.EFloat32,
            Desc:      "电池电量",
            Min:       0,
            Max:       100,
        },
        MethodName: "GetSoc",
    }
)

// ❌ 错误 - SPoint字段未设置
var (
    telemetrySocPoint = &SReflectPoint{
        MethodName: "GetSoc",
        // SPoint字段未设置，启动时验证会失败
    }
)
```

#### 2. 预定义配置使用

```go
// ✅ 推荐 - 使用预定义配置
protocolCurrentPoint = &SModbusPoint{
    SProtocolPoint: &SProtocolPoint{
        SPoint:     c_default.VPointIa,        // 使用预定义配置
        DataAccess: c_default.VDataAccessFloat32, // 使用预定义配置
    },
    Addr: 0x1001,
}
```

#### 3. 接口方法实现

```go
// ✅ 正确 - 实现所有必需的接口方法
func (d *DeviceImpl) GetPoints() []*SReflectPoint {
    return []*SReflectPoint{
        telemetrySocPoint,
        telemetryVoltagePoint,
    }
}

func (d *DeviceImpl) GetProtocolPoints() []*SProtocolPoint {
    return []*SProtocolPoint{
        protocolCurrentPoint.SProtocolPoint,
    }
}

func (d *DeviceImpl) GetConfigPoints() []*SConfigPoint {
    return []*SConfigPoint{
        // 配置点位定义
    }
}
```

### 注意事项

1. **不向前兼容**：完全移除v1兼容性，直接使用新的统一结构体
2. **启动时验证**：在系统启动时验证所有*SPoint字段是否设置，确保数据完整性
3. **不可变设计**：所有结构体数据在创建后不允许修改，确保数据一致性
4. **预定义配置优先**：优先使用c_default包中的预定义配置，避免手动创建
5. **类型安全**：通过类型判断确定获取方式，避免配置错误

## 相关文件

- [c_base_driver_info_s.go](mdc:common/c_base/c_base_driver_info_s.go) - 驱动信息结构体定义
- [c_base_driver_info_f.go](mdc:common/c_base/c_base_driver_info_f.go) - 驱动信息处理函数
- [c_base_config_struct_fields_s.go](mdc:common/c_base/c_base_config_struct_fields_s.go) - 配置字段结构体
- [c_base_config_struct_fields_f.go](mdc:common/c_base/c_base_config_struct_fields_f.go) - 配置字段处理函数
- [c_base_reflect_point_s.go](mdc:common/c_base/c_base_reflect_point_s.go) - 反射遥测点位结构体
- [c_base_protocol_point_s.go](mdc:common/c_base/c_base_protocol_point_s.go) - 协议点位基础结构体
- [c_base_config_point_s.go](mdc:common/c_base/c_base_config_point_s.go) - 配置点位结构体
- [c_base_point_validation_f.go](mdc:common/c_base/c_base_point_validation_f.go) - 启动时验证机制
