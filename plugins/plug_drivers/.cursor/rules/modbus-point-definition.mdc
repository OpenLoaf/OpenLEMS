---
description: Modbus点位定义规范和最佳实践
globs: **/*point*.go,**/*modbus*.go,**/*task*.go
---

# Modbus 点位定义规范

## 基本结构

所有 Modbus 点位必须使用 `c_proto.SModbusPoint` 结构，字段顺序：
1. `Addr`: Modbus 地址 (必须放在第一位)
2. `SPoint`: 点位基本信息
3. `DataAccess`: 数据访问配置
4. `StatusExplain`: 状态解释函数 (可选)
5. `Trigger`: 告警触发函数 (可选)

参考: `common/c_proto/c_modbus_point_s.go`

## 预定义配置使用规范

### 数据访问配置 (DataAccess)
**必须优先使用 `c_default.VDataAccess*` 预定义配置，避免手动创建 `&c_base.SDataAccess{}`**

参考: `common/c_default/c_default_data_access_v.go`

**匹配规则：**
- `DataFormat: c_enum.DataFormatUInt16` + `ValueType: c_enum.EUint16` → `c_default.VDataAccessUInt16`
- `DataFormat: c_enum.DataFormatInt16` + `ValueType: c_enum.EInt16` → `c_default.VDataAccessInt16`
- `DataFormat: c_enum.DataFormatUInt32` + `ValueType: c_enum.EUint32` → `c_default.VDataAccessUInt32`
- `DataFormat: c_enum.DataFormatInt32` + `ValueType: c_enum.EInt32` → `c_default.VDataAccessInt32`
- `DataFormat: c_enum.DataFormatFloat32` + `ValueType: c_enum.EFloat32` → `c_default.VDataAccessFloat32`
- `DataFormat: c_enum.DataFormatFloat64` + `ValueType: c_enum.EFloat64` → `c_default.VDataAccessFloat64`
- `Factor: 0.1` + UInt16 → `c_default.VDataAccessUInt16Scale01`
- `Factor: 0.01` + UInt16 → `c_default.VDataAccessUInt16Scale001`
- `Factor: 0.001` + UInt16 → `c_default.VDataAccessUInt16Scale0001`
- `DataFormat: c_enum.DataFormatUInt16` + `ValueType: c_enum.EBool` → `c_default.VDataAccessUInt16ToBool`
- `DataFormat: c_enum.DataFormatBitRange` + `ValueType: c_enum.EBool` → `c_default.VDataAccessBitRangeBool`
- `DataFormat: c_enum.DataFormatBitRange` + `ValueType: c_enum.EUint16` → `c_default.VDataAccessBitRangeUInt16`

### 点位信息 (SPoint)
**必须优先使用 `c_default.VPoint*` 预定义配置，避免手动创建 `&c_base.SPoint{}`**

参考: `common/c_default/c_default_point_v.go`

**匹配规则：**
- `Name: "A相电流"` 或 `Unit: "A"` + `Key: "Ia"` → `c_default.VPointIa`
- `Name: "B相电流"` 或 `Unit: "A"` + `Key: "Ib"` → `c_default.VPointIb`
- `Name: "C相电流"` 或 `Unit: "A"` + `Key: "Ic"` → `c_default.VPointIc`
- `Name: "平均电流"` 或 `Unit: "A"` + `Key: "Iavg"` → `c_default.VPointIavg`
- `Name: "最大电流"` 或 `Unit: "A"` + `Key: "Imax"` → `c_default.VPointImax`
- `Name: "A相电压"` 或 `Unit: "V"` + `Key: "Ua"` → `c_default.VPointUa`
- `Name: "B相电压"` 或 `Unit: "V"` + `Key: "Ub"` → `c_default.VPointUb`
- `Name: "C相电压"` 或 `Unit: "V"` + `Key: "Uc"` → `c_default.VPointUc`
- `Name: "平均电压"` 或 `Unit: "V"` + `Key: "Uavg"` → `c_default.VPointUavg`
- `Name: "最大电压"` 或 `Unit: "V"` + `Key: "Umax"` → `c_default.VPointUmax`
- `Name: "AB线电压"` 或 `Unit: "V"` + `Key: "Uab"` → `c_default.VPointUab`
- `Name: "BC线电压"` 或 `Unit: "V"` + `Key: "Ubc"` → `c_default.VPointUbc`
- `Name: "CA线电压"` 或 `Unit: "V"` + `Key: "Uca"` → `c_default.VPointUca`
- `Name: "电池电量"` 或 `Unit: "%"` + `Key: "SOC"` → `c_default.VPointSOC`
- `Name: "电池健康度"` 或 `Unit: "%"` + `Key: "SOH"` → `c_default.VPointSOH`
- `Name: "电池电压"` 或 `Unit: "V"` + `Key: "Voltage"` → `c_default.VPointDcVoltage`
- `Name: "电池电流"` 或 `Unit: "A"` + `Key: "Current"` → `c_default.VPointDcCurrent`
- `Name: "电池直流功率"` 或 `Unit: "kW"` + `Key: "Power"` → `c_default.VPointDcPower`
- `Name: "温度"` 且无 Max/Min → `c_default.VPointTemp`
- `Name: "最高温度"` 或 `Name: "温度"` + `Max` → `c_default.VPointTempMax`
- `Name: "最低温度"` 或 `Name: "温度"` + `Min` → `c_default.VPointTempMin`
- `Name: "湿度"` 或 `Unit: "%RH"` → `c_default.VPointHumidity`
- `Name: "频率"` 或 `Unit: "Hz"` → `c_default.VPointFreq`
- `Name: "状态"` 或 `Key: "Status"` → `c_default.VPointStatus`
- `Name: "错误代码"` 或 `Name: "故障"` + `Key: "ErrorCode"` → `c_default.VPointErrorCode`

### 自定义配置简化
**位范围配置:**
```go
DataAccess: &c_base.SDataAccess{
    BitIndex: 0, BitLength: 2,  // 位参数放在前面
    DataFormat: c_enum.DataFormatBitRange,
    ValueType: c_enum.EUint16,
}
```

**避免冗余默认值:**
- 不要定义 `Factor: 1.0` 和 `Offset: 0` (这些是默认值)
- 不要定义 `ByteEndian: c_enum.ByteEndianBig` 和 `WordOrder: c_enum.WordOrderHighLow` (这些是默认值)

## 函数使用规范

### StatusExplain 函数
**函数签名：** `func(value any) (string, error)`

参考: `common/c_default/c_default_status_explain.go`

**预定义函数：**
- `c_default.StatusExplainProtectFunc`
- `c_default.StatusExplainEnableFunc`
- `c_default.StatusExplainIsNotFunc`
- `c_default.StatusExplainErrorFunc`

### Trigger 函数
**函数签名：** `func(value interface{}) (trigger bool, level c_enum.EAlarmLevel, err error)`

参考: `common/c_default/c_default_alarm_trigger_f.go`

**预定义函数：**
- `c_default.FAlarmTriggerWarnBool` - 布尔值警告触发
- `c_default.FAlarmTriggerAlertBool` - 布尔值警报触发
- `c_default.FAlarmTriggerErrorBool` - 布尔值故障触发
- `c_default.FAlarmTriggerWarnNotZero` - 非零值警告触发
- `c_default.FAlarmTriggerAlertNotZero` - 非零值警报触发
- `c_default.FAlarmTriggerErrorNotZero` - 非零值故障触发
- `c_default.FAlarmTriggerRangeThan` - 范围告警触发

## 告警范围配置

### SAlarmRangeTrigger 结构
参考: `common/c_base/c_base_alarm_range_s.go`

**告警判断逻辑：**
- 从错误级别开始判断（最高优先级）
- 如果值 < Before 或 > After，则触发对应级别的告警
- 告警级别优先级：错误 > 警报 > 警告

## 代码格式规范

### 1. 单行定义
除非包含方法，否则变量定义应该在一行内

### 2. 多行定义 (包含方法时)
按字段顺序排列，方法单独占行

## 命名规范

- 变量名使用 PascalCase
- 地址使用十六进制格式 (0x1100)
- 描述信息保持与原始代码一致
- 不要自我发挥，保持原有含义

## 唯一性约束

### Point Key 唯一性
在同一个驱动文件中，所有点位的 `Key` 字段必须唯一，不能重复。

**检查方法:**
```bash
grep -r "Key:" plugins/plug_drivers/your_driver/ | grep -o 'Key: "[^"]*"' | sort | uniq -d
```

## Modbus Task 定义规范

### 基本结构
Modbus Task 使用 `c_proto.SModbusPointTask` 结构，用于组织和管理多个相关的 Modbus 点位。

参考: `common/c_proto/c_modbus_point_s.go`

### 任务分组建议

#### 1. 心跳类任务 (GroupHeart)
- 循环间隔: 1000ms
- 缓存时间: 默认

#### 2. 基本信息任务 (GroupInfo)
- 循环间隔: 1000ms
- 缓存时间: 默认

#### 3. 时间类任务 (GroupTime)
- 循环间隔: 1000ms 或 0 (按需读取)
- 缓存时间: 默认

#### 4. 统计类任务 (GroupStatistics)
- 循环间隔: 1000ms 或更长间隔
- 缓存时间: 30 * time.Second

### 任务设计原则

1. **地址连续性**: 同一任务中的点位地址应该连续
2. **功能相关性**: 将功能相关的点位组织在同一个任务中
3. **读取频率**: 根据数据更新频率设置合适的 CycleMill
4. **缓存策略**: 根据数据重要性设置合适的 Lifetime
5. **任务大小**: 避免单个任务包含过多点位，影响读取效率

### 任务命名规范

- 任务名称使用 `Group` 前缀 + 功能描述
- 常见命名模式：
  - `GroupHeart` - 心跳类
  - `GroupInfo` - 基本信息类
  - `GroupTime` - 时间类
  - `GroupStatistics` - 统计类
  - `GroupAlarm` - 告警类
  - `GroupControl` - 控制类

## 重构检查清单

1. ✅ 地址字段放在第一位
2. ✅ 根据匹配规则使用 c_default 预定义配置
3. ✅ 避免手动创建 `&c_base.SDataAccess{}` 和 `&c_base.SPoint{}`
4. ✅ 移除冗余的默认值配置 (Factor: 1.0, Offset: 0 等)
5. ✅ 使用预定义函数 (StatusExplain, Trigger)
6. ✅ 保持原有代码含义不变
7. ✅ 单行定义 (除非包含方法)
8. ✅ 正确的字段顺序
9. ✅ 确保 Point Key 在驱动中唯一
10. ✅ Task 中点位地址连续
11. ✅ Task 按功能合理分组
12. ✅ 设置合适的 CycleMill 和 Lifetime
13. ✅ 使用正确的枚举类型 (EDataFormat, EValueType, EAlarmLevel 等)
14. ✅ 触发函数返回正确的告警级别
15. ✅ 状态解释函数返回正确的字符串格式
16. ✅ 位范围配置使用 BitIndex 和 BitLength 字段
17. ✅ 自定义配置时避免冗余的默认值
18. ✅ 告警范围配置使用 SAlarmRangeTrigger 结构
19. ✅ 告警判断从错误级别开始，按优先级顺序检查

## SProtocolPoint 继承规范

### 概述

SProtocolPoint 是统一点位管理系统中的协议点位基础结构体，SModbusPoint 继承自 SProtocolPoint，实现了协议点位的统一管理。

### 新的继承结构

```text
SPoint (基础点位信息)
└── SProtocolPoint (协议点位基础)
    └── SModbusPoint (Modbus协议点位)
```

### SProtocolPoint 结构体定义

```go
// SProtocolPoint 协议点位基础结构
type SProtocolPoint struct {
    *SPoint     // 嵌套基础点位信息
    DataAccess *SDataAccess `json:"dataAccess" v:"required" dc:"数据访问配置"` // 数据访问配置
}

// SModbusPoint 调整后的结构
type SModbusPoint struct {
    *SProtocolPoint  // 替换原来的 *SPoint
    Addr uint16      // Modbus地址

    // 功能函数（不可变）
    StatusExplain func(value any) (text string, err error)                                    `json:"-" dc:"状态解释函数"`
    Trigger       func(value interface{}) (trigger bool, level c_enum.EAlarmLevel, err error) `json:"-" dc:"告警触发函数"`
}
```

### 新的点位定义方式

#### 旧方式（直接继承SPoint）

```go
// ❌ 旧方式 - 直接继承SPoint
var (
    PhaseAVoltage = &c_proto.SModbusPoint{
        Addr: 0x0191, 
        SPoint: &c_base.SPoint{
            Key: "PhaseAVoltage", 
            Name: "A相电压", 
            Unit: "V", 
            Desc: "A相电压"
        }, 
        DataAccess: c_default.VDataAccessInt16
    }
)
```

#### 新方式（继承SProtocolPoint）

```go
// ✅ 新方式 - 继承SProtocolPoint
var (
    PhaseAVoltage = &SModbusPoint{
        SProtocolPoint: &SProtocolPoint{
            SPoint: &SPoint{
                Key:       "PhaseAVoltage",
                Name:      "A相电压",
                Unit:      "V",
                Desc:      "A相电压",
                ValueType: c_enum.EFloat32,
            },
            DataAccess: c_default.VDataAccessInt16, // 复用现有的数据访问配置
        },
        Addr: 0x0191, // 复用现有的地址配置
    }
)
```

### 使用预定义配置的新方式

#### 优先使用预定义配置

```go
// ✅ 推荐 - 使用预定义配置
var (
    protocolCurrentPoint = &SModbusPoint{
        SProtocolPoint: &SProtocolPoint{
            SPoint:     c_default.VPointIa,        // 使用预定义A相电流点位
            DataAccess: c_default.VDataAccessFloat32, // 使用预定义浮点数数据访问配置
        },
        Addr: 0x1001,
    }

    protocolVoltagePoint = &SModbusPoint{
        SProtocolPoint: &SProtocolPoint{
            SPoint:     c_default.VPointUa,         // 使用预定义A相电压点位（包含告警触发）
            DataAccess: c_default.VDataAccessFloat32, // 使用预定义浮点数数据访问配置
        },
        Addr: 0x1002,
    }
)
```

#### 自定义协议点位

```go
// 自定义协议点位
var (
    customVoltagePoint = &SModbusPoint{
        SProtocolPoint: &SProtocolPoint{
            SPoint: &SPoint{
                Key:       "customVoltage",
                Name:      "自定义电压",
                Unit:      "V",
                ValueType: c_enum.EFloat32,
                Desc:      "自定义电压点位",
                Min:       0,
                Max:       1000,
            },
            DataAccess: &SDataAccess{
                DataFormat: c_enum.EDataFormatFloat32,
                ByteEndian: c_enum.EByteEndianBig,
                WordOrder:  c_enum.EWordOrderHighLow,
            },
        },
        Addr: 0x2001,
    }
)
```

### 方法实现调整

#### 保持现有方法不变

```go
// 保持现有方法不变
func (s *SModbusPoint) GetDataAccess() *c_base.SDataAccess {
    return s.SProtocolPoint.DataAccess
}

func (s *SModbusPoint) String() string {
    return fmt.Sprintf("%s[0x%x]", s.GetName(), s.Addr)
}

// 注意：不需要重复实现IPoint接口方法
// 通过结构体嵌套自动继承SProtocolPoint的方法实现
// 只有在需要覆盖方法时才重新实现
// SProtocolPoint字段将在启动时验证是否设置
```

### 启动时验证

#### 验证要求

1. **SPoint字段必须设置**：所有SModbusPoint的SProtocolPoint.SPoint字段都必须在创建时设置
2. **不可变设计**：点位创建后不允许修改，确保数据一致性
3. **类型安全**：通过类型判断确定获取方式，避免配置错误

#### 验证机制

```go
// 在系统启动时验证所有点位的SPoint字段是否设置
func ValidateAllPoints(deviceManager IDeviceManager) error {
    // 遍历所有设备
    // 验证协议点位的SPoint字段是否设置
    // 如果未设置，返回错误信息
}
```

### 迁移指南

#### 从旧结构迁移到新结构

1. **分析现有点位定义**：
   - 识别现有的SModbusPoint定义
   - 分析现有的SPoint嵌套结构
   - 理解现有的DataAccess配置

2. **更新点位定义**：
   - 将`*SPoint`嵌套更新为`*SProtocolPoint`嵌套
   - 保持现有的地址配置、数据访问配置等业务参数不变
   - 复用现有的预定义配置（如`c_default.VDataAccessFloat32`）

3. **验证修改结果**：
   - 确保每个驱动修改后相关模块能正常编译
   - 验证现有的业务功能不受影响
   - 测试协议通信功能正常
   - 运行启动时验证，确保所有*SPoint字段都已设置

### 最佳实践

#### 1. 点位定义规范

```go
// ✅ 正确 - 使用完整的协议点位定义
var (
    protocolCurrentPoint = &SModbusPoint{
        SProtocolPoint: &SProtocolPoint{
            SPoint:     c_default.VPointIa,        // 使用预定义配置
            DataAccess: c_default.VDataAccessFloat32, // 使用预定义配置
        },
        Addr: 0x1001,
    }
)

// ❌ 错误 - SProtocolPoint字段未设置
var (
    protocolCurrentPoint = &SModbusPoint{
        Addr: 0x1001,
        // SProtocolPoint字段未设置，启动时验证会失败
    }
)
```

#### 2. 预定义配置使用

```go
// ✅ 推荐 - 使用预定义配置
protocolCurrentPoint = &SModbusPoint{
    SProtocolPoint: &SProtocolPoint{
        SPoint:     c_default.VPointIa,        // 使用预定义配置
        DataAccess: c_default.VDataAccessFloat32, // 使用预定义配置
    },
    Addr: 0x1001,
}
```

#### 3. 业务逻辑复用

```go
// 复用现有的遥测获取方法
func (s *sAmmeterDemo) GetUa() (*float32, error) {
    return s.GetFromPointFloat32(PhaseAVoltage) // 复用现有的业务逻辑
}

// 复用现有的协议点位
func (s *sAmmeterDemo) GetProtocolPoints() []*SProtocolPoint {
    return []*SProtocolPoint{
        PhaseAVoltage.SProtocolPoint, // 复用现有的协议点位
        PhaseACurrent.SProtocolPoint,
        // ... 其他协议点位
    }
}
```

### 预定义配置使用规范

#### 优先使用预定义配置

在创建SModbusPoint时，应优先使用 `c_default` 包中的预定义配置，避免手动创建配置。

**预定义配置优势：**

1. **标准化**：使用经过验证的标准配置
2. **一致性**：确保相同类型点位使用相同配置
3. **维护性**：集中管理，便于更新和维护
4. **告警功能**：预定义点位已包含告警触发逻辑

#### 预定义配置类型

- **点位配置**：`VPointIa`、`VPointUa`、`VPointSoc` 等电力系统常用点位
- **数据访问配置**：`VDataAccessFloat32`、`VDataAccessUInt16` 等Modbus常用配置
- **点位分组**：`VPointGroupSystemBasic` 等预定义分组
- **告警触发**：预定义点位已包含告警触发函数

#### 使用预定义配置示例

```go
// ✅ 推荐 - 使用预定义配置
var (
    // 电流点位 - 使用预定义A相电流点位
    protocolCurrentPoint = &SModbusPoint{
        SProtocolPoint: &SProtocolPoint{
            SPoint:     c_default.VPointIa,        // 使用预定义A相电流点位
            DataAccess: c_default.VDataAccessFloat32, // 使用预定义浮点数数据访问配置
        },
        Addr: 0x1001,
    }

    // 电压点位 - 使用预定义A相电压点位（包含告警触发）
    protocolVoltagePoint = &SModbusPoint{
        SProtocolPoint: &SProtocolPoint{
            SPoint:     c_default.VPointUa,         // 使用预定义A相电压点位
            DataAccess: c_default.VDataAccessFloat32, // 使用预定义浮点数数据访问配置
        },
        Addr: 0x1002,
    }

    // SOC点位 - 使用预定义SOC点位
    protocolSocPoint = &SModbusPoint{
        SProtocolPoint: &SProtocolPoint{
            SPoint:     c_default.VPointSOC,        // 使用预定义SOC点位
            DataAccess: c_default.VDataAccessFloat32, // 使用预定义浮点数数据访问配置
        },
        Addr: 0x1003,
    }
)
```

#### 预定义配置匹配规则

**点位配置匹配：**
- `Name: "A相电流"` + `Unit: "A"` → `c_default.VPointIa`
- `Name: "A相电压"` + `Unit: "V"` → `c_default.VPointUa`
- `Name: "电池电量"` + `Unit: "%"` → `c_default.VPointSOC`
- `Name: "频率"` + `Unit: "Hz"` → `c_default.VPointFreq`

**数据访问配置匹配：**
- `ValueType: c_enum.EFloat32` → `c_default.VDataAccessFloat32`
- `ValueType: c_enum.EUint16` → `c_default.VDataAccessUInt16`
- `ValueType: c_enum.EInt16` → `c_default.VDataAccessInt16`

### 启动时验证规范

#### 验证机制

系统启动时会验证所有SModbusPoint的SProtocolPoint.SPoint字段是否设置：

```go
// 在系统启动时调用
func ValidateAllPoints(deviceManager IDeviceManager) error {
    // 遍历所有设备
    // 验证协议点位的SProtocolPoint.SPoint字段是否设置
    // 如果未设置，返回错误信息
}
```

#### 验证要求

1. **SProtocolPoint字段必须设置**：所有SModbusPoint的SProtocolPoint字段都必须在创建时设置
2. **SPoint字段必须设置**：所有SProtocolPoint的SPoint字段都必须在创建时设置
3. **不可变设计**：点位创建后不允许修改，确保数据一致性
4. **类型安全**：通过类型判断确定获取方式，避免配置错误

#### 验证示例

```go
// ✅ 正确 - 完整的点位定义
var (
    protocolCurrentPoint = &SModbusPoint{
        SProtocolPoint: &SProtocolPoint{
            SPoint:     c_default.VPointIa,        // SPoint字段已设置
            DataAccess: c_default.VDataAccessFloat32, // DataAccess字段已设置
        },
        Addr: 0x1001, // Addr字段已设置
    }
)

// ❌ 错误 - SProtocolPoint字段未设置
var (
    protocolCurrentPoint = &SModbusPoint{
        Addr: 0x1001,
        // SProtocolPoint字段未设置，启动时验证会失败
    }
)

// ❌ 错误 - SPoint字段未设置
var (
    protocolCurrentPoint = &SModbusPoint{
        SProtocolPoint: &SProtocolPoint{
            // SPoint字段未设置，启动时验证会失败
            DataAccess: c_default.VDataAccessFloat32,
        },
        Addr: 0x1001,
    }
)
```

#### 验证错误处理

当启动时验证失败时，系统会：

1. **记录错误信息**：详细记录哪个设备的哪个点位验证失败
2. **阻止启动**：如果验证失败，系统不会启动，确保数据完整性
3. **提供修复建议**：在错误信息中提供修复建议

#### 验证最佳实践

1. **包级别定义**：在包级别定义点位，确保在系统启动时就能验证
2. **完整初始化**：确保所有字段都在创建时设置
3. **使用预定义配置**：优先使用预定义配置，减少配置错误
4. **测试验证**：在开发过程中定期运行验证，确保配置正确

### 注意事项

1. **不向前兼容**：完全移除v1兼容性，直接使用新的统一结构体
2. **启动时验证**：在系统启动时验证所有*SPoint字段是否设置，确保数据完整性
3. **不可变设计**：所有结构体数据在创建后不允许修改，确保数据一致性
4. **预定义配置优先**：优先使用c_default包中的预定义配置，避免手动创建
5. **业务逻辑不变**：只修改点位管理方式，不改变驱动的业务逻辑
6. **方法继承**：通过结构体嵌套自动继承方法，不需要重复实现IPoint接口方法
7. **验证完整性**：确保所有SModbusPoint都通过启动时验证
8. **配置一致性**：使用预定义配置确保相同类型点位使用相同配置