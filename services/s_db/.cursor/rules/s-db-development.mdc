---
alwaysApply: true
globs: services/s_db/**/*.go
description: s_db 服务开发规则 - SQL、Model、Service 创建标准化流程
---

# s_db 服务开发规则

`s_db` 服务是项目的数据库服务层，负责数据持久化和业务数据访问。遵循以下标准化开发流程：

## 目录结构

```text
services/s_db/
├── s_db_export_f.go              # 服务导出接口
├── s_db_basic/                   # 服务接口定义
│   └── s_db_basic_interface.go   # 所有服务接口
├── s_db_model/                   # 数据模型层
│   ├── s_db_model_const_c.go     # 常量定义
│   └── s_db_model_*.go          # 具体模型文件
├── internal/                     # 内部实现
│   ├── s_db_init_f.go           # 数据库初始化
│   └── impl/                    # 服务实现
│       └── s_db_impl_*.go       # 具体服务实现
└── go.mod                       # 模块依赖
```

## 开发流程

### 1. 创建 SQL 表结构

在 [s_db_init_f.go](mdc:services/s_db/internal/s_db_init_f.go) 的 `initConfigDatabase()` 函数中添加表创建 SQL：

```sql
// 创建 [表名] 表
_, err = g.DB().Exec(ctx, `
    CREATE TABLE IF NOT EXISTS [table_name] (
        id INTEGER PRIMARY KEY AUTOINCREMENT,  -- 或 VARCHAR(255) PRIMARY KEY
        [field1] [type] [constraints],
        [field2] [type] [constraints],
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
        updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )
`)
if err != nil {
    c_log.Fatal(ctx, err)
}
```

**SQL 规范**：

- 表名使用下划线命名法
- 主键通常为 `INTEGER PRIMARY KEY AUTOINCREMENT` 或 `VARCHAR(255) PRIMARY KEY`
- 必须包含 `created_at` 和 `updated_at` 时间戳字段
- 布尔字段使用 `BOOLEAN DEFAULT 0/1`
- 文本字段使用 `TEXT` 或 `VARCHAR(255)`
- 数值字段使用 `INTEGER` 或 `REAL`

### 2. 创建 Model 文件

在 `s_db_model/` 目录下创建 `s_db_model_[table_name]_s.go` 文件：

```go
package s_db_model

import (
    "context"
    "github.com/gogf/gf/v2/frame/g"
    "github.com/gogf/gf/v2/os/gtime"
)

// 数据库相关常量
const (
    Table[TableName] = "[table_name]"
    // 表特有字段常量（如果适用）
    Field[TableName][FieldName] = "[field_name]"
)

// [TableName] 表结构
type S[TableName]Model struct {
    g.Meta       `orm:"table:[table_name]"`
    Id           int         `json:"id" orm:"id"`
    [Field1]     string      `json:"[field1]" orm:"[field1]"`
    [Field2]     string      `json:"[field2]" orm:"[field2]"`
    CreatedAt    *gtime.Time `json:"createdAt" orm:"created_at"`
    UpdatedAt    *gtime.Time `json:"updatedAt" orm:"updated_at"`
}

// 必须实现的方法：Create, GetById, Update, UpdateFields, Delete
// 必须使用字段常量进行数据库操作
```

**Model 规范**：

- 结构体命名：`S[TableName]Model`
- 表常量命名：`Table[TableName]`
- 必须包含 `g.Meta` 和 ORM 标签
- 提供完整的 Getter/Setter 方法
- 实现标准 CRUD 操作
- **必须使用字段常量进行数据库操作**

## 字段常量规范

### 字段常量定义原则

数据库字段常量按照使用范围分为两类：

#### 1. 通用字段常量
在2个以上表中都使用的字段，定义在 `s_db_model_const_c.go` 文件中：

```go
const (
    // 通用字段 - 在2个以上表中都使用的字段
    FieldId        = "id"
    FieldName      = "name"
    FieldType      = "type"
    FieldEnabled   = "enabled"
    FieldSort      = "sort"
    FieldCreatedAt = "created_at"
    FieldUpdatedAt = "updated_at"
    FieldValue     = "value"
    FieldRemark    = "remark"
    FieldGroup     = "group_name"
)
```

#### 2. 表特有字段常量
只在特定表中使用的字段，定义在各自的模型文件中：

```go
// 在 s_db_model_alarm_history_s.go 中
const (
    // 表名
    TableAlarmHistory = "alarm_history"

    // 告警历史表特有字段
    FieldAlarmHistoryDeviceId       = "device_id"
    FieldAlarmHistorySourceDeviceId = "source_device_id"
    FieldAlarmHistoryPoint          = "point"
    FieldAlarmHistoryLevel          = "level"
    FieldAlarmHistoryTitle          = "title"
    FieldAlarmHistoryDetail         = "detail"
    FieldAlarmHistoryTriggerAt      = "trigger_at"
    FieldAlarmHistoryClearAt        = "clear_at"
)
```

### 字段常量命名规范

1. **通用字段命名格式**：`Field + 字段名（驼峰命名）`
   - 适用于在2个以上表中都使用的字段
   - 示例：`FieldId`, `FieldName`, `FieldType`, `FieldEnabled`

2. **表特有字段命名格式**：`Field + 表名 + 字段名（驼峰命名）`
   - 适用于只在特定表中使用的字段
   - 示例：`FieldAlarmHistoryDeviceId`, `FieldLogLevel`, `FieldDeviceParams`

3. **字段名转换规则**：数据库字段名（下划线）转换为 Go 常量名（驼峰）
   - 数据库字段：`device_id` → 表特有常量：`FieldAlarmHistoryDeviceId`
   - 数据库字段：`created_at` → 通用常量：`FieldCreatedAt`
   - 数据库字段：`is_public` → 表特有常量：`FieldSettingIsPublic`

### 字段常量使用规范

1. **查询条件中使用字段常量**：
```go
// ✅ 正确 - 使用字段常量
err := g.Model(TableAlarmHistory).Ctx(ctx).Where(FieldAlarmHistoryDeviceId, deviceId).Scan(&records)

// ❌ 错误 - 直接使用字符串
err := g.Model(TableAlarmHistory).Ctx(ctx).Where("device_id", deviceId).Scan(&records)
```

2. **Map 过滤和更新中使用字段常量**：
```go
// ✅ 正确 - 使用字段常量作为 Map 的 key
filters := map[string]interface{}{
    FieldAlarmHistoryDeviceId: deviceId,
    FieldAlarmHistoryLevel:    level,
    FieldAlarmHistoryPoint:    point,
}

// 更新操作
updateData := g.Map{
    FieldEnabled: true,
    FieldValue:   newValue,
}

// ❌ 错误 - 直接使用字符串
filters := map[string]interface{}{
    "device_id": deviceId,  // 应该使用 FieldAlarmHistoryDeviceId
    "level":     level,     // 应该使用 FieldAlarmHistoryLevel
}
```

3. **分页查询中的过滤条件**：
```go
func (a *SAlarmHistoryModel) GetPage(ctx context.Context, page, pageSize int, filters map[string]interface{}) ([]*SAlarmHistoryModel, int, error) {
    model := g.Model(TableAlarmHistory).Ctx(ctx)
    
    if filters != nil {
        // 使用字段常量进行过滤
        if deviceId, ok := filters[FieldAlarmHistoryDeviceId].(string); ok && deviceId != "" {
            model = model.Where(FieldAlarmHistoryDeviceId, deviceId)
        }
        
        if level, ok := filters[FieldAlarmHistoryLevel].(string); ok && level != "" {
            model = model.Where(FieldAlarmHistoryLevel, level)
        }
    }
    
    // ... 其他逻辑
}
```

### 字段常量使用的好处

1. **统一管理**：通用字段统一管理，表特有字段各自管理
2. **类型安全**：避免字符串拼写错误
3. **重构友好**：字段名变更时只需修改常量定义
4. **代码可读性**：使用有意义的常量名提高代码可读性
5. **IDE 支持**：IDE 可以提供自动补全和重构支持

### 3. 定义 Service 接口

在 [s_db_basic_interface.go](mdc:services/s_db/s_db_basic/s_db_basic_interface.go) 中添加服务接口：

```go
type I[TableName]Service interface {
    // 基础 CRUD 方法
    Create[TableName](ctx context.Context, [params]) (int, error)
    Get[TableName]ById(ctx context.Context, id int) (*s_db_model.S[TableName]Model, error)
    Update[TableName](ctx context.Context, id int, data map[string]interface{}) error
    Delete[TableName](ctx context.Context, id int) error
    
    // 查询和统计方法
    GetAll[TableName]s(ctx context.Context) ([]*s_db_model.S[TableName]Model, error)
    Get[TableName]Page(ctx context.Context, page, pageSize int, filters map[string]interface{}) ([]*s_db_model.S[TableName]Model, int, error)
    Get[TableName]Count(ctx context.Context) (int, error)
}
```

### 4. 实现 Service

在 `internal/impl/` 目录下创建 `s_db_impl_[table_name]_service_s.go` 文件：

```go
package impl

import (
    "context"
    "s_db/s_db_basic"
    "s_db/s_db_model"
    "sync"
    "github.com/gogf/gf/v2/frame/g"
    "github.com/gogf/gf/v2/os/gtime"
)

type s[TableName]ServiceImpl struct {}

var (
    [tableName]ServiceInstance s_db_basic.I[TableName]Service
    [tableName]ServiceOnce     sync.Once
)

func Get[TableName]Service() s_db_basic.I[TableName]Service {
    [tableName]ServiceOnce.Do(func() {
        [tableName]ServiceInstance = &s[TableName]ServiceImpl{}
    })
    return [tableName]ServiceInstance
}

// 实现接口方法，使用单例模式，添加详细日志记录
```

### 5. 导出 Service

在 [s_db_export_f.go](mdc:services/s_db/s_db_export_f.go) 中添加导出函数：

```go
func Get[TableName]Service() s_db_basic.I[TableName]Service {
    return impl.Get[TableName]Service()
}
```

## 命名约定

### 文件命名

- Model: `s_db_model_[table_name]_s.go`
- Service 实现: `s_db_impl_[table_name]_service_s.go`
- 常量: `s_db_model_const_c.go`

### 类型命名

- 接口: `I[TableName]Service`
- 实现: `s[TableName]ServiceImpl`
- 模型: `S[TableName]Model`
- 表常量: `Table[TableName]`

### 方法命名

- 创建: `Create[TableName]`
- 查询: `Get[TableName]ById`, `GetAll[TableName]s`
- 更新: `Update[TableName]`
- 删除: `Delete[TableName]`
- 分页: `Get[TableName]Page`
- 统计: `Get[TableName]Count`

## 技术栈

- **GoFrame v2**: Web 框架和 ORM
- **SQLite**: 轻量级数据库
- **Context**: 上下文传递
- **单例模式**: 服务实例管理

## 开发检查清单

创建新的数据库实体时，确保完成以下步骤：

- [ ] 在 `s_db_init_f.go` 中添加 SQL 表创建语句
- [ ] 在 `s_db_model/` 中创建模型文件
- [ ] 在 `s_db_basic_interface.go` 中定义服务接口
- [ ] 在 `internal/impl/` 中实现服务
- [ ] 在 `s_db_export_f.go` 中添加导出函数
- [ ] **字段常量规范**：
  - [ ] 通用字段在 `s_db_model_const_c.go` 中定义，表特有字段在各自模型文件中定义
  - [ ] 通用字段使用 `Field + 字段名` 命名格式
  - [ ] 表特有字段使用 `Field + 表名 + 字段名` 命名格式
  - [ ] 查询条件中使用字段常量而非硬编码字符串
  - [ ] Map 过滤条件中的 key 使用字段常量
  - [ ] 更新操作中的字段名使用字段常量
  - [ ] 分页查询中的过滤条件使用字段常量
  - [ ] 通用字段判断标准：只有真正在2个以上表中都使用的字段才能定义为通用字段
- [ ] 确保所有方法都有适当的日志记录
- [ ] 测试 CRUD 操作的正确性

## GoFrame 验证规范

### 1. Model 层验证标签

在 Model 结构体中使用 GoFrame 验证标签（`v:` 标签）：

```go
type SEnergyStorageStrategyModel struct {
    g.Meta      `orm:"table:energy_storage_strategy"`
    Id          int         `json:"id" orm:"id,primary,auto_increment"`
    Name        string      `json:"name" orm:"name" v:"required|length:2,50"`           // 必填，长度2-50
    Description string      `json:"description" orm:"description"`                      // 可选
    Priority    int         `json:"priority" orm:"priority" v:"required|between:1,5"`   // 必填，范围1-5
    Status      string      `json:"status" orm:"status" v:"required|in:active,inactive"` // 必填，枚举值
    CreatedAt   *gtime.Time `json:"createdAt" orm:"created_at,created_at"`
    UpdatedAt   *gtime.Time `json:"updatedAt" orm:"updated_at,updated_at"`
}
```

### 2. Service 层验证

在 Service 实现中使用 GoFrame 验证器：

```go
func (s *sServiceImpl) validateModel(ctx context.Context, model *s_db_model.SModel) error {
    // 基础字段验证（通过标签自动验证）
    if err := g.Validator().Data(model).Run(ctx); err != nil {
        return errors.Wrap(err.FirstError(), "字段验证失败")
    }
    
    // 复杂验证逻辑（手动验证）
    if model.Config != "" {
        var cfg struct {
            MinValue float64 `json:"minValue"`
            MaxValue float64 `json:"maxValue"`
        }
        if err := gjson.DecodeTo(model.Config, &cfg); err == nil {
            if cfg.MaxValue < cfg.MinValue {
                return errors.New("config.minValue 不能大于 maxValue")
            }
        }
    }
    
    return nil
}
```

### 3. 验证规则

常用验证规则：

- `required` - 必填字段
- `length:min,max` - 字符串长度范围
- `between:min,max` - 数值范围
- `in:value1,value2` - 枚举值
- `email` - 邮箱格式
- `url` - URL 格式
- `date` - 日期格式

## 类型转换规范

### 1. 使用 cvt 库进行类型转换

**依赖**：`github.com/shockerli/cvt`

**常用转换方法**：

```go
import "github.com/shockerli/cvt"

// 安全转换（带错误返回）
id, err := cvt.IntE(value)
name, err := cvt.StringE(value)
enabled, err := cvt.BoolE(value)
price, err := cvt.Float64E(value)

// 简单转换（不返回错误，失败返回零值）
id := cvt.Int(value)
name := cvt.String(value)
enabled := cvt.Bool(value)
price := cvt.Float64(value)
```

### 2. Service 层类型转换示例

```go
func (s *sServiceImpl) GetPage(ctx context.Context, page, pageSize int, filters map[string]interface{}) ([]*s_db_model.SModel, int, error) {
    model := g.Model(s_db_model.TableName).Ctx(ctx)
    
    // 使用 cvt 进行类型转换
    if priority, ok := filters[s_db_model.FieldPriority]; ok {
        p := cvt.Int(priority)
        if p >= 1 && p <= 5 {
            model = model.Where(s_db_model.FieldPriority, p)
        }
    }
    
    if keyword, ok := filters["keyword"]; ok {
        kw := cvt.String(keyword)
        if strings.TrimSpace(kw) != "" {
            like := "%" + strings.TrimSpace(kw) + "%"
            model = model.WhereLike(s_db_model.FieldName, like)
        }
    }
    
    // ... 其他逻辑
}
```

## ORM 标签完整规范

### 1. 主键标签

```go
Id int `json:"id" orm:"id,primary,auto_increment"` // 自增主键
```

### 2. 时间戳标签

```go
CreatedAt *gtime.Time `json:"createdAt" orm:"created_at,created_at"` // 创建时自动填充
UpdatedAt *gtime.Time `json:"updatedAt" orm:"updated_at,updated_at"` // 更新时自动填充
```

### 3. 普通字段标签

```go
Name string `json:"name" orm:"name"` // 基本字段
```

### 4. CRUD 操作中排除字段

```go
// Create 时排除 ID（让数据库自动生成）
_, err := g.Model(TableName).Ctx(ctx).FieldsEx(FieldId).Insert(m)

// Update 时排除 ID 和 CreatedAt
_, err := g.Model(TableName).Ctx(ctx).Where(FieldId, m.Id).FieldsEx(FieldId, FieldCreatedAt).Update(m)
```

## 自动字段设置规范

### 1. createdBy 字段自动设置

在 Service 的 Create 方法中自动设置 `createdBy` 字段：

```go
func (s *sServiceImpl) CreateModel(ctx context.Context, model *s_db_model.SModel) (int, error) {
    // 验证模型
    if err := s.validateModel(ctx, model); err != nil {
        return 0, err
    }
    
    // 自动设置 createdBy 为 admin
    model.CreatedBy = "admin"
    
    // 创建记录（时间戳和ID由数据库自动处理）
    if err := model.Create(ctx); err != nil {
        return 0, errors.Wrap(err, "创建记录失败")
    }
    
    // 获取刚插入的记录ID
    lastIdValue, err := g.Model(s_db_model.TableName).Ctx(ctx).
        Fields(s_db_model.FieldId).
        Order("id DESC").
        Limit(1).
        Value()
    if err != nil {
        return 0, errors.Wrap(err, "获取新创建的记录ID失败")
    }
    
    return cvt.IntE(lastIdValue)
}
```

### 2. SQL 表中设置默认值

在数据库表创建时设置默认值：

```sql
CREATE TABLE IF NOT EXISTS table_name (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(255) NOT NULL,
    created_by VARCHAR(255) NOT NULL DEFAULT 'admin',  -- 设置默认值
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
)
```

## Service 接口设计规范

### 1. 使用 Model 结构体而非 map

**推荐**：

```go
type IEnergyStorageStrategyService interface {
    CreateEnergyStorageStrategy(ctx context.Context, model *s_db_model.SEnergyStorageStrategyModel) (int, error)
    UpdateEnergyStorageStrategy(ctx context.Context, model *s_db_model.SEnergyStorageStrategyModel) error
    DeleteEnergyStorageStrategy(ctx context.Context, id int) error
}
```

**不推荐**：

```go
type IEnergyStorageStrategyService interface {
    CreateEnergyStorageStrategy(ctx context.Context, data map[string]interface{}) (int, error)
    UpdateEnergyStorageStrategy(ctx context.Context, id int, data map[string]interface{}) error
}
```

### 2. Controller 层构造 Model

在 Controller 层直接构造 Model 结构体：

```go
func (c *Controller) CreateResource(ctx context.Context, req *v1.CreateResourceReq) (*v1.CreateResourceRes, error) {
    // 构造 Model
    model := &s_db_model.SResourceModel{
        Name:        req.Name,
        Description: req.Description,
        Priority:    req.Priority,
        Status:      req.Status.String(),
        Config:      marshalJSON(req.Config),
    }
    
    // 调用服务层
    id, err := s_db.GetResourceService().CreateResource(ctx, model)
    if err != nil {
        return nil, err
    }
    
    return &v1.CreateResourceRes{}, nil
}
```

## 示例参考

参考现有实现：

- [s_db_model_energy_storage_strategy_s.go](mdc:services/s_db/s_db_model/s_db_model_energy_storage_strategy_s.go) - 完整的 Model 实现（含 ORM 标签和验证标签）
- [s_db_impl_energy_storage_strategy_service_s.go](mdc:services/s_db/internal/impl/s_db_impl_energy_storage_strategy_service_s.go) - 完整的 Service 实现（含验证和类型转换）
- [energy_storage_strategy_v1_controller.go](mdc:application/internal/controller/policy/energy_storage_strategy_v1_controller.go) - 完整的 Controller 实现（使用 Model 传递）
- [s_db_basic_interface.go](mdc:services/s_db/s_db_basic/s_db_basic_interface.go) - 接口定义示例
